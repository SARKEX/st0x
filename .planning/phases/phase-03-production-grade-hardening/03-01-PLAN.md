---
phase: 03-production-grade-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/config/networks.ts
  - src/lib/clients/raindex.ts
  - src/lib/server/accessCodes.ts
  - .env.example
autonomous: true
requirements: [SEC-01]
requirements_addressed: [SEC-01]
tags: [phase-3, sec-01, env-vars, alchemy-rotation, base-rpc]
must_haves:
  truths:
    - "The literal Alchemy URL `https://base-mainnet.g.alchemy.com/v2/y3BXawVv5uuP_g8BaDlKbKoTBGHo9zD9` does not appear anywhere under src/"
    - "Client-side `networks.ts` and `raindex.ts` read `PUBLIC_BASE_RPC_URL` from `$env/dynamic/public` with a public-RPC dev fallback"
    - "Server-side `accessCodes.ts` reads `BASE_RPC_URL` from `$env/dynamic/private` and throws at module load when missing in production"
    - "`.env.example` documents both `PUBLIC_BASE_RPC_URL` and `BASE_RPC_URL` so contributors know what to set"
  artifacts:
    - path: src/lib/config/networks.ts
      provides: "Client-bundled network config reading PUBLIC_BASE_RPC_URL"
      contains: "publicEnv.PUBLIC_BASE_RPC_URL"
    - path: src/lib/clients/raindex.ts
      provides: "SETTINGS_YAML interpolated with PUBLIC_BASE_RPC_URL"
      contains: "publicEnv.PUBLIC_BASE_RPC_URL"
    - path: src/lib/server/accessCodes.ts
      provides: "Server viem client reading BASE_RPC_URL with module-load fail-closed"
      contains: "env.BASE_RPC_URL"
  key_links:
    - from: src/lib/config/networks.ts
      to: $env/dynamic/public
      via: "publicEnv.PUBLIC_BASE_RPC_URL || dev fallback"
      pattern: "PUBLIC_BASE_RPC_URL"
    - from: src/lib/server/accessCodes.ts
      to: $env/dynamic/private
      via: "env.BASE_RPC_URL with !dev throw guard"
      pattern: "BASE_RPC_URL.*required in production"
---

<objective>
Remove the hardcoded Alchemy API key from `src/lib/config/networks.ts:48,51`, `src/lib/clients/raindex.ts:26`, and `src/lib/server/accessCodes.ts:11` by reading `PUBLIC_BASE_RPC_URL` (client) and `BASE_RPC_URL` (server) from SvelteKit env modules. Establishes the env var that REL-02 (Wave 5) will reuse to build the fallback chain. Same Alchemy key on both sides per D-02.

Purpose: Closes SEC-01. Eliminates the committed-key security finding from CONCERNS.md and unblocks REL-02. The actual Alchemy key rotation is a Vercel-side ops step documented in 03-RUNBOOK.md (Plan 03-10) — this plan ships the code so the rotation can land on next deploy.

Output: 3 source files refactored to read env vars; `.env.example` updated; module-load throw guard on server. No behavior change when the env vars resolve to the same Alchemy URL value (atomic swap-then-rotate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-03-production-grade-hardening/03-CONTEXT.md
@.planning/phases/phase-03-production-grade-hardening/03-RESEARCH.md
@.planning/phases/phase-03-production-grade-hardening/03-PATTERNS.md
@.planning/phases/phase-03-production-grade-hardening/03-VALIDATION.md
@src/lib/config/networks.ts
@src/lib/clients/raindex.ts
@src/lib/server/accessCodes.ts
@src/routes/api/cron/snapshots/+server.ts

<interfaces>
<!-- Existing precedents the executor must use verbatim -->

From src/routes/+layout.svelte:5 (existing PUBLIC_* read pattern):
```typescript
import { env as publicEnv } from '$env/dynamic/public';
// publicEnv.PUBLIC_WALLETCONNECT_ID
```

From src/routes/api/cron/snapshots/+server.ts:42-49 (existing module-local fail-closed precedent):
```typescript
const cronSecret = env.CRON_SECRET;
// Fail closed in production if CRON_SECRET is missing
if (!cronSecret && !dev) {
    console.error('[Cron] CRON_SECRET is not configured');
    return json({ error: 'Cron endpoint not configured' }, { status: 503 });
}
```
(Adapt to module-top throw per RESEARCH Pitfall 2 — fires at cold start, not first request.)

From src/lib/server/snapshots/generator.ts:14 (RPC_URLS canonical shape — REL-02 will reuse this exact pattern):
```typescript
const RPC_URLS = [networks[0].rpcUrl, ...networks[0].fallbackRpcUrls];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SEC-01 client-side env-var swap (networks.ts + raindex.ts)</name>
  <read_first>
    - src/lib/config/networks.ts (full file — lines 1-30 imports, lines 40-60 base config block with hardcoded URL on lines 48 and 51)
    - src/lib/clients/raindex.ts (full file — lines 1-50; SETTINGS_YAML at lines 22-44 with hardcoded URL on line 26)
    - src/routes/+layout.svelte:1-30 (existing `import { env as publicEnv } from '$env/dynamic/public'` pattern at line 5)
    - .planning/phases/phase-03-production-grade-hardening/03-PATTERNS.md §"Wave 1 — SEC-01 (env-var swap)" (lines 41-97 for both files)
    - .planning/phases/phase-03-production-grade-hardening/03-RESEARCH.md §"SEC-01: Client-side env-var read" (lines 846-888)
  </read_first>
  <files>src/lib/config/networks.ts, src/lib/clients/raindex.ts</files>
  <action>
Implement SEC-01 client-side per D-02 (single Alchemy key, exposed via PUBLIC_*).

**File 1 — `src/lib/config/networks.ts`:**

Add at the top of the imports block:
```typescript
import { env as publicEnv } from '$env/dynamic/public';
```

Above the `networks` array declaration (around line 30, after type definitions), define:
```typescript
const PRIMARY_RPC = publicEnv.PUBLIC_BASE_RPC_URL || 'https://base-rpc.publicnode.com';
```

Replace the literal URL on line 48 (currently `'https://base-mainnet.g.alchemy.com/v2/y3BXawVv5uuP_g8BaDlKbKoTBGHo9zD9'`) with `PRIMARY_RPC`. Replace the same literal URL on line 51 (inside `fallbackRpcUrls` array) with `PRIMARY_RPC`. Leave all other entries in `fallbackRpcUrls` unchanged (publicnode, llamarpc, meowrpc, blastapi, tenderly).

Final shape mirrors RESEARCH §"SEC-01: Client-side env-var read" lines 850-868. The duplicated `PRIMARY_RPC` in `fallbackRpcUrls` is intentional per RESEARCH (simpler than de-dup; falling back to itself is harmless because viem's fallback transport tries each entry once).

**File 2 — `src/lib/clients/raindex.ts`:**

Add at the top of the imports block:
```typescript
import { env as publicEnv } from '$env/dynamic/public';
```

Above the existing `SETTINGS_YAML` const (around line 22), define:
```typescript
const PRIMARY_RPC = publicEnv.PUBLIC_BASE_RPC_URL || 'https://base-rpc.publicnode.com';
```

Replace the existing `SETTINGS_YAML = \`...\`` block (lines 22-44, the const-string with hardcoded URL on line 26) with a template literal that interpolates `${PRIMARY_RPC}` at the position previously occupied by the hardcoded URL. Preserve every other YAML field verbatim (chain-id, network-id, currency, subgraphs, metaboards, deployers, accounts — DO NOT touch any of these). Final shape mirrors RESEARCH §"SEC-01" raindex.ts block lines 870-888.

**No other behavior changes.** This is purely a literal-to-env-var swap. The dev fallback (`'https://base-rpc.publicnode.com'`) keeps `npm run dev` working without setting the env var.
  </action>
  <behavior>
    - Build with `PUBLIC_BASE_RPC_URL=https://test-rpc.example.com npm run build` produces a bundle where `networks[0].rpcUrl === 'https://test-rpc.example.com'`
    - Build without the env var falls back to `'https://base-rpc.publicnode.com'`
    - SETTINGS_YAML in raindex.ts contains the env-resolved URL after build, not the literal Alchemy URL
    - `grep -r "y3BXawVv5uuP" src/lib/config src/lib/clients` returns 0 hits
  </behavior>
  <verify>
    <automated>! grep -r "y3BXawVv5uuP" src/lib/config/ src/lib/clients/ &amp;&amp; grep -c "PUBLIC_BASE_RPC_URL" src/lib/config/networks.ts src/lib/clients/raindex.ts | grep -v ":0$" | wc -l | xargs -I {} test {} -ge 2 &amp;&amp; npm run check 2>&amp;1 | tail -3 | grep -E "[0-3] errors? and [0-9]+ warnings?" &amp;&amp; npm test -- --run accessCodes referrals 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -r "y3BXawVv5uuP" src/lib/config/networks.ts src/lib/clients/raindex.ts` (returns 0 hits — committed key absent from these files)
    - `grep -c "publicEnv.PUBLIC_BASE_RPC_URL" src/lib/config/networks.ts` returns ≥ 1
    - `grep -c "publicEnv.PUBLIC_BASE_RPC_URL" src/lib/clients/raindex.ts` returns ≥ 1
    - `grep -c "import { env as publicEnv } from '\$env/dynamic/public'" src/lib/config/networks.ts` returns 1
    - `grep -c "import { env as publicEnv } from '\$env/dynamic/public'" src/lib/clients/raindex.ts` returns 1
    - `npm run check` passes at the Phase 2 baseline (≤ 3 errors)
    - `fallbackRpcUrls` array still contains `'https://base-rpc.publicnode.com'`, `'https://base.llamarpc.com'`, `'https://base.meowrpc.com'`, `'https://base-mainnet.public.blastapi.io'`, `'https://gateway.tenderly.co/public/base'` (verified by grep)
  </acceptance_criteria>
  <done>
    - networks.ts and raindex.ts read PUBLIC_BASE_RPC_URL from $env/dynamic/public
    - Hardcoded Alchemy URL string `y3BXawVv5uuP_g8BaDlKbKoTBGHo9zD9` removed from both files
    - svelte-check baseline ≤ 3 errors preserved
    - Public RPC fallback keeps dev mode functional without the env var
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SEC-01 server-side env-var swap with module-load fail-closed (accessCodes.ts) + .env.example</name>
  <read_first>
    - src/lib/server/accessCodes.ts (full file — focus on lines 1-12 imports + basePublicClient definition)
    - src/routes/api/cron/snapshots/+server.ts:42-49 (CRON_SECRET module-local fail-closed precedent)
    - .planning/phases/phase-03-production-grade-hardening/03-PATTERNS.md §"`src/lib/server/accessCodes.ts` — SEC-01 portion" (lines 100-119)
    - .planning/phases/phase-03-production-grade-hardening/03-RESEARCH.md §"SEC-01 + REL-02: Fallback transport with env-var-driven URL list" (lines 802-844 — note REL-02 wraps this in fallback transport in Wave 5; Wave 1 only swaps the URL source)
    - .planning/phases/phase-03-production-grade-hardening/03-RESEARCH.md §"Pitfall 2: Module-load throw vs first-request throw" (lines 674-694)
    - .env.example (existing — confirm structure before adding new entries)
  </read_first>
  <files>src/lib/server/accessCodes.ts, .env.example</files>
  <action>
Per D-02 + D-02b: Server-side reads `BASE_RPC_URL` from `$env/dynamic/private` with module-load fail-closed (mirrors CRON_SECRET precedent).

**File 1 — `src/lib/server/accessCodes.ts`:**

Add to the imports block at the top of the file (after the existing `env` import on line 4):
```typescript
import { dev } from '$app/environment';
```

REPLACE the existing `basePublicClient` definition at lines 7-12 with:
```typescript
// Same Alchemy key on both sides per D-02 (single key, single rotation event).
// REL-02 (Wave 5) wraps this in viem's fallback([...]) transport using the same
// RPC_URLS shape as src/lib/server/snapshots/generator.ts:14.
const PRIMARY_RPC_URL = env.BASE_RPC_URL;
if (!dev && !PRIMARY_RPC_URL) {
    throw new Error('[accessCodes] BASE_RPC_URL required in production');
}

// Create a public client for Base network for signature verification.
// Supports ECDSA (EOA), EIP-1271 (Smart Contracts), and EIP-6492 (Undeployed).
const basePublicClient = createPublicClient({
    chain: base,
    transport: http(PRIMARY_RPC_URL || 'https://base-rpc.publicnode.com')  // dev fallback
});
```

This is a Wave 1 swap — the `fallback([...])` Transport upgrade lands in Wave 5 / REL-02 (Plan 03-07). Per RESEARCH Pitfall 2, the throw lives at module top so cold-start surfaces in Vercel Logs immediately rather than at first request.

**The `verifyWalletSignature` function body at lines 75-120 is NOT changed in this plan** — its OBS-04 instrumentation (`recordRpcAttempt` with `rpc_url: 'alchemy-base-mainnet'`) stays as-is. REL-02 in Wave 5 updates the label to `'fallback-chain-base'` when the fallback transport lands.

**File 2 — `.env.example`:**

Append to the file (preserve existing entries; do NOT delete or reorder):
```
# Base mainnet RPC — Alchemy app URL (or compatible). Same value used on both sides per
# Phase 3 D-02 (single key both sides; bundle key is exposed regardless, splitting doubles
# operational surface for marginal blast-radius reduction).
PUBLIC_BASE_RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY_HERE
BASE_RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY_HERE
```
  </action>
  <behavior>
    - In dev mode (`dev === true`), missing BASE_RPC_URL falls back to publicnode RPC and module loads fine
    - In production mode (`dev === false`), missing BASE_RPC_URL throws at module-top during cold start
    - `import('./accessCodes')` in production with BASE_RPC_URL set returns the module successfully
    - The `'alchemy-base-mainnet'` label in OBS-04 fan-out at lines 92, 103, 113 is preserved unchanged (REL-02 / Wave 5 updates it)
  </behavior>
  <verify>
    <automated>! grep -r "y3BXawVv5uuP" src/lib/server/accessCodes.ts &amp;&amp; grep -c "env.BASE_RPC_URL" src/lib/server/accessCodes.ts | xargs test 1 -le &amp;&amp; grep -c "BASE_RPC_URL required in production" src/lib/server/accessCodes.ts | xargs test 1 -le &amp;&amp; grep -c "PUBLIC_BASE_RPC_URL" .env.example | xargs test 1 -le &amp;&amp; grep -c "^BASE_RPC_URL" .env.example | xargs test 1 -le &amp;&amp; npm run check 2>&amp;1 | tail -3 &amp;&amp; npm test -- --run accessCodes 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -r "y3BXawVv5uuP" src/lib/server/accessCodes.ts` (returns 0)
    - `grep -c "env.BASE_RPC_URL" src/lib/server/accessCodes.ts` ≥ 1
    - `grep -c "BASE_RPC_URL required in production" src/lib/server/accessCodes.ts` returns 1
    - `grep -c "import { dev } from '\$app/environment'" src/lib/server/accessCodes.ts` returns 1
    - `grep -c "^PUBLIC_BASE_RPC_URL=" .env.example` returns 1
    - `grep -c "^BASE_RPC_URL=" .env.example` returns 1
    - **Phase-exit gate (carry-forward)**: `! grep -r "y3BXawVv5uuP" src/` returns 0 hits across the entire src/ tree
    - svelte-check baseline ≤ 3 errors preserved
    - Existing `accessCodes.test.ts` still passes (no test changes in this plan)
  </acceptance_criteria>
  <done>
    - accessCodes.ts reads BASE_RPC_URL from $env/dynamic/private
    - Module-load throw fires in production when env var is missing
    - .env.example documents both PUBLIC_BASE_RPC_URL + BASE_RPC_URL
    - Hardcoded Alchemy key fully removed from src/ tree (verified by phase-exit grep)
    - svelte-check + test suite green at Phase 2 baseline
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo + JS bundle | Anyone with read access to source or production bundle sees the Alchemy URL |
| Vercel env vars | Only authorized maintainers can read/rotate; production + preview separated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SEC-01-01 | Information Disclosure | networks.ts:48,51 + raindex.ts:26 + accessCodes.ts:11 hardcoded Alchemy key | mitigate (BLOCKER — closes the audit finding) | Replace literal URL with env-var read; rotation procedure documented in 03-RUNBOOK.md (Plan 03-10) |
| T-03-SEC-01-02 | Tampering / Denial of Service | Production lambda boots without BASE_RPC_URL set | mitigate | Module-top throw mirrors CRON_SECRET pattern; surfaces in Vercel Logs at cold start, not at first request |
| T-03-SEC-01-03 | Denial of Service | Bundle still depends on Alchemy quota (single key both sides per D-02) | accept | Documented residual risk per CONTEXT D-02 specifics; "split keys" deferred until quota abuse is measurable |
| T-03-SEC-01-04 | Tampering | Dev fallback URL `https://base-rpc.publicnode.com` is reachable from production lambda if env var resolution silently fails | mitigate | Production fail-closed throw runs BEFORE the `||` fallback evaluates in lambda boot; production never sees the dev fallback |
</threat_model>

<verification>
- `npm run check` passes at the Phase 2 baseline (≤ 3 errors)
- `npm test -- --run accessCodes referrals` passes (existing tests unmodified)
- `! grep -r "y3BXawVv5uuP" src/` returns 0 hits across all of src/
- `grep -c "PUBLIC_BASE_RPC_URL" src/lib/config/networks.ts src/lib/clients/raindex.ts` ≥ 2 total
- `grep -c "BASE_RPC_URL" src/lib/server/accessCodes.ts` ≥ 1
- `.env.example` contains both `PUBLIC_BASE_RPC_URL=` and `BASE_RPC_URL=` entries

**Phase 2 carry-forward gates (must remain green):**
- TRADE-01 IO-perspective lockdown — no changes (this plan does not touch marketOrderExecution.ts, transaction.ts, or orderPerspective.ts)
- TRADE-02 cycle severance — no changes
- `failWith()` count in marketOrderExecution.ts ≥ 12 — no changes
- `EMERGENCY_RATIO_MULTIPLIER` count = 0 — no changes
- staleTime: Infinity — no changes
</verification>

<success_criteria>
- Hardcoded Alchemy key absent from `src/lib/config/networks.ts`, `src/lib/clients/raindex.ts`, and `src/lib/server/accessCodes.ts`
- Both client (`PUBLIC_BASE_RPC_URL`) and server (`BASE_RPC_URL`) env-var paths in place
- Server-side module-load throw guards production; dev mode tolerates missing env vars
- `.env.example` documents both env vars
- All existing tests pass; svelte-check baseline preserved
- Wave 5 (REL-02) can read `BASE_RPC_URL` and `networks[0].fallbackRpcUrls` to build the fallback transport
</success_criteria>

<output>
After completion, create `.planning/phases/phase-03-production-grade-hardening/03-01-SUMMARY.md` per the GSD summary template.
</output>
