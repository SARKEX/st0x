---
phase: 01-ui-driven-e2e-order-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md
autonomous: true
requirements: [TEST-10]
must_haves:
  truths:
    - "Every existing test under tests/lib/** + tests/integration/marketOrder/** related to order deployment, market execution, side semantics, and freshness has been classified into the audit matrix"
    - "Each row in the matrix maps to a bug-class from .planning/codebase/CONCERNS.md or a TEST-08 failure mode"
    - "Each cell contains either a test file path OR an explicit `—` (gap)"
    - "Every gap is classified as `must-fix` or `nice-to-have` per D-13 must-fix bar"
    - "Must-fix gaps are itemized so Plan 01-08 can mechanically convert them into tasks"
  artifacts:
    - path: .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md
      provides: "TEST-10 deliverable — per-REQ coverage matrix per D-12 + must-fix list for TEST-11"
  key_links:
    - from: 01-AUDIT.md
      to: tests/lib/** + tests/integration/marketOrder/**
      via: "every existing order-related test referenced or explicitly absent"
      pattern: "tests/(lib|integration/marketOrder)/.*\\.test\\.ts"
    - from: 01-AUDIT.md
      to: .planning/codebase/CONCERNS.md
      via: "rows = bug-class register"
      pattern: "TRADE-0[1-4]|TEST-08"
---

<objective>
Produce the TEST-10 audit deliverable — a single coverage matrix mapping every relevant existing test to the bug-class register, with every gap classified as must-fix or nice-to-have per D-13.

Purpose: Without the audit, we cannot identify what TEST-11 must close. Pure analysis — no code execution dependencies; can run in parallel with the smoke-stack work in 01-01.
Output: `.planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md` with a fully-populated matrix and explicit must-fix list. Plan 01-08 mechanically converts the must-fix list into tasks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ui-driven-e2e-order-test-coverage/01-CONTEXT.md
@.planning/phases/01-ui-driven-e2e-order-test-coverage/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory existing tests and produce TEST-10 audit matrix with must-fix classification</name>
  <files>
    .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md
  </files>
  <read_first>
    - .planning/phases/01-ui-driven-e2e-order-test-coverage/01-RESEARCH.md §"Audit Matrix Template" (D-12 deliverable shape — pre-populated with 15 rows that this audit must verify and extend)
    - .planning/codebase/CONCERNS.md (bug-class register; rows of the matrix)
    - .planning/codebase/TESTING.md (test layout conventions)
    - List ALL files under: `ls tests/lib/services/ tests/lib/utils/ tests/lib/types/ tests/lib/stores/ tests/integration/marketOrder/ 2>/dev/null` — every order-related test must appear in the matrix or be deliberately excluded with rationale
    - Headers/describe blocks of each existing test (read first ~30 lines per file to classify what it covers)
  </read_first>
  <action>
    Walk every test file under:
    - `tests/lib/services/marketOrderExecution.test.ts`
    - `tests/lib/utils/marketOrderFill.test.ts` (19 cases)
    - `tests/lib/utils/marketPrice.test.ts`
    - `tests/lib/utils/quote.test.ts`
    - `tests/lib/utils/tokenMath.test.ts`
    - `tests/lib/utils/approvalDecimals.test.ts`
    - `tests/lib/types/orderPerspective.test.ts`
    - All `tests/integration/marketOrder/*.test.ts` (anvil-fork + 7 replay specs)
    - Any `tests/lib/stores/transactionStore.test.ts` / `handleTakeOrders.test.ts` / `validateDeploymentArgs.test.ts` / `marketHours.test.ts` if present
    - Any `tests/lib/services/observability/` related tests

    For each, read first ~30 lines (describe blocks + it() titles) to classify what bug-class / failure mode it covers.

    Author `.planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md` with the exact shape from RESEARCH §"Audit Matrix Template" — but VERIFY each cell against the actual file existence and content. Update the pre-seeded matrix from RESEARCH (15 rows) to reflect ground truth.

    Required structure:

    ```markdown
    # Phase 1 — Order Test Coverage Audit (TEST-10)

    **Audited:** YYYY-MM-DD
    **Method:** Walked tests/lib/** + tests/integration/marketOrder/**, classified each test against the bug-class register from .planning/codebase/CONCERNS.md + v1.0 milestone close.

    ## Coverage Matrix

    | Bug-Class Row | unit (tests/lib/) | service-integration (tests/integration/marketOrder/) | UI E2E (tests/integration/ui/) | Gap (must-fix? Y/N + rationale) |
    |---------------|-------------------|------------------------------------------------------|-------------------------------|---------------------------------|
    | TRADE-01 — INPUT/OUTPUT side semantics | <verified path or —> | <verified path or —> | (planned: marketBuy.spec.ts + marketSell.spec.ts in 01-04/01-05) | <Y or N + reason> |
    | TRADE-02 — transaction.ts ↔ marketOrderExecution.ts cycle severance | ... | ... | ... | ... |
    | TRADE-03 — on-chain freshness pre-flight | ... | ... | ... | ... |
    | TRADE-04 — mode×side spend/asset-anchored symmetry | ... | ... | ... | ... |
    | TEST-08a — slippage exceeded | ... | ... | (planned: marketFailures.spec.ts) | ... |
    | TEST-08b — no liquidity | ... | ... | (planned) | ... |
    | TEST-08c — stale oracle | ... | ... | (planned) | ... |
    | TEST-08d — insufficient balance | ... | ... | (planned) | ... |
    | TEST-08e — market-hours gating | ... | — | (planned) | ... |
    | Limit-deploy correct-vault deposit | ... | ... | (planned: limitDeploy.spec.ts) | ... |
    | Simulated counterparty fill on fork | — | — | (planned) | N — covered by 01-07 |
    | DCA-deploy | ... | — | — | N — nice-to-have per D-13 → 999.x |
    | Hydration failure recovery | ... | ... | — | <Y/N rationale> |
    | Stale wallet session | ... | ... | — | <Y/N rationale> |
    | Slippage cap exceeded (per-order) | ... | ... | (covered by TEST-08a) | <Y/N rationale> |
    | <any additional rows discovered during walk> | ... | ... | ... | ... |

    ## Must-Fix Gap List (TEST-11 input)

    Per D-13 must-fix bar: gap is must-fix iff (1) corresponds to a TRADE-01..04 boundary regression class with no test in any column, OR (2) corresponds to a TEST-08 failure mode lacking E2E coverage after this phase's E2E plans (01-04..01-07) land.

    1. <bug-class> — <missing column(s)> — <suggested test file path + spec>
    2. ...

    (Empty if no gaps — explicitly state "No must-fix gaps remain after 01-04..01-07 land.")

    ## Nice-to-Have / Backlog (→ 999.x)

    Per D-13: everything outside the must-fix bar.

    1. DCA-deploy E2E — captured in CONTEXT Deferred Ideas; backlog as 999.x.
    2. ...

    ## Audit Method Notes

    - Test files walked: <COUNT>
    - Tests classified: <COUNT>
    - Tests excluded as out-of-scope: <COUNT> (with rationale per file)
    - Source-of-truth for bug-class rows: .planning/codebase/CONCERNS.md + v1.0 ROADMAP cross-cutting constraints
    ```

    Notes for must-fix classification:
    - **TRADE-01..04 rows**: After 01-04..01-07 land, every TRADE-01..04 row has at least one cell populated. If any TRADE-01..04 row has all three columns empty (rare — most are covered by existing v1.0 unit/integration tests), it is must-fix.
    - **TEST-08 rows**: After 01-06 (TEST-08 plan) lands, every TEST-08 sub-row has UI E2E coverage. Any TEST-08 row that 01-06 leaves uncovered is must-fix.
    - **`marketHours.test.ts` likely doesn't exist** (per RESEARCH matrix template): if grep confirms, that's a candidate must-fix (TEST-08e unit coverage) — verify in audit.
    - **`tests/integration/marketOrder/replay-fallback-no-liquidity.test.ts`** etc. exist per Phase 4 04-08 — confirm every replay-*.test.ts is mapped.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md && grep -E "^# Phase 1 — Order Test Coverage Audit" .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md && grep -E "^## Coverage Matrix$" .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md && grep -E "^## Must-Fix Gap List" .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md && grep -E "^## Nice-to-Have" .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md && [ "$(grep -c '^| TRADE-0[1-4]\\|^| TEST-08[a-e]\\|^| Limit-deploy\\|^| Simulated counterparty\\|^| DCA-deploy\\|^| Hydration\\|^| Stale wallet' .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md)" -ge 12 ]</automated>
  </verify>
  <acceptance_criteria>
    - `01-AUDIT.md` exists at `.planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md`
    - Contains all 4 required `##` sections: `Coverage Matrix`, `Must-Fix Gap List`, `Nice-to-Have / Backlog`, `Audit Method Notes`
    - Matrix has ≥ 12 rows from the bug-class register (TRADE-01..04 + 5 TEST-08 sub-rows + limit-deploy + simulated counterparty + DCA-deploy)
    - Every cell is either a path under `tests/lib/` or `tests/integration/`, an explicit `—`, or `(planned: ...)` for UI E2E plans not yet shipped
    - Must-fix list is either empty (with explicit "No must-fix gaps remain after 01-04..01-07 land.") OR enumerated 1, 2, ...
    - Audit Method Notes records counts of files walked / classified / excluded
  </acceptance_criteria>
  <done>01-AUDIT.md committed; Plan 01-08 has the must-fix list it needs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auditor's classification ↔ ground truth | Misclassifying a test (saying X covers TRADE-03 when it actually doesn't) leaves a real gap unflagged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02-01 | Information Disclosure (false-coverage claim) | Audit matrix cells | mitigate | Each cell that names a test path is verified against the file's describe blocks (read_first requires reading top of file before claiming coverage). |
| T-1-02-02 | Tampering | Must-fix list under-counts gaps | mitigate | D-13 bar applied mechanically: any TRADE-01..04 row with all three columns `—` post-01-07 is automatically must-fix; any TEST-08 row uncovered after 01-06 lands is automatically must-fix. No discretion. |
</threat_model>

<verification>
- 01-AUDIT.md exists with all 4 required sections
- Matrix row count ≥ 12 (covers full bug-class register)
- Must-fix list either explicitly empty or enumerated
- Plan 01-08 can mechanically read the must-fix list and produce tasks from it
</verification>

<success_criteria>
- TEST-10 deliverable shipped as a single matrix per D-12
- D-13 must-fix bar applied to every gap; everything else routed to 999.x backlog
- Audit is pure documentation — no code changes; can run in parallel with 01-01
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-driven-e2e-order-test-coverage/01-02-SUMMARY.md`.
</output>
