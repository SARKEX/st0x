---
phase: 01-ui-driven-e2e-order-test-coverage
plan: 08
type: execute
wave: 4
depends_on: [02, 04, 05, 06, 07]
files_modified:
  - .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md
autonomous: true
requirements: [TEST-11]
must_haves:
  truths:
    - "Every gap classified must-fix in 01-AUDIT.md (TEST-10) has a closing test added in this milestone OR is documented as already covered by 01-04..01-07 with rationale"
    - "Audit matrix is re-walked after 01-04..01-07 land — UI E2E columns now contain real test paths, not `(planned: ...)` stubs"
    - "Must-Fix Gap List is empty (all gaps closed) OR every remaining item has an explicit closing-task spec or backlog reference"
  artifacts:
    - path: .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md
      provides: "Updated audit with UI E2E columns populated and must-fix list resolved"
    - path: "(test files created on demand for any unclosed must-fix items)"
      provides: "Test additions closing TRADE-01..04 / TEST-08 gaps that 01-04..01-07 did not cover"
  key_links:
    - from: 01-AUDIT.md must-fix list
      to: closing test files (paths added per gap)
      via: "each must-fix item maps to a created test path or a documented `Already covered by:` reference"
      pattern: "Closed by:|Already covered by:"
---

<objective>
Close every must-fix gap from the TEST-10 audit (Plan 01-02 deliverable) AFTER 01-04..01-07 have landed and populated the UI E2E columns. Per CONTEXT D-13.2: "must-fix bar applies after E2E specs land."

Purpose: TEST-11 — Every gap classified "must-fix" in TEST-10 has tests added in this milestone.
Output: Updated `01-AUDIT.md` with all UI E2E cells populated to real test paths; either an empty Must-Fix list or every remaining item has a `Closed by: <path>` or `Already covered by: <path>` annotation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md
@.planning/codebase/CONCERNS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-walk audit matrix; close every remaining must-fix gap</name>
  <files>
    .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md
  </files>
  <read_first>
    - .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md (the audit shipped by 01-02)
    - tests/integration/ui/smoke.spec.ts + marketBuy.spec.ts + marketSell.spec.ts + marketFailures.spec.ts + limitDeploy.spec.ts (everything 01-01 + 01-04..01-07 added — read describe blocks + it titles)
    - .planning/phases/01-ui-driven-e2e-order-test-coverage/01-CONTEXT.md §"D-13" must-fix bar
    - tests/lib/utils/marketHours.test.ts if it exists (per RESEARCH §"Audit Matrix Template" — likely doesn't; if absent, that's a candidate must-fix gap for TEST-08e unit coverage)
  </read_first>
  <action>
    Re-walk the audit matrix in `01-AUDIT.md`. For each row:

    1. **UI E2E column**: Replace `(planned: marketBuy.spec.ts)` etc. with the actual test path that landed (e.g. `tests/integration/ui/marketBuy.spec.ts:test('spend-anchored: 100 USDC → tNVDA fills + success toast + USDC debited')`). If the planned spec didn't land, mark the cell `—` and the row becomes a candidate must-fix.

    2. **Must-fix bar (D-13)**: A gap is must-fix iff:
       - (1) It corresponds to a TRADE-01..04 boundary regression class with no test in any column, OR
       - (2) It corresponds to a TEST-08 failure mode lacking E2E coverage AFTER 01-06 lands.

    3. **For each remaining must-fix item**: choose ONE of:
       - **Close it with a new test** — author the missing test file (e.g. if `tests/lib/utils/marketHours.test.ts` doesn't exist and TEST-08e unit coverage is must-fix, write a unit test there). Append `Closed by: <path>` to the audit entry.
       - **Document already-covered** — if the gap is implicitly covered by an existing 01-04..01-07 spec (e.g. TRADE-04 mode×side coverage by marketBuy + marketSell), append `Already covered by: <path>` with a one-line rationale.
       - **Document as nice-to-have downgrade** — only if the gap fails the D-13 must-fix bar on re-read; move to the Nice-to-Have section with rationale and a 999.x backlog ID.

    4. **Update Must-Fix Gap List section** to be either:
       - Empty: `**No must-fix gaps remain after 01-04..01-07 landed. All TRADE-01..04 + TEST-08 rows have at least one populated cell.**`
       - OR: enumerated list with every item annotated `Closed by: <path>` or `Already covered by: <path>`.

    5. **Update Audit Method Notes** to reflect the re-walk date.

    Most likely closing patterns:
    - **TEST-08e — market-hours gating** unit gap: if `tests/lib/utils/marketHours.test.ts` doesn't exist, author a minimal unit test (Saturday/weekday boundaries, ET vs UTC) — typical 30-50 LOC, 4-6 cases. Title: `marketHours - isOutsideMarketHours classifies weekend / weekday-night / market-open correctly`.
    - **Hydration failure UI surface** gap: if 01-06 didn't cover hydration failure (it's a `replay-hydration-failure.test.ts` service-level concern, not a TEST-08 mode), document `Already covered by: tests/integration/marketOrder/replay-hydration-failure.test.ts (service-level — not user-facing per CONTEXT)` and downgrade to nice-to-have.
    - **DCA-deploy** gap: per D-13 / CONTEXT Deferred — explicitly nice-to-have → 999.x backlog. Document the backlog ID.

    For any new test file authored to close a must-fix gap:
    - Lives under `tests/lib/**` for unit-class gaps OR `tests/integration/marketOrder/**` for service-integration-class gaps OR `tests/integration/ui/**` for UI E2E gaps.
    - Follows the existing-test conventions for that location (Vitest + jsdom for unit; Vitest + anvil for service-integration; Playwright for UI).
    - Includes the BASE_RPC_URL skip-gate if anvil-driven.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md && ! grep -E '\\(planned:' .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md && (grep -qE 'No must-fix gaps remain|^[0-9]+\\. .*(Closed by|Already covered by):' .planning/phases/01-ui-driven-e2e-order-test-coverage/01-AUDIT.md) && [ "$(grep -c 'failWith(' src/lib/services/marketOrderExecution.ts)" -ge 12 ]</automated>
  </verify>
  <acceptance_criteria>
    - `01-AUDIT.md` no longer contains any `(planned: ...)` placeholder cells (all replaced with real test paths or explicit `—`)
    - Must-Fix Gap List is either empty (with the exact "No must-fix gaps remain..." string) OR every remaining item has `Closed by: <path>` or `Already covered by: <path>` annotation
    - Any new test files created (e.g. `tests/lib/utils/marketHours.test.ts`) follow location conventions and pass: `npm test` green
    - Locked invariants intact (failWith ≥ 12; svelte-check baseline preserved)
  </acceptance_criteria>
  <done>TEST-11 closed: every must-fix gap has either a closing test or a justified downgrade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit re-classification ↔ ground truth | A gap mis-marked "Already covered by" when actually NOT covered leaves the regression class unprotected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-08-01 | Information Disclosure | False "Already covered by" claim | mitigate | Each `Already covered by: <path>` MUST cite a specific test name (not just a file). Reviewer can grep the file for the named test to verify. |
| T-1-08-02 | Tampering | Must-fix item silently downgraded to nice-to-have | mitigate | D-13 bar applied as a binary gate: TRADE-01..04 row with all columns `—` is automatically must-fix; TEST-08 row uncovered post-01-06 is automatically must-fix. Downgrade requires explicit rationale + a 999.x backlog ID. |
</threat_model>

<verification>
- 01-AUDIT.md updated; every cell is real path, `—`, or annotated reference
- All new tests authored to close gaps pass green
- Locked invariants intact
</verification>

<success_criteria>
- TEST-11 satisfied: must-fix list resolved (closed or justified)
- Audit reflects real coverage post-Phase-1
- Phase 1 has structurally closed every TRADE-01..04 + TEST-08 boundary regression class with at least one test in some column
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-driven-e2e-order-test-coverage/01-08-SUMMARY.md`.
</output>
