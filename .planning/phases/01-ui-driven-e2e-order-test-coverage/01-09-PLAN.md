---
phase: 01-ui-driven-e2e-order-test-coverage
plan: 09
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .github/workflows/test.yml
autonomous: true
requirements: [TEST-05]
must_haves:
  truths:
    - "GitHub Actions test-integration job no longer fails at `Install Foundry (anvil)` step (closes 999.8) — uses foundry-rs/foundry-toolchain@v1"
    - "test-e2e CI job exists, gated on BASE_RPC_URL secret, with Playwright browser cache, foundry-toolchain install, and a smoke pre-flight"
    - "First Playwright invocation runs ONLY smoke.spec.ts (smoke-fast-fail per D-14); a second invocation runs the full suite"
    - "BASE_RPC_URL is sourced from GitHub Actions secret, not echoed in logs"
    - "test:integration job has its first green archive-RPC run available (closes 999.11)"
  artifacts:
    - path: .github/workflows/test.yml
      provides: "Updated workflow: test-integration uses foundry-toolchain action; new test-e2e job; Playwright browser cache"
  key_links:
    - from: .github/workflows/test.yml test-e2e job
      to: secrets.BASE_RPC_URL
      via: "env: BASE_RPC_URL: ${{ secrets.BASE_RPC_URL }}"
      pattern: "secrets\\.BASE_RPC_URL"
    - from: test-e2e job
      to: smoke.spec.ts pre-flight
      via: "first run: playwright test smoke.spec.ts; second run: full suite"
      pattern: "smoke.spec.ts"
---

<objective>
Wire `test:e2e` and the upgraded `test:integration` into CI. Closes 999.8 (Foundry install drift) and 999.11 (no green archive-RPC run yet) per D-14.

Purpose: Without CI gating, the suite is local-only and the milestone "lock in trade-execution correctness" goal is unfulfilled. This plan ships green CI for both `test:integration` (using `foundry-rs/foundry-toolchain@v1`) and the new `test:e2e` (Playwright + vite preview + anvil + smoke pre-flight).
Output: Updated `.github/workflows/test.yml` with the foundry-toolchain swap on the existing `test-integration` job + a new `test-e2e` job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ui-driven-e2e-order-test-coverage/01-RESEARCH.md
@.planning/phases/01-ui-driven-e2e-order-test-coverage/01-PATTERNS.md
@.github/workflows/test.yml

<interfaces>
From .github/workflows/test.yml `test-integration` job (lines 29-74) — analog for the new test-e2e job:
- checkout@v2 with submodules:recursive + fetch-depth:0
- Install Nix (DeterminateSystems/nix-installer-action@main)
- DeterminateSystems/flakehub-cache-action@main
- (currently) custom curl + foundryup install — TO BE REPLACED with foundry-rs/foundry-toolchain@v1
- nix develop -c npm i
- nix develop -c npm run test:integration
- env: BASE_RPC_URL: ${{ secrets.BASE_RPC_URL }}

From package.json (after 01-01):
- "test:e2e": "playwright test"
- @playwright/test in devDependencies

Playwright browser cache pattern (from RESEARCH §"Don't Hand-Roll"):
- ~/.cache/ms-playwright
- key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap foundry install + add test-e2e job with smoke pre-flight</name>
  <files>
    .github/workflows/test.yml
  </files>
  <read_first>
    - .github/workflows/test.yml (full file — read both unit-test job and test-integration job; identify the foundry install block to replace; preserve everything else)
    - .planning/phases/01-ui-driven-e2e-order-test-coverage/01-PATTERNS.md §".github/workflows/test.yml" (D-14 + 999.8 deltas)
    - .planning/phases/01-ui-driven-e2e-order-test-coverage/01-RESEARCH.md §"Don't Hand-Roll" (foundry-toolchain action + Playwright cache pattern)
    - .planning/phases/01-ui-driven-e2e-order-test-coverage/01-RUNBOOK.md (FORK_BLOCK to wire as env var if pinned per-CI)
  </read_first>
  <behavior>
    - test-integration job uses foundry-rs/foundry-toolchain@v1 instead of the custom curl + foundryup script (closes 999.8).
    - test-e2e job clones the test-integration job structure with these deltas:
      - Install Playwright browsers with cache: `actions/cache@v4` keyed on `~/.cache/ms-playwright` + `hashFiles('package-lock.json')`.
      - `npx playwright install --with-deps chromium` after `npm i`.
      - Two-step run: first `npx playwright test smoke.spec.ts` (smoke pre-flight per D-14); only on success run `nix develop -c npm run test:e2e` (full suite). Smoke pre-flight surfaces misconfig in <2 min.
    - Both jobs source `BASE_RPC_URL` from `${{ secrets.BASE_RPC_URL }}`; never echo to logs.
  </behavior>
  <action>
    A. **Replace the foundry install block in the existing `test-integration` job.** Locate the cache + custom-install steps in `.github/workflows/test.yml` (per PATTERNS lines 43-62: `Cache Foundry installation` + `Install Foundry (anvil)` + `Verify anvil`). Replace ONLY those steps with:
    ```yaml
          - uses: foundry-rs/foundry-toolchain@v1
            with:
              version: stable
    ```
    Preserve everything else: checkout, Nix, flakehub-cache, npm i, the test:integration run command, and the `env:` block carrying `BASE_RPC_URL`.

    B. **Add a new `test-e2e` job** as a near-clone of `test-integration` with the deltas from PATTERNS §".github/workflows/test.yml" §"Apply (two changes — D-14 + 999.8)":
    ```yaml
      test-e2e:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v2
            with:
              submodules: recursive
              fetch-depth: 0
          - name: Install Nix
            uses: DeterminateSystems/nix-installer-action@main
            with:
              determinate: true
          - uses: DeterminateSystems/flakehub-cache-action@main
          - uses: foundry-rs/foundry-toolchain@v1
            with:
              version: stable
          - name: Verify anvil
            run: anvil --version
          - run: nix develop -c npm i
          - name: Cache Playwright browsers
            uses: actions/cache@v4
            with:
              path: ~/.cache/ms-playwright
              key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
              restore-keys: |
                playwright-${{ runner.os }}-
          - name: Install Playwright browsers
            run: nix develop -c npx playwright install --with-deps chromium
          - name: E2E smoke pre-flight (fast-fail per D-14)
            run: nix develop -c npx playwright test smoke.spec.ts
            env:
              BASE_RPC_URL: ${{ secrets.BASE_RPC_URL }}
          - name: E2E full suite
            run: nix develop -c npm run test:e2e
            env:
              BASE_RPC_URL: ${{ secrets.BASE_RPC_URL }}
    ```
    Add this block after the existing `test-integration` job in the same file. Do NOT modify the unit-test job.

    C. **Document in 01-RUNBOOK.md** the CI shape: which secrets are required (`BASE_RPC_URL`), where Playwright browser cache lives, and the smoke-fast-fail expectation. Append a `## CI workflow` section if not already present.

    D. **Validate locally** by linting the YAML (`yamllint .github/workflows/test.yml` if available) and inspecting the diff. The actual green run requires the BASE_RPC_URL CI secret — closing 999.11 happens when the workflow runs against `main`.

    E. **Verify locked invariants are still green** at plan close (no CI plumbing should touch source code; this is a sanity check):
    ```bash
    grep -c 'failWith(' src/lib/services/marketOrderExecution.ts   # ≥ 12
    grep -RE 'EMERGENCY_RATIO_MULTIPLIER' src/                      # 0 hits
    grep -E "from ['\"]\\\$lib/stores/transaction['\"]" src/lib/services/marketOrderExecution.ts  # 0 hits
    ```
  </action>
  <verify>
    <automated>grep -q "foundry-rs/foundry-toolchain@v1" .github/workflows/test.yml && grep -q "test-e2e:" .github/workflows/test.yml && grep -q "actions/cache@v4" .github/workflows/test.yml && grep -q "playwright-\\${{ runner.os }}-\\${{ hashFiles('package-lock.json') }}" .github/workflows/test.yml && grep -q "playwright test smoke.spec.ts" .github/workflows/test.yml && grep -q "secrets.BASE_RPC_URL" .github/workflows/test.yml && ! grep -q "curl -L https://foundry.paradigm.xyz" .github/workflows/test.yml && [ "$(grep -c 'failWith(' src/lib/services/marketOrderExecution.ts)" -ge 12 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/test.yml` references `foundry-rs/foundry-toolchain@v1` (closes 999.8)
    - `test-integration` job NO LONGER contains `curl -L https://foundry.paradigm.xyz` or the custom install steps
    - New `test-e2e:` job exists with: foundry-toolchain action + Playwright browser cache (`actions/cache@v4` keyed on package-lock hash) + smoke pre-flight running ONLY `smoke.spec.ts` + full-suite run + `BASE_RPC_URL` from secrets
    - Locked invariants intact:
      - `grep -c 'failWith(' src/lib/services/marketOrderExecution.ts` ≥ 12
      - No `EMERGENCY_RATIO_MULTIPLIER` in src/
      - No `marketOrderExecution.ts` import from `$lib/stores/transaction`
  </acceptance_criteria>
  <done>CI plumbing landed; once the workflow runs against `main` with BASE_RPC_URL secret, 999.8 + 999.11 close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BASE_RPC_URL secret ↔ CI logs | Must NEVER be echoed; only injected via `env:` from `${{ secrets.* }}`. |
| GitHub Actions runner ↔ public action versions | `foundry-rs/foundry-toolchain@v1` is a public action; tag-pin v1 protects against silent upgrades but not against compromised tags. Out of scope for this plan; track as backlog if escalated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-09-01 | Information Disclosure | BASE_RPC_URL leaking via job logs | mitigate | Sourced from `${{ secrets.BASE_RPC_URL }}`; never `echo $BASE_RPC_URL`. GitHub Actions auto-masks values from `secrets.` in logs. |
| T-1-09-02 | Tampering | Cache-key collision serves stale Playwright browsers across version bumps | mitigate | Cache key includes `hashFiles('package-lock.json')` — Playwright version bump invalidates the cache automatically (RESEARCH §"Open Questions" 6). |
| T-1-09-03 | DoS | Foundry-toolchain action upstream unavailability | accept | If `foundry-rs/foundry-toolchain@v1` is down, CI fails closed — no functional risk to production. Manual workaround documented in 01-RUNBOOK.md (revert to custom curl install). |
</threat_model>

<verification>
- Workflow YAML is well-formed (lint-able)
- foundry-toolchain action is the install path (no curl-bash anywhere in workflow)
- test-e2e job exists with all required steps in correct order: checkout → Nix → flakehub → foundry-toolchain → npm i → Playwright cache → Playwright install → smoke pre-flight → full suite
- Locked invariants from CONTEXT all hold green (failWith ≥ 12, EMERGENCY_RATIO_MULTIPLIER = 0, no transaction.ts ↔ marketOrderExecution.ts cycle)
</verification>

<success_criteria>
- D-14 satisfied: CI plumbing for both test:integration (foundry-toolchain) and test:e2e (full Playwright stack with smoke pre-flight)
- 999.8 closes once the workflow runs green
- 999.11 closes once the archive-RPC run completes green
- Smoke fast-fail surfaces misconfig in <2 minutes per D-14
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-driven-e2e-order-test-coverage/01-09-SUMMARY.md`.
</output>
