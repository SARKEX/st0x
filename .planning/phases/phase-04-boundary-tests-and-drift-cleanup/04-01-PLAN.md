---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - DRIFT-03
tags: [phase-4, drift, docs, claude-md]
must_haves:
  truths:
    - "CLAUDE.md no longer mentions Rhinestone, EIP-7702, account-abstraction, or multi-chain Arbitrum/Optimism/Ethereum"
    - "CLAUDE.md has a Ground Truth header at the top pointing at .planning/codebase/ as the authoritative source"
    - "Order Semantics — INPUT/OUTPUT Perspective (Critical) section is preserved verbatim (it is the prose statement of TRADE-01 and is accurate)"
    - "Rainlang section, Dev Commands, Project Overview, Tech Stack (minus Rhinestone), Project Structure (minus account-abstraction/) are preserved"
  artifacts:
    - path: CLAUDE.md
      provides: "Ground-truth-aligned project orientation pointing at .planning/codebase/"
      contains: "## Ground Truth"
  key_links:
    - from: CLAUDE.md
      to: .planning/codebase/CONCERNS.md
      via: "Ground Truth header text references the audit-of-record"
      pattern: ".planning/codebase"
  evidence:
    - "grep -E 'Rhinestone|EIP-7702|account-abstraction|Account Abstraction' CLAUDE.md returns 0 matches"
    - "grep -c 'Ground Truth' CLAUDE.md == 1"
    - "grep -c 'INPUT/OUTPUT Perspective' CLAUDE.md >= 1 (Order Semantics preserved)"
    - "grep -c 'Single chain' CLAUDE.md >= 1 (multi-chain table replaced with single-chain Base statement)"
---

<objective>
DRIFT-03 — Surgically edit CLAUDE.md to match the actual shipped code: single chain (Base 8453), two auth paths (wagmi + Dynamic embedded), no Rhinestone / EIP-7702 / `account-abstraction/` directory. Add a Ground Truth header at the top pointing at `.planning/codebase/` as the authoritative source.

Purpose: CLAUDE.md is the orientation document AI agents and new contributors land on first. Today it describes aspirational features (Rhinestone SDK, EIP-7702 smart EOAs, USDC gas sponsorship, multi-chain support across Arbitrum/Optimism/Ethereum) that do not exist in code. This drift causes downstream agents and humans to plan against a system that isn't there. After this plan, CLAUDE.md describes only what ships, and any disagreement defers to `.planning/codebase/`.

Output: A surgically-edited CLAUDE.md with 4 strikes (multi-chain table, Account Abstraction section, Project Structure `account-abstraction/` line, Tech Stack Rhinestone mention) and 1 add (Ground Truth header). No backfill of accurate content from `.planning/codebase/` per D-05a — the Ground Truth pointer makes redundancy unnecessary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-CONTEXT.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RESEARCH.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Surgical edit CLAUDE.md per D-05 — strike 4 false claims, add Ground Truth header</name>
  <read_first>
    - CLAUDE.md (full file — must understand current shape before editing)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-CONTEXT.md §"D-05 DRIFT-03 — CLAUDE.md Edit Scope" (verbatim Ground Truth header text + 4-strike list)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-PATTERNS.md §"CLAUDE.md (doc — DRIFT-03)" (preserve list, Pitfall 7)
  </read_first>
  <action>
Apply 5 surgical edits to CLAUDE.md. Use the Edit tool for each — never rewrite the whole file. Preserve all unrelated content verbatim including (CRITICAL): the entire `## Order Semantics — INPUT/OUTPUT Perspective (Critical)` section (TRADE-01 prose statement), the `## Rainlang` section, `## Dev Commands`, `## Project Overview`, and the `> **Planning context lives in `.planning/`**` blockquote at the top.

**Edit 1 — Add Ground Truth header.** Insert immediately after the existing top-level blockquote (the "Planning context" one, ending at the `**GSD workflow** is in use...` line) and BEFORE the `## Project Overview` heading. Insert this exact text (with the leading blank line for spacing):

```
## Ground Truth

This file is a high-level orientation. The authoritative codebase audit lives in
`.planning/codebase/` (ARCHITECTURE.md, STACK.md, CONVENTIONS.md, INTEGRATIONS.md,
STRUCTURE.md, TESTING.md) and the audit-of-record is `.planning/codebase/CONCERNS.md`.
When this file conflicts with `.planning/codebase/`, the latter is correct.

```

**Edit 2 — Tech Stack: remove Rhinestone.** In the `## Tech Stack` bulleted list, remove the line:
```
- **Web3:** wagmi + viem (core), Dynamic Labs (embedded wallets), Rhinestone SDK (account abstraction)
```
Replace it with:
```
- **Web3:** wagmi + viem (core), Dynamic Labs (embedded wallets)
```

**Edit 3 — Project Structure: remove account-abstraction/ entry.** In the `## Project Structure` ASCII tree, remove the line:
```
│   │   ├── aa/         # Account abstraction UI
```
AND remove the line:
```
│   │   ├── account-abstraction/  # Rhinestone SDK integration
```
(both refer to AA surfaces — `aa/` directory does not exist; `account-abstraction/` does not exist; verify with `ls src/lib/components/ src/lib/services/` before editing.)

**Edit 4 — Replace Multi-Chain Support section.** Locate the `## Multi-Chain Support` section with its 4-row chain table (Base/Arbitrum/Optimism/Ethereum). Replace the entire section (heading + intro paragraph + table + trailing networks paragraph) with:

```
## Single Chain

Single chain: Base (8453). Multi-chain expansion (Arbitrum / Optimism / Ethereum) is deferred to a future milestone — see `.planning/codebase/CONCERNS.md` (Documentation Drift) and the Out of Scope section in `.planning/REQUIREMENTS.md`. Network configuration lives in `src/lib/config/networks.ts`.
```

**Edit 5 — Replace Account Abstraction section.** Locate the `## Account Abstraction` section. Replace the entire section (heading + bullet list mentioning Rhinestone SDK / EIP-7702 / USDC gas sponsorship / cross-chain swap / `account-abstraction/`) with:

```
## Account Abstraction

No account abstraction. The `account-abstraction/` directory and Rhinestone SDK integration referenced in earlier drafts of this file do not exist in code. Account abstraction is deferred to a future milestone — see `.planning/REQUIREMENTS.md` Out of Scope and `.planning/codebase/CONCERNS.md`.
```

After all 5 edits, run a self-check via `git diff CLAUDE.md` — diff MUST show changes ONLY in the 5 regions above. If any other regions changed (especially the Order Semantics section), revert and redo. Per Pitfall 7: `grep -c "INPUT/OUTPUT Perspective" CLAUDE.md` must still return ≥ 1 after editing.

This implements D-05 (verbatim) and D-05a (no backfill). Single atomic commit.
  </action>
  <verify>
    <automated>bash -c "set -e; grep -E 'Rhinestone|EIP-7702|account-abstraction|Account Abstraction' CLAUDE.md | grep -v 'No account abstraction' | grep -v 'directory and Rhinestone SDK integration referenced in earlier drafts' && exit 1; echo OK; grep -c 'Ground Truth' CLAUDE.md | grep -qE '^[1-9]'; grep -c 'INPUT/OUTPUT Perspective' CLAUDE.md | grep -qE '^[1-9]'; grep -c 'Single chain' CLAUDE.md | grep -qE '^[1-9]'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'Rhinestone|EIP-7702|account-abstraction' CLAUDE.md` returns ONLY matches inside the new "Account Abstraction" disclaimer paragraph (which references the strings only to deny their existence). All other historical mentions removed.
    - `grep -c 'Ground Truth' CLAUDE.md` == 1
    - `grep -c 'INPUT/OUTPUT Perspective' CLAUDE.md` >= 1 (Order Semantics preserved per Pitfall 7)
    - `grep -c 'Rainlang' CLAUDE.md` >= 1 (Rainlang section preserved)
    - `grep -c 'Dev Commands' CLAUDE.md` >= 1 (Dev Commands preserved)
    - `grep -E 'Arbitrum|Optimism' CLAUDE.md | grep -v 'deferred'` returns 0 matches outside the Single Chain disclaimer text
    - `git diff --stat CLAUDE.md` shows a single file change with sensible insertions/deletions count (~30-50 lines net)
  </acceptance_criteria>
  <done>
CLAUDE.md describes the actual shipped system; aspirational AA + multi-chain content struck; Ground Truth header points at `.planning/codebase/`; Order Semantics + Rainlang preserved verbatim; phase-exit Wave 6 grep gate (`grep -E "Rhinestone|EIP-7702|account-abstraction" CLAUDE.md` post-strike) will return 0 matches against historical-claim text.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Doc-only edit; no runtime surface, no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Repudiation | CLAUDE.md edit | accept | Doc edit only; no new attack surface. Single atomic commit recorded in git history. |
| T-04-01-02 | Tampering | CLAUDE.md (orientation doc) | mitigate | Phase-exit Wave 6 grep gate (`grep -E "Rhinestone|EIP-7702|account-abstraction" CLAUDE.md` returns 0 outside the disclaimer) prevents silent re-introduction of false claims via future PRs. |
</threat_model>

<verification>
Single-task plan. Verification = the automated grep set above + manual diff review (Pitfall 7 — Order Semantics section MUST appear in post-edit file).
</verification>

<success_criteria>
- All 5 acceptance-criteria grep checks pass
- `npm run check` passes (svelte-check baseline = 3 unchanged — doc-only edit must not touch any .ts/.svelte file)
- `npm test -- --run` passes (no test impact)
- `git diff CLAUDE.md` review confirms changes confined to the 5 documented regions
</success_criteria>

<output>
After completion, create `.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-01-SUMMARY.md` documenting the 5 edits applied, the grep evidence, and confirmation that Order Semantics + Rainlang were preserved.
</output>
