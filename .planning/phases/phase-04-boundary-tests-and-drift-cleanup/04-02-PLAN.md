---
phase: 04
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/routes/admin/+page.svelte
  - src/routes/api/admin/nansen/+server.ts
autonomous: true
requirements:
  - DRIFT-02
tags: [phase-4, drift, payment-tokens, admin]
must_haves:
  truths:
    - "No hardcoded `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913` USDC string literal remains in src/routes/admin/ or src/routes/api/admin/"
    - "src/routes/admin/+page.svelte and src/routes/api/admin/nansen/+server.ts use isPaymentToken / getPaymentTokensForNetwork from existing helpers — no NEW helper authored"
    - "Behavior is preserved: every site that previously checked address-equality against USDC now resolves the same boolean via the canonical helpers"
  artifacts:
    - path: src/routes/admin/+page.svelte
      provides: "USDC hardcoding removed; canonical isPaymentToken/getPaymentTokensForNetwork imports added"
      contains: "getPaymentTokensForNetwork"
    - path: src/routes/api/admin/nansen/+server.ts
      provides: "USDC hardcoding removed; canonical helpers imported"
      contains: "getPaymentTokensForNetwork"
  key_links:
    - from: src/routes/admin/+page.svelte
      to: src/lib/config/tokens.ts (getPaymentTokensForNetwork at line 25)
      via: "import { getPaymentTokensForNetwork } from '$lib/config/tokens'"
      pattern: "from '\\$lib/config/tokens'"
    - from: src/routes/api/admin/nansen/+server.ts
      to: src/lib/utils/tokenMath.ts (isPaymentToken at line 213)
      via: "import { isPaymentToken } from '$lib/utils/tokenMath'"
      pattern: "from '\\$lib/utils/tokenMath'"
  evidence:
    - "grep -RE '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913' src/routes/admin/ src/routes/api/admin/ returns 0 matches"
    - "grep -c 'getPaymentTokensForNetwork' src/routes/admin/+page.svelte >= 1"
    - "grep -c 'isPaymentToken' src/routes/api/admin/nansen/+server.ts >= 1"
    - "npm run check exits 0 with svelte-check baseline = 3 unchanged"
    - "npm test -- --run passes (no behavioral regression)"
---

<objective>
DRIFT-02 — Replace hardcoded USDC address string `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913` in `src/routes/admin/+page.svelte` (8 references at lines 901, 1167-1168, 1263-1290, 1383) and `src/routes/api/admin/nansen/+server.ts` (5 references at lines 14, 141-142, 168, 172-174) with calls to the existing `isPaymentToken` (`src/lib/utils/tokenMath.ts:213`) and `getPaymentTokensForNetwork` (`src/lib/config/tokens.ts:25`) helpers.

Purpose: Hardcoded payment-token addresses scatter the "is this USDC?" decision across the codebase. When USDC's address changes (Base USDbC migration precedent), every hardcoded site is a silent breakage. Canonicalizing the lookup through `isPaymentToken` + `getPaymentTokensForNetwork` makes the decision a single grep target and routes all payment-token logic through one tested code path.

CRITICAL — per RESEARCH §"DRIFT-02 Resolution" + Anti-Patterns: BOTH helpers ALREADY EXIST. **Do NOT author new helpers.** This is a consume-don't-author plan. Authoring duplicates would reintroduce the exact drift this plan removes.

Output: 2 modified files (admin/+page.svelte + admin/nansen/+server.ts); 0 new helper code; ~13 line changes total; single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-CONTEXT.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RESEARCH.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-PATTERNS.md
@src/lib/config/tokens.ts
@src/lib/utils/tokenMath.ts
@src/routes/admin/+page.svelte
@src/routes/api/admin/nansen/+server.ts

<interfaces>
<!-- Existing helpers to consume — do NOT re-author -->

From src/lib/config/tokens.ts:25:
```typescript
export function getPaymentTokensForNetwork(chainId: number): PythToken[]
```
Returns all payment-class tokens (USDC, USDT, WETH) for the given chain. For Base mainnet pass `8453`.

From src/lib/utils/tokenMath.ts:213:
```typescript
export function isPaymentToken(
  token: { address: string; symbol?: string },
  networkPaymentToken?: { address: string }
): boolean
```
Symbol-or-address signature. When called with `(token, paymentTokens[0])`, returns true iff `token.address` matches the canonical payment-token address (case-insensitive).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace USDC hardcoding in src/routes/api/admin/nansen/+server.ts (5 sites)</name>
  <read_first>
    - src/routes/api/admin/nansen/+server.ts (full file — must understand current shape; only ~200 lines)
    - src/lib/utils/tokenMath.ts:213 (isPaymentToken signature)
    - src/lib/config/tokens.ts:25 (getPaymentTokensForNetwork signature)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-PATTERNS.md §"src/routes/admin/+page.svelte + src/routes/api/admin/nansen/+server.ts (component + api — DRIFT-02)" — verbatim before/after pattern
  </read_first>
  <action>
1. Delete the line `const USDC_ADDRESS = '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913'.toLowerCase();` (around line 14 — verify exact location by grepping).

2. Add imports near the top of the file (group with existing `$lib/*` imports):
```ts
import { isPaymentToken } from '$lib/utils/tokenMath';
import { getPaymentTokensForNetwork } from '$lib/config/tokens';
```

3. At the entry of the handler (or first use of `USDC_ADDRESS` — typically around line 140), bind once:
```ts
const paymentTokens = getPaymentTokensForNetwork(8453);
const usdc = paymentTokens[0]; // canonical USDC entry; preserved by helper ordering
```

4. Replace each of the 5 `USDC_ADDRESS` references:
   - Comparisons of the form `inputAddr === USDC_ADDRESS` (lines 14, 141-142, 168, 172-174) become `isPaymentToken({ address: inputAddr }, usdc)`.
   - If a site previously did `outputAddr === USDC_ADDRESS`, mirror with `isPaymentToken({ address: outputAddr }, usdc)`.
   - If the site needs the address string itself (not just a boolean), use `usdc.address` instead of the literal.

5. Verify no `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913` literal remains in the file. Verify no `USDC_ADDRESS` identifier remains.

6. Behavior preservation: the comparisons before were case-folded via `.toLowerCase()`. `isPaymentToken` per `tokenMath.ts:213` already handles case-insensitive comparison internally — no need to lowercase at the call site. If you observe any site that compares already-lowercase strings on both sides AND the helper does NOT case-fold, fall back to passing the lowercased address.

Single atomic commit covers Tasks 1+2 (split the work cognitively, but commit once).
  </action>
  <verify>
    <automated>bash -c "set -e; ! grep -q '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913' src/routes/api/admin/nansen/+server.ts; ! grep -q 'USDC_ADDRESS' src/routes/api/admin/nansen/+server.ts; grep -q \"from '\\\$lib/utils/tokenMath'\" src/routes/api/admin/nansen/+server.ts; grep -q \"from '\\\$lib/config/tokens'\" src/routes/api/admin/nansen/+server.ts; npm run check 2>&1 | tail -5"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913' src/routes/api/admin/nansen/+server.ts` == 0
    - `grep -c 'USDC_ADDRESS' src/routes/api/admin/nansen/+server.ts` == 0
    - `grep -c 'getPaymentTokensForNetwork' src/routes/api/admin/nansen/+server.ts` >= 1
    - `grep -c 'isPaymentToken' src/routes/api/admin/nansen/+server.ts` >= 1
    - `npm run check` exits 0 with svelte-check at baseline 3 (unchanged)
    - `npm test -- --run` passes (no nansen-related test failures)
  </acceptance_criteria>
  <done>
nansen endpoint determines USDC-ness via canonical helpers; behavior preserved (same boolean outcomes for the same input addresses); no NEW helper code authored.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace USDC hardcoding in src/routes/admin/+page.svelte (8 sites)</name>
  <read_first>
    - src/routes/admin/+page.svelte (lines 880-920, 1160-1300, 1370-1400 — focus the read on the regions containing USDC_ADDRESS references; the file is 2400+ lines so do NOT read end-to-end)
    - src/lib/utils/tokenMath.ts:213 (isPaymentToken signature)
    - src/lib/config/tokens.ts:25 (getPaymentTokensForNetwork signature)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-PATTERNS.md §"src/routes/admin/+page.svelte + src/routes/api/admin/nansen/+server.ts" — verbatim before/after pattern
    - MEMORY.md note "Svelte 4 TypeScript: JSDoc type annotations don't work in <script lang=\"ts\"> blocks. Use typed constants or `as` casts instead."
  </read_first>
  <action>
1. Locate the 8 references to the literal address `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913` and the `USDC_ADDRESS` constant via:
```bash
grep -n '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913\|USDC_ADDRESS' src/routes/admin/+page.svelte
```
Expect: line 901 (const declaration), 1167-1168, 1263-1290 (multiple comparisons in a block), 1383.

2. In the `<script lang="ts">` block (top of file), add imports in the existing import group:
```ts
import { isPaymentToken } from '$lib/utils/tokenMath';
import { getPaymentTokensForNetwork } from '$lib/config/tokens';
```

3. Delete the `const USDC_ADDRESS = '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913'.toLowerCase();` declaration at line ~901.

4. At a top-of-script (or first-use) location, bind once:
```ts
const paymentTokens = getPaymentTokensForNetwork(8453);
const usdc = paymentTokens[0];
```
If the file already has a `chainId` or `networkChainId` reactive variable, pass that instead of the literal `8453`. Confirm by grepping for `chainId\|chain_id` near the top of the script block.

5. Replace each of the 8 references:
   - `addr === USDC_ADDRESS` → `isPaymentToken({ address: addr }, usdc)`
   - `addr.toLowerCase() === USDC_ADDRESS` → `isPaymentToken({ address: addr }, usdc)` (helper handles case)
   - String literal use (e.g., as a Map key or display) → `usdc.address`
   - If a site uses USDC inside a destructured loop where `addr` is already lowercased, the helper still works (case-insensitive).

6. Verify no `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913` remains. Verify no `USDC_ADDRESS` identifier remains. Verify the imports landed correctly (svelte-check will catch malformed import statements).

7. Confirm behavior preservation: the page renders with the same admin TVL / USDC bucket calculations as before. svelte-check baseline must remain at 3 — do not introduce new svelte-check errors.
  </action>
  <verify>
    <automated>bash -c "set -e; ! grep -q '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913' src/routes/admin/+page.svelte; ! grep -q 'USDC_ADDRESS' src/routes/admin/+page.svelte; grep -q \"from '\\\$lib/utils/tokenMath'\" src/routes/admin/+page.svelte; grep -q \"from '\\\$lib/config/tokens'\" src/routes/admin/+page.svelte; npm run check 2>&1 | tail -10"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913' src/routes/admin/+page.svelte` == 0
    - `grep -c 'USDC_ADDRESS' src/routes/admin/+page.svelte` == 0
    - `grep -c 'getPaymentTokensForNetwork' src/routes/admin/+page.svelte` >= 1
    - `grep -c 'isPaymentToken' src/routes/admin/+page.svelte` >= 1
    - `npm run check` exits 0 with svelte-check at baseline 3 (unchanged) — no new errors introduced
    - `npm test -- --run` passes
    - `npm run lint` passes (no ESLint regressions)
  </acceptance_criteria>
  <done>
Admin page determines USDC-ness via canonical helpers; behavior preserved; svelte-check baseline unchanged; no NEW helper code authored.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin Browser → admin/+page.svelte (DRIFT-02 site 1) | Admin user input + on-chain reads cross here. Existing `requireAdmin` gate already protects route. No NEW boundary added. |
| Admin Browser → /api/admin/nansen/+server.ts (DRIFT-02 site 2) | Same — admin route already protected by `requireAdmin`. Only payment-token-comparison logic changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | Payment-token classification | mitigate | Helper `isPaymentToken` (tokenMath.ts:213) is the canonical case-insensitive comparison. Codemod replaces 13 ad-hoc comparisons with one tested code path; no new attack surface. |
| T-04-02-02 | Information Disclosure | None — no new data exposed | accept | Behavior-preserving refactor. No new logs, no new responses, no new endpoints. |
</threat_model>

<verification>
- All 13 USDC literal references removed (grep returns 0 in both files)
- Both files import the canonical helpers from `$lib/utils/tokenMath` and `$lib/config/tokens`
- svelte-check baseline preserved at 3 errors
- Test suite green (no behavioral regression)
- Lint green
</verification>

<success_criteria>
- All Task 1 + Task 2 acceptance criteria pass
- Phase 4 phase-exit Wave 6 grep gate (`grep -RE '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913' src/routes/admin/ src/routes/api/admin/`) will return 0 matches
- No new helper authored — entire DRIFT-02 surface is consume-don't-author per RESEARCH critical finding
</success_criteria>

<output>
After completion, create `.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-02-SUMMARY.md` documenting: 13 sites migrated; helpers consumed (no new authored); svelte-check + tests + lint green; grep evidence.
</output>
