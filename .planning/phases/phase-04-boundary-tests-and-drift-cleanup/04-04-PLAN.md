---
phase: 04
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - tests/hooks/_helpers.ts
  - tests/hooks/cors.test.ts
  - tests/hooks/csp.test.ts
  - tests/hooks/public-paths.test.ts
  - tests/hooks/admin-gate.test.ts
  - tests/hooks/wallet-session.test.ts
  - tests/hooks/bot-rejection.test.ts
autonomous: true
requirements:
  - TEST-01
tags: [phase-4, test, hooks, integration, security-coverage]
must_haves:
  truths:
    - "tests/hooks/ contains 6 test files, each <300 lines, each pinning one named concern"
    - "tests/hooks/_helpers.ts exports createMockRequestEvent / createMockKv / createMockSession typed against SvelteKit RequestEvent (no `as any` per Pitfall 5)"
    - "wallet-session.test.ts asserts the post-Phase-3 SEC-03+04 atomic-flip invariant: legacy `wallet-address` cookie is NOT treated as authoritative; only the session-id cookie + KV record drives event.locals.walletAddress"
    - "csp.test.ts asserts CSP_DIRECTIVES contains the explicit Sentry hosts (https://*.ingest.sentry.io and https://*.ingest.us.sentry.io) and NO bare *.sentry.io wildcard (Phase 1 cross-cutting Pitfall 1) and NO frame-src Onramper entry (DEPR-03 carry-forward)"
    - "admin-gate.test.ts asserts requireAdmin enforcement on /api/admin/* routes"
    - "public-paths.test.ts asserts /api/public/* and /api/access/* are honored before auth checks"
    - "bot-rejection.test.ts asserts bot-detection ordering relative to OPTIONS / public path / auth checks"
    - "cors.test.ts asserts production-domain + Vercel preview origin classification, preflight handling, header allowlist"
  artifacts:
    - path: tests/hooks/_helpers.ts
      provides: "Shared scaffolding: createMockRequestEvent, createMockKv, createMockSession"
      contains: "createMockRequestEvent"
    - path: tests/hooks/cors.test.ts
      provides: "CORS classification + preflight + allowlist tests"
      contains: "describe"
    - path: tests/hooks/csp.test.ts
      provides: "CSP host pinning + Pitfall-1 wildcard guard tests"
      contains: "ingest.sentry.io"
    - path: tests/hooks/public-paths.test.ts
      provides: "Public-path classification tests"
      contains: "PUBLIC_PATHS"
    - path: tests/hooks/admin-gate.test.ts
      provides: "Admin-route requireAdmin enforcement tests"
      contains: "requireAdmin"
    - path: tests/hooks/wallet-session.test.ts
      provides: "SEC-03+04 session-cookie classification + D-04 atomic-flip invariant tests"
      contains: "wallet-session"
    - path: tests/hooks/bot-rejection.test.ts
      provides: "Bot-detection ordering tests"
      contains: "describe"
  key_links:
    - from: "tests/hooks/*.test.ts"
      to: src/hooks.server.ts
      via: "import { handle } from '../../src/hooks.server'"
      pattern: "from.*hooks.server"
    - from: tests/hooks/_helpers.ts
      to: "@sveltejs/kit (RequestEvent type)"
      via: "import type { RequestEvent } from '@sveltejs/kit'"
      pattern: "RequestEvent"
  evidence:
    - "ls tests/hooks/{_helpers.ts,cors.test.ts,csp.test.ts,public-paths.test.ts,admin-gate.test.ts,wallet-session.test.ts,bot-rejection.test.ts} all exist"
    - "npm test -- tests/hooks/ --run exits 0"
    - "grep -c 'describe(' tests/hooks/wallet-session.test.ts >= 1"
    - "grep -c 'ingest.sentry.io' tests/hooks/csp.test.ts >= 1"
    - "grep -c 'wallet-address' tests/hooks/wallet-session.test.ts >= 1 (legacy-cookie negative assertion present)"
---

<objective>
TEST-01 — Author 6 integration test files in `tests/hooks/` plus a shared `_helpers.ts`, pinning the post-Phase-3 `src/hooks.server.ts` surface (auth/CORS/CSP/wallet-session/bot-rejection layering). Each file covers one named concern (D-02 split-per-concern), keeps under ~300 lines, and points failure messages at the concern.

Purpose: Per ROADMAP success criteria #1, "a regression that silently exposes an admin endpoint or breaks login flow now fails CI rather than reaching production." `hooks.server.ts` (lines 152-469) is the trust-boundary layering: CORS classification, CSP directive emission, public-path bypass, admin gating, wallet-session classification, bot-detection ordering. Today none of these have integration tests; a future refactor (e.g., adding a new admin endpoint, changing the cookie shape, dropping a CSP host) can silently regress security with no CI signal. After this plan, every layer has explicit assertions tied to its CONCERNS.md / Pitfall-1 / SEC-03+04 / D-04 atomic-flip invariants.

Output: 7 new test files (`_helpers.ts` + 6 `*.test.ts`); ~30-50 `it` blocks total; all run under `npm test -- --run` (jsdom, fast); inherit the existing Vitest 1.6.0 + jsdom + setupFiles config from `vite.config.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-CONTEXT.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RESEARCH.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-PATTERNS.md
@.planning/phases/phase-01-shrink-the-surface-see-what-s-happening/01-CONTEXT.md
@.planning/phases/phase-03-production-grade-hardening/03-CONTEXT.md
@.planning/phases/phase-03-production-grade-hardening/03-08a-SUMMARY.md
@.planning/phases/phase-03-production-grade-hardening/03-08b-SUMMARY.md
@src/hooks.server.ts

<interfaces>
<!-- Patterns to mirror -->

From src/lib/server/walletSession.test.ts (closest analog — lines 1-60):
- vi.hoisted + vi.mock for KV layer
- per-test in-memory Map<string,string> as KV stub
- beforeEach reset (`vi.resetModules(); vi.clearAllMocks(); process.env.NODE_ENV = 'test';`)
- dynamic `await import('./walletSession')` after mocks established

From src/hooks.server.ts (SUT under test):
- `handle` export (the SvelteKit hooks entry)
- CSP_DIRECTIVES at lines 176-197 — assertions reference explicit Sentry hosts
- isPublicApiPath at 158-160; PUBLIC_PATHS at 217-218
- auth flow at 152-469 — wallet-session + admin-gate + bot-rejection ordering

Type to use for mocks (Pitfall 5):
```typescript
import type { RequestEvent } from '@sveltejs/kit';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author tests/hooks/_helpers.ts (shared scaffolding)</name>
  <read_first>
    - src/lib/server/walletSession.test.ts (entire file ~60 lines — closest precedent for vi.hoisted + KV mock pattern)
    - src/hooks.server.ts lines 1-50 (imports + types) and lines 152-220 (handle entry, classification helpers, PUBLIC_PATHS)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-PATTERNS.md §"tests/hooks/_helpers.ts" — verbatim helper signatures
    - tests/utils/mockStores.ts (existing scaffolding style reference)
  </read_first>
  <action>
Create `tests/hooks/_helpers.ts` (underscore prefix prevents Vitest auto-discovery as a test file). Export 3 factories typed against SvelteKit's `RequestEvent` — DO NOT use `as any` per Pitfall 5.

```ts
import type { RequestEvent, Cookies } from '@sveltejs/kit';
import { vi } from 'vitest';

export interface MockRequestEventOptions {
    method?: string;
    url?: string;
    pathname?: string;
    headers?: Record<string, string>;
    cookies?: Record<string, string>;
    body?: BodyInit | null;
}

export function createMockRequestEvent(opts: MockRequestEventOptions = {}): RequestEvent {
    const method = opts.method ?? 'GET';
    const fullUrl = opts.url ?? `http://localhost${opts.pathname ?? '/'}`;
    const url = new URL(fullUrl);
    const headers = new Headers(opts.headers ?? {});
    const cookieJar = new Map<string, string>(Object.entries(opts.cookies ?? {}));

    const cookies = {
        get: (name: string) => cookieJar.get(name) ?? undefined,
        getAll: () => Array.from(cookieJar.entries()).map(([name, value]) => ({ name, value })),
        set: (name: string, value: string) => { cookieJar.set(name, value); },
        delete: (name: string) => { cookieJar.delete(name); },
        serialize: vi.fn(() => '')
    } as unknown as Cookies;

    const request = new Request(fullUrl, {
        method,
        headers,
        body: method === 'GET' || method === 'HEAD' ? null : (opts.body ?? null)
    });

    return {
        cookies,
        fetch: globalThis.fetch,
        getClientAddress: () => '127.0.0.1',
        locals: {} as App.Locals,
        params: {},
        platform: undefined,
        request,
        route: { id: null },
        setHeaders: vi.fn(),
        url,
        isDataRequest: false,
        isSubRequest: false
    } as unknown as RequestEvent;
}

export function createMockKv() {
    const store = new Map<string, string>();
    return {
        get: vi.fn(async (key: string) => store.get(key) ?? null),
        set: vi.fn(async (key: string, value: string, _opts?: { PX?: number; EX?: number }) => {
            store.set(key, value);
        }),
        del: vi.fn(async (key: string) => { store.delete(key); }),
        _store: store
    };
}

export function createMockSession({ walletAddress }: { walletAddress: string }) {
    return {
        sessionId: 'test-session-id-' + walletAddress.slice(2, 10),
        walletAddress: walletAddress.toLowerCase(),
        createdAt: Date.now(),
        expiresAt: Date.now() + 30 * 24 * 60 * 60 * 1000
    };
}
```

The `as unknown as RequestEvent` is the SvelteKit-recommended escape (the `RequestEvent` shape is wider than what tests need). The shared `_helpers.ts` is the ONLY file allowed this escape; the test files MUST consume the typed return without further casts.

Place at `tests/hooks/_helpers.ts`. Verify svelte-check passes.
  </action>
  <verify>
    <automated>bash -c "set -e; test -f tests/hooks/_helpers.ts; grep -q 'createMockRequestEvent' tests/hooks/_helpers.ts; grep -q 'createMockKv' tests/hooks/_helpers.ts; grep -q 'createMockSession' tests/hooks/_helpers.ts; grep -q 'RequestEvent' tests/hooks/_helpers.ts; ! grep -q ' as any' tests/hooks/_helpers.ts || (echo 'FAIL: as any present (Pitfall 5)' && exit 1); npm run check 2>&1 | tail -3"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/hooks/_helpers.ts` exists
    - Exports `createMockRequestEvent`, `createMockKv`, `createMockSession`
    - Imports `RequestEvent` from `@sveltejs/kit`
    - No `as any` (Pitfall 5)
    - `npm run check` exits 0 with svelte-check baseline = 3
  </acceptance_criteria>
  <done>
Shared scaffolding in place; the 6 test files in Task 2 will import from this single source.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author the 6 tests/hooks/*.test.ts files (one per concern)</name>
  <read_first>
    - src/hooks.server.ts (full file — needed to write meaningful assertions; ~470 lines)
    - tests/hooks/_helpers.ts (just authored — the helper signatures)
    - src/lib/server/walletSession.test.ts (vi.hoisted + KV mock pattern to mirror)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-PATTERNS.md §"tests/hooks/{cors,...}.test.ts" — verbatim mock + assertion shape
    - .planning/phases/phase-03-production-grade-hardening/03-CONTEXT.md §"D-04 SEC-03+04 wallet-session shape" + "D-04a 30-day sliding session lifetime" + "D-04b per-request never re-signs"
    - .planning/phases/phase-01-shrink-the-surface-see-what-s-happening/01-CONTEXT.md §"D-15 OBS-03 transcript shape" + Pitfall 1 (CSP host pinning — no bare *.sentry.io wildcard)
  </read_first>
  <action>
Create 6 test files. Each follows the Pattern A scaffolding (vi.hoisted + vi.mock + beforeEach reset). Each imports the `handle` from `src/hooks.server.ts` and drives mock RequestEvents through it. Each mocks the KV layer via `createMockKv()` from `_helpers.ts` and stubs the relevant `$lib/server/*` modules at the same hoisted-mock layer used by `walletSession.test.ts`.

**File 1: `tests/hooks/cors.test.ts` — CORS classification + preflight + header allowlist**

`describe('hooks.server CORS', () => { ... })` with at least:
- `it('allows production domains in Access-Control-Allow-Origin')` — call `handle` with `Origin: https://st0x.io` (or whichever production domain `hooks.server.ts` recognises — verify via grep), assert response header `Access-Control-Allow-Origin` matches.
- `it('allows Vercel preview origins')` — `Origin: https://*-st-0x.vercel.app` pattern (verify pattern in source).
- `it('allows public endpoint origins from any source')` — assert `/api/public/*` paths allow any origin.
- `it('rejects unknown origins for protected paths')` — assert no `Access-Control-Allow-Origin` echoed for unknown origin on `/api/admin/*` path.
- `it('handles OPTIONS preflight with Access-Control-Allow-Methods + -Headers')` — send OPTIONS, assert correct headers.
- `it('honors header allowlist (Content-Type, Authorization, X-CSRF-Token)')` — assert only allowlisted headers are reflected.

**File 2: `tests/hooks/csp.test.ts` — CSP host pinning + Pitfall-1 wildcard guard + DEPR-03 carry-forward**

`describe('hooks.server CSP', () => { ... })`:
- `it('connect-src includes https://*.ingest.sentry.io')` — assert directive contains the explicit Sentry-ingest host.
- `it('connect-src includes https://*.ingest.us.sentry.io')` — assert second Sentry-region host.
- `it('connect-src does NOT contain bare *.sentry.io wildcard (Pitfall 1)')` — string-search the response header for `*.sentry.io ` (with trailing space) NOT preceded by `ingest.`. This is the Phase 1 cross-cutting Pitfall 1 invariant.
- `it('connect-src includes Speed Insights endpoint')` — assert `vitals.vercel-insights.com` (or whichever the actual Speed Insights host is — verify in src).
- `it('script-src does NOT contain unsafe-eval')` — assert `unsafe-eval` is absent from `script-src`.
- `it('frame-src does NOT contain Onramper hosts (DEPR-03 carry-forward)')` — assert no `*.onramper.com` etc.
- `it('default-src is self')` — assert `default-src 'self'`.

**File 3: `tests/hooks/public-paths.test.ts` — public-path classification before auth**

`describe('hooks.server public paths', () => { ... })`:
- `it('/api/public/* skips auth checks')` — invoke `handle` with `/api/public/some-route`, NO session cookie, assert `event.locals.walletAddress` is unset and request proceeds (no 401).
- `it('/api/access/* skips auth checks')` — same shape for access-code endpoints.
- `it('any /api/health route is public')` — verify-or-skip based on actual PUBLIC_PATHS contents at hooks.server.ts:217-218.
- `it('admin path is NOT public')` — invoke `/api/admin/codes`, assert auth path runs.
- `it('public path classification runs BEFORE auth check')` — assert no KV lookup attempted on public paths (mock KV `.get` and assert it's never called).

**File 4: `tests/hooks/admin-gate.test.ts` — requireAdmin enforcement**

`describe('hooks.server admin gate', () => { ... })`:
- `it('rejects /api/admin/* without session cookie with 401')` — assert response status 401 (or whatever the source returns).
- `it('rejects /api/admin/* with session cookie but non-admin wallet with 403')` — set up KV with a non-admin wallet session, assert 403.
- `it('allows /api/admin/* with session cookie + admin wallet')` — set up KV with admin wallet, assert request proceeds.
- `it('cron route /api/cron/* is exempt from admin gate, gated only by CRON_SECRET (SEC-06 carry-forward)')` — assert with valid CRON_SECRET header, request proceeds without admin session.

(Verify the cron/admin/non-admin shape against actual `requireAdmin` and `hooks.server.ts` admin classification — adjust assertions to match real behavior, not aspirational.)

**File 5: `tests/hooks/wallet-session.test.ts` — SEC-03+04 atomic-flip invariant** (the highest-value test)

`describe('hooks.server wallet session', () => { ... })`:
- `it('valid session-id cookie + matching KV record sets event.locals.walletAddress')` — set cookie `session-id=...`, mock KV to return the session, invoke `handle`, assert `event.locals.walletAddress` matches the session's wallet (after the request flows through).
- `it('missing session-id cookie leaves event.locals.walletAddress undefined')` — no cookie, no KV lookup, assert undefined.
- `it('expired session-id KV record (KV returns null) leaves event.locals.walletAddress undefined')` — cookie present but KV returns null, assert undefined and assert NO call to `verifyWalletSignature` (D-04b code-level guarantee).
- `it('legacy wallet-address cookie present WITHOUT session-id is NOT authoritative (D-04 atomic-flip)')` — set `wallet-address` cookie only, assert `event.locals.walletAddress` is undefined (not derived from the legacy cookie). This is the D-04 atomic-flip invariant.
- `it('per-request path NEVER calls verifyWalletSignature (D-04b)')` — mock `verifyWalletSignature` and assert call count = 0 across multiple per-request invocations.

Mock the wallet-session module via `vi.mock('$lib/server/walletSession', ...)` and provide stubs for `readSession`, `createSession`, etc. Use `createMockKv()` from `_helpers.ts`.

**File 6: `tests/hooks/bot-rejection.test.ts` — bot-detection ordering**

`describe('hooks.server bot rejection', () => { ... })`:
- `it('OPTIONS preflight bypasses bot detection')` — assert OPTIONS request with bot User-Agent still receives CORS preflight response, not 403.
- `it('public path /api/public/* bypasses bot detection')` — assert public path request with bot UA proceeds.
- `it('protected route with bot User-Agent is rejected before auth check')` — assert no KV lookup attempted, response is 403/429 (whatever source returns).
- `it('non-bot request to admin route proceeds to auth gate')` — assert auth-gate runs for non-bot UA.
- `it('bot detection runs AFTER public-path classification but BEFORE auth')` — assert ordering by mocking both layers and asserting call order.

(Verify the actual bot-rejection logic in `hooks.server.ts` — if it's heuristic-based, adjust UA strings to known bots vs known browsers.)

**Cross-cutting requirements for all 6 files:**
- Each file imports from `'./_helpers'` (relative).
- Each file uses `import { handle } from '../../src/hooks.server'` (or whichever path the project tsconfig prefers — verify by checking how `walletSession.test.ts` imports its SUT).
- Each file uses `vi.hoisted` for module mocks established before `import`.
- Each file uses `beforeEach(() => { vi.resetModules(); vi.clearAllMocks(); })`.
- Each file < 300 lines.
- Failure messages reference the concern (e.g., "CSP must include https://*.ingest.sentry.io (Pitfall 1)") so a CI failure points at the invariant, not just the line.

Run `npm test -- tests/hooks/ --run` and iterate until all 6 files green.
  </action>
  <verify>
    <automated>bash -c "set -e; for f in cors csp public-paths admin-gate wallet-session bot-rejection; do test -f tests/hooks/\${f}.test.ts; done; npm test -- tests/hooks/ --run 2>&1 | tail -20"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist: `cors.test.ts`, `csp.test.ts`, `public-paths.test.ts`, `admin-gate.test.ts`, `wallet-session.test.ts`, `bot-rejection.test.ts`
    - `npm test -- tests/hooks/ --run` exits 0 (all hooks tests green)
    - `grep -c 'describe(' tests/hooks/*.test.ts` >= 6 (one per file minimum)
    - `grep -c 'ingest.sentry.io' tests/hooks/csp.test.ts` >= 2 (both Sentry-ingest hosts asserted)
    - `grep -c 'wallet-address' tests/hooks/wallet-session.test.ts` >= 1 (D-04 atomic-flip negative assertion)
    - `grep -c 'verifyWalletSignature' tests/hooks/wallet-session.test.ts` >= 1 (D-04b assertion)
    - Each file < 300 lines (`wc -l tests/hooks/*.test.ts | grep -v total | awk '{ if ($1 >= 300) print "TOO LONG: " $2 }'` returns empty)
    - `npm run check` exits 0 with svelte-check baseline = 3
  </acceptance_criteria>
  <done>
6 hooks integration tests pin the post-Phase-3 hooks.server.ts surface; ROADMAP success criteria #1 met (regressions to admin/login/CSP/CORS now fail CI).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted Browser → SvelteKit hooks | Tests pin the trust-boundary classification for every request. No new attack surface; tests verify existing surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Authentication Bypass | wallet-session classification | mitigate | wallet-session.test.ts asserts D-04 atomic-flip invariant: legacy `wallet-address` cookie is NOT authoritative; only session-id cookie + KV record drives `event.locals.walletAddress`. Future regression to accept the legacy cookie fails this test. |
| T-04-04-02 | Tampering | CSP directives | mitigate | csp.test.ts asserts explicit Sentry hosts AND no bare `*.sentry.io` wildcard (Pitfall 1). Asserts no `unsafe-eval`. Asserts no Onramper frame-src (DEPR-03). |
| T-04-04-03 | Elevation of Privilege | admin gating | mitigate | admin-gate.test.ts asserts `/api/admin/*` requires admin session; non-admin/no-session paths return 401/403. |
| T-04-04-04 | Information Disclosure | CORS classification | mitigate | cors.test.ts asserts unknown origins on protected paths receive no Access-Control-Allow-Origin echo. |
| T-04-04-05 | Denial of Service | Bot detection | mitigate | bot-rejection.test.ts asserts bot UAs are rejected on protected routes; OPTIONS + public paths bypass to avoid breaking legitimate clients. |
</threat_model>

<verification>
- All 6 test files exist and are <300 lines
- `npm test -- tests/hooks/ --run` passes
- All STRIDE assertions are pinned by named `it` blocks
- svelte-check baseline preserved
</verification>

<success_criteria>
- All Task 1 + Task 2 acceptance criteria pass
- Phase 4 phase-exit Wave 6 grep gate (`ls tests/hooks/{cors,csp,public-paths,admin-gate,wallet-session,bot-rejection}.test.ts` succeeds) green
- All Phase 1-3 cross-cutting gates remain green: TRADE-01 lockdown, TRADE-02 cycle severance, failWith ≥ 12, EMERGENCY_RATIO_MULTIPLIER = 0, staleTime: Infinity, no Alchemy hardcoding
</success_criteria>

<output>
After completion, create `.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-04-SUMMARY.md` documenting: 7 files created (1 helper + 6 tests); ~30-50 it blocks; STRIDE coverage table; CSP Pitfall-1 + D-04 atomic-flip + D-04b D-04b assertions verified active.
</output>
