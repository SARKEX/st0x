---
phase: 02-observability-for-transacting-users
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - .planning/phases/02-observability-for-transacting-users/02-RUNBOOK.md
  - .planning/phases/02-observability-for-transacting-users/02-PRIVACY-REVIEW.md
  - .planning/phases/02-observability-for-transacting-users/artifacts/funnel-market.json
  - .planning/phases/02-observability-for-transacting-users/artifacts/funnel-limit.json
autonomous: false
requirements: [OBS-08, OBS-10, OBS-11]
tags: [observability, posthog, funnel, privacy-review, runbook, operator]

must_haves:
  truths:
    - "A single PostHog dashboard renders trade-page → quote → submit → signed → confirmed funnels with named drop-off steps and counts, broken out by order type (market, limit) — D-claim OBS-08."
    - "PostHog session-recording sample rate is set in the PostHog dashboard ingestion settings page to 0.05–0.10 per D-04 (Pitfall 1 — not via posthog-js init)."
    - "Sentry project Replay is enabled in the Sentry dashboard."
    - "A real production trade roundtrip is captured end-to-end across Sentry replay, PostHog events, and pino server logs (OBS-10) with screenshots checked into the RUNBOOK."
    - "A privacy review checklist exists with operator sign-off, mapping each masking decision (D-03 Sentry, D-04 PostHog) and each event property to PII classification, cross-referenced with `.planning/codebase/CONCERNS.md` and `src/lib/observability/scrub.ts` (OBS-11)."
  artifacts:
    - path: ".planning/phases/02-observability-for-transacting-users/02-RUNBOOK.md"
      provides: "Operator runbook — PostHog dashboard config recipe, Sentry Replay enable steps, OBS-10 verification screenshot bundle"
      contains: "OBS-10"
    - path: ".planning/phases/02-observability-for-transacting-users/02-PRIVACY-REVIEW.md"
      provides: "OBS-11 privacy review checklist with operator sign-off"
      contains: "Sign-off"
    - path: ".planning/phases/02-observability-for-transacting-users/artifacts/funnel-market.json"
      provides: "Exported PostHog funnel definition (market order)"
    - path: ".planning/phases/02-observability-for-transacting-users/artifacts/funnel-limit.json"
      provides: "Exported PostHog funnel definition (limit order)"
  key_links:
    - from: "02-RUNBOOK.md OBS-10 section"
      to: "Sentry replay + PostHog events + Vercel pino logs (3 screenshot bundle)"
      via: "shared trade_id from a real production trade"
      pattern: "trade_id"
    - from: "02-PRIVACY-REVIEW.md"
      to: ".planning/codebase/CONCERNS.md PII guidance + src/lib/observability/scrub.ts"
      via: "checklist references"
      pattern: "CONCERNS.md|scrub.ts"
---

<objective>
Operator-side configuration (Sentry Replay enable, PostHog session-recording sample rate, OBS-08 funnel build) plus repo-side artifacts (RUNBOOK, PRIVACY-REVIEW, exported funnel JSON). Closes OBS-08, OBS-10, OBS-11.

This plan has TWO autonomous tasks (Claude can do) and TWO checkpoint tasks (operator must do in SaaS dashboards).

Purpose: Land the SaaS-side configuration that the code in Plans 01-03 depends on, capture the OBS-10 verification, and produce the OBS-11 sign-off artifact. After this plan, Phase 2 is shippable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONCERNS.md
@.planning/phases/02-observability-for-transacting-users/02-CONTEXT.md
@.planning/phases/02-observability-for-transacting-users/02-RESEARCH.md
@.planning/phases/02-observability-for-transacting-users/02-PATTERNS.md
@.planning/phases/02-observability-for-transacting-users/02-01-SUMMARY.md
@.planning/phases/02-observability-for-transacting-users/02-02-SUMMARY.md
@.planning/phases/02-observability-for-transacting-users/02-03-SUMMARY.md
@CLAUDE.md
@src/lib/observability/scrub.ts
@src/lib/services/analytics.ts
@src/lib/services/observability/tradeEvents.ts

<interfaces>
<!-- Event taxonomy from Plan 01 (TradeEventName + ErrorClass + TradeEventProps) and Plan 03 emission sites: -->

Funnel step events (in PostHog):
- `page_viewed` (with `page: 'trade'` filter) — intent step (route `/trade/[id]`)
- `trade_button_clicked` (with `order_type: 'market' | 'limit' | 'dca'`)
- `quote_received` (market only — limit/DCA do not have a pre-submit quote step)
- `trade_initiated` (market) / `limit_order_deployed` (limit + DCA)
- `sign_approval` (optional — depends on token allowance state)
- `sign_trade`
- `broadcast`
- `confirmed` / `trade_failed` (terminal)

Properties available for breakdown: `order_type`, `order_side`, `mode`, `error_class`, `trade_id`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 02-RUNBOOK.md with operator recipes for PostHog + Sentry + funnel build</name>
  <files>.planning/phases/02-observability-for-transacting-users/02-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/01-ui-driven-e2e-order-test-coverage/01-RUNBOOK.md (analog — RUNBOOK shape, section structure, screenshot embed convention)
    - .planning/milestones/v1.0-phases/phase-01-shrink-the-surface-see-what-s-happening/01-RUNBOOK.md (analog — operator step format, recipe granularity)
    - .planning/phases/02-observability-for-transacting-users/02-RESEARCH.md §"Runtime State Inventory" + §"Recommended Phase Sequencing" Wave 4 + §"How OBS-10's real production trade roundtrip is captured"
    - src/lib/services/analytics.ts (PostHog config baseline at lines 27-32 — D-04 keeps `maskAllInputs: true`)
    - src/hooks.client.ts (Sentry init from Plan 02)
  </read_first>
  <action>
    Create `.planning/phases/02-observability-for-transacting-users/02-RUNBOOK.md` with the following sections. Use the structure from `phase-01-shrink-the-surface-see-what-s-happening/01-RUNBOOK.md` as the format analog (numbered sections, recipe-style steps, screenshot-as-attachment placeholders).

    ## §1. Sentry Project Replay Enable (D-02)
    - Step 1: Operator logs into Sentry (EU region per CONTEXT canonical_refs) → Project Settings → Replay
    - Step 2: Toggle "Session Replay" ON for the project
    - Step 3: Verify Replay quota / billing tier supports `replaysOnErrorSampleRate: 1.0`
    - Step 4: After deploy, confirm via DevTools console on production trade page: no CSP `worker-src` violations
    - Screenshot placeholder: `[SCREENSHOT-1: Sentry project Replay settings showing toggle ON]`

    ## §2. PostHog Session-Recording Sample Rate (D-04, Pitfall 1)
    - Critical note: Per RESEARCH Pitfall 1, set sampling in the PostHog dashboard "Replay ingestion settings page" — NOT in `posthog-js` init code.
    - Step 1: Operator logs into PostHog (EU region) → Settings → Replay
    - Step 2: Set "Session sample rate" to `0.05` (5%) per D-04 (start low; increase to 0.10 if funnel-investigation usage demands more samples)
    - Step 3: Confirm `maskAllInputs: true` is still active (verified via `analytics.ts:27-32` in code; PostHog UI shows setting from SDK)
    - Step 4: Verify by clearing session, visiting `/trade/[id]` in incognito → check PostHog "Replays" tab shows recording attempted but rate-limited away (no recording stored unless 5% bucket)
    - Screenshot placeholder: `[SCREENSHOT-2: PostHog Replay ingestion settings page with 0.05]`

    ## §3. OBS-08 Funnel Dashboard Build (manual)
    Per RESEARCH Open Question 2 + §Wave 4c: build TWO funnels in ONE dashboard, broken out by `order_type`.

    Dashboard name: `Trade Funnels — Phase 2 Observability`

    Funnel A — Intent → Submit (broken out by `order_type`):
    - Step 1: `page_viewed` filter `page = 'trade'`
    - Step 2: `trade_button_clicked` (any `order_type`)
    - Step 3: `quote_received` (market only — limit/DCA skip this step; show as "n/a" in dashboard description)

    Funnel B — Submit → Confirmed (broken out by `order_type`):
    - Step 1: `trade_button_clicked`
    - Step 2: `sign_trade`
    - Step 3: `broadcast`
    - Step 4: `confirmed` OR `limit_order_deployed`

    Drop-off step naming convention (PostHog supports custom step labels — set):
    - Funnel A: "Page Visit", "Button Click", "Quote Shown"
    - Funnel B: "Submit Click", "User Signed", "Tx Broadcast", "Tx Confirmed"

    Breakdown: enable "Breakdown by event property → `order_type`" so each funnel renders three lines (`market`, `limit`, `dca`).

    Step 4: After funnel created in UI, export to JSON via PostHog's "Export funnel definition" feature → save to `.planning/phases/02-observability-for-transacting-users/artifacts/funnel-market.json` and `funnel-limit.json` (or one combined `funnel-trade.json` if PostHog exports the full dashboard). NOTE: the `artifacts/` directory must be created if it does not exist (`mkdir -p`).

    Step 5: Screenshot placeholder: `[SCREENSHOT-3: PostHog dashboard rendering Funnel A + Funnel B with order_type breakdown, populated with test events from a Plan 03 smoke trade]`

    ## §4. Cookie Consent Stance for Sentry Replay (D-02 + RESEARCH Open Question 3)
    Document the operator decision: Sentry Replay is treated as "essential" (not analytics) because it activates only on errors and is the primary triage tool. No consent gating in code. This stance is referenced in §02-PRIVACY-REVIEW.md.

    Operator/legal sign-off line (placeholder for human signature in markdown).

    ## §5. OBS-10 Production Smoke Verification
    Per RESEARCH §"How OBS-10's real production trade roundtrip is captured" — mirrors v1.0 PERF-01 HUMAN-UAT (post-deploy human verification, not synchronous CI gate).

    Recipe:
    1. Deploy Phase 2 changes to production
    2. Operator from a real wallet performs ONE Buy market trade (small size, e.g. $5 USDC of tNVDA) on production
    3. Operator from a real wallet performs ONE limit-order deploy + cancel (or wait for natural fill)
    4. (Optional, depending on real-trade tolerance) ONE DCA deploy
    5. (Optional) Intentionally trigger ONE failure case (e.g. submit market order outside NYSE hours) to verify Sentry Replay attaches
    6. For each trade, capture three screenshots into this RUNBOOK:
       - `[SCREENSHOT-4a: Sentry event detail page for the failure (or attached Replay for an intentional failure)]`
       - `[SCREENSHOT-4b: PostHog event timeline filtered by trade_id showing the events in order]`
       - `[SCREENSHOT-4c: Vercel Logs filter showing pino lines with the same trade_id field]`
    7. Verify all three screenshots share the same `trade_id` value (cross-tool correlation = PASS)

    Pitfall 6 verification: ensure at least ONE smoke trade uses a Dynamic embedded wallet (not just a wagmi-direct wallet) to verify the embedded-wallet signing flow does not lose `trade_id` synchronous context (RESEARCH Assumption A6).

    ## §6. Rollback Recipe
    If post-deploy issues:
    - Sentry Replay: disable in Sentry dashboard project settings (immediate, no deploy)
    - PostHog session recording: set sample rate to 0 in PostHog dashboard (immediate)
    - Code rollback: revert Plan 02 (`hooks.client.ts` Sentry init) + Plan 03 (component event sites). Plan 01 modules can stay (no consumer = no effect)

    ## §7. References
    - `.planning/REQUIREMENTS.md` — OBS-06..OBS-11
    - `.planning/phases/02-observability-for-transacting-users/02-CONTEXT.md` — D-01..D-04
    - `.planning/phases/02-observability-for-transacting-users/02-RESEARCH.md` — full technical context
    - Sentry SvelteKit Replay docs: docs.sentry.io/platforms/javascript/guides/sveltekit/session-replay/
    - PostHog session sampling docs: posthog.com/docs/session-replay/how-to-control-which-sessions-you-record

    Per CLAUDE.md "avoid over-engineering": this RUNBOOK is operator-facing prose, not code. No new abstractions; recipe-style.

    Create `.planning/phases/02-observability-for-transacting-users/artifacts/` directory (`mkdir -p`); leave empty placeholder `.gitkeep` until §3 funnel JSON is exported during operator step.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-observability-for-transacting-users/02-RUNBOOK.md && grep -c "OBS-08\|OBS-10\|D-02\|D-04\|Pitfall 1" .planning/phases/02-observability-for-transacting-users/02-RUNBOOK.md | awk '$1 >= 5 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    - File `02-RUNBOOK.md` exists with all 7 sections
    - `grep -c "SCREENSHOT" .planning/phases/02-observability-for-transacting-users/02-RUNBOOK.md` >= 4 (placeholders for operator-captured screenshots)
    - `grep -c "trade_id" .planning/phases/02-observability-for-transacting-users/02-RUNBOOK.md` >= 3
    - `artifacts/` subdir exists (with `.gitkeep` if empty)
    - References section links to REQUIREMENTS.md, CONTEXT, RESEARCH
  </done>
  <acceptance_criteria>
    - File length > 100 lines (substantive recipe content, not skeleton)
    - All 7 sections present (grep section headers)
    - PostHog Pitfall 1 explicitly called out in §2 (operator must NOT add `sampleRate` in code)
    - OBS-10 §5 includes Pitfall 6 Dynamic-wallet verification step
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author 02-PRIVACY-REVIEW.md checklist with PII classification + sign-off (OBS-11)</name>
  <files>.planning/phases/02-observability-for-transacting-users/02-PRIVACY-REVIEW.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (entire — focus on Security Considerations / PII guidance section)
    - src/lib/observability/scrub.ts (entire — Sentry boundary scrubber contract)
    - src/lib/services/analytics.ts (lines 27-32 PostHog session_recording config; lines 91-108 `track` enrichment with `wallet_address` lowercase normalization)
    - src/lib/services/observability/tradeEvents.ts (Plan 01 — `TradeEventProps` interface, `scrubProps` helper)
    - src/hooks.client.ts (Plan 02 — Sentry Replay config: `maskAllText`, `maskAllInputs`, `blockAllMedia`)
    - .planning/phases/02-observability-for-transacting-users/02-CONTEXT.md §decisions D-03, D-04
    - .planning/phases/02-observability-for-transacting-users/02-RESEARCH.md §"Privacy Review Acceptance Criteria (OBS-11)"
  </read_first>
  <action>
    Create `.planning/phases/02-observability-for-transacting-users/02-PRIVACY-REVIEW.md` per RESEARCH §"Privacy Review Acceptance Criteria (OBS-11)" — 5 sections each with operator sign-off line.

    ## §1. Replay Masking (Sentry + PostHog delta)
    Table:
    | Surface | Mask Config | Source | Rationale |
    | Sentry Replay | `maskAllText: true`, `maskAllInputs: true`, `blockAllMedia: true` | `src/hooks.client.ts` (Plan 02) | D-03 maximum privacy — text and inputs rendered as redacted blocks |
    | PostHog session recording | `maskAllInputs: true` (no `maskAllText`) | `src/lib/services/analytics.ts:27-32` | D-04 — input masking only; text visible (lower-traffic, low-rate sample) |

    Delta: Sentry is stricter than PostHog (text masking). PostHog plays the funnel-investigation role per D-01; less masking is acceptable because (a) lower sample rate, (b) admin-only-readable in PostHog UI, (c) the scope of "what's visible" is bounded by the trade-page surface where addresses/balances/error-text are present but already classified non-PII per CONCERNS.md (wallet addresses are public on-chain identifiers, not personally identifying without external linkage).

    Operator sign-off: `[ ] Reviewed by ________________  Date: ________`

    ## §2. Event Property Contract Audit (OBS-07 / `TradeEventProps`)
    Table — every field of `TradeEventProps` from `src/lib/services/observability/tradeEvents.ts`:
    | Field | PII Classification | Notes |
    | `order_type` | NOT PII | Enum: `market`/`limit`/`dca` |
    | `order_side` | NOT PII | Enum: `buy`/`sell` |
    | `mode` | NOT PII | Enum: `spendUpTo`/`buyUpTo` |
    | `asset_symbol` | NOT PII | Enum from project token list (`tNVDA`, etc.) |
    | `payment_symbol` | NOT PII | Enum: `USDC`/`USDT`/`WETH` |
    | `amount` | NOT PII | Decimal string from `formatUnits` |
    | `slippage_bps` | NOT PII | Integer |
    | `error_class` | NOT PII | Enum from `ErrorClass` union |
    | `error_message` | POTENTIAL PII | Free text. Plan 01 `scrubProps` redacts `0x[40]` and `0x[130]` patterns defensively. Caller responsibility: do not pass `error.cause.tx.from` raw. |
    | `trade_id` | NOT PII | UUIDv4, opaque, no derivation from PII |
    | `wallet_address` | PII (per project policy) | Auto-added by `track()` enrichment lowercase-normalized. PostHog policy: admin-only-readable. Pre-existing v1.0 OBS-01 stance — not regressed. |
    | `auth_method` | NOT PII | Enum: `wallet`/`dynamic`/`none` |
    | `network` / `chain_id` | NOT PII | Public chain metadata |

    Operator sign-off: `[ ] Reviewed by ________________  Date: ________`

    ## §3. Sentry Boundary Scrubber Coverage
    Reference: `src/lib/observability/scrub.ts` — patterns intact:
    - `ADDR_RE = /0x[a-fA-F0-9]{40}/g` → `[REDACTED_ADDR]`
    - `SIG_RE = /0x[a-fA-F0-9]{130}/g` → `[REDACTED_SIGNATURE]`
    - `SIG_QUERY_RE = /([?&])signature=[^&]*/g` → `signature=[REDACTED]`

    Plan 02 verifies via Sentry replay config test that `beforeSend` and `beforeBreadcrumb` are still wired through `scrubSentryEvent` (regression guard for OBS-01).

    Operator sign-off: `[ ] Reviewed by ________________  Date: ________`

    ## §4. Cookie Consent Stance for Sentry Replay
    Per RESEARCH Open Question 3:
    - PostHog: gated behind cookie consent via `initAnalytics()` callback (existing v1.0 wiring).
    - Sentry: runs from module load WITHOUT consent. Sentry Replay is `replaysOnErrorSampleRate: 1.0` (active only when an error fires).
    - Stance: Sentry Replay is "essential" (error-triage tool, no proactive recording per D-02). Document this explicitly here so a future operator/legal review has the rationale captured.
    - Action item if legal review reverses: gate `Sentry.replayIntegration` behind a consent callback similarly to `initAnalytics`. NOT required for this phase per RESEARCH recommendation.

    Operator sign-off (legal/operator decision): `[ ] Sentry Replay essential-tool stance approved by ________________  Date: ________`

    ## §5. CONCERNS.md Cross-Reference Audit
    Walk every PII-adjacent recommendation in `.planning/codebase/CONCERNS.md` (the audit-of-record). For each, verify Phase 2 introduces no regression:
    - [ ] Wallet-address handling — `track()` enrichment lowercase-normalizes (existing); no new code adds raw mixed-case addresses to events
    - [ ] Signature handling — `scrub.ts` `SIG_RE` covers; no new code logs signatures (verified by grep across Plan 01 + 02 + 03 modules)
    - [ ] Error-message contents — Plan 01 `scrubProps` redacts; component error classifiers use `error.message` (string), not raw error objects
    - [ ] Replay capture — D-03 maximum masking on Sentry side; D-04 input masking on PostHog side

    Add specific line-references to CONCERNS.md sections during operator review.

    Operator sign-off: `[ ] CONCERNS.md cross-reference complete  ________________  Date: ________`

    ## §6. Acceptance Summary
    All five sections signed off → OBS-11 complete. Reviewer countersignature: `[ ] OBS-11 closed by ________________  Date: ________`.

    Per CLAUDE.md "avoid over-engineering": this is a checklist file, not a policy framework. No new abstractions.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-observability-for-transacting-users/02-PRIVACY-REVIEW.md && grep -c "Sign-off\|Reviewed by\|Sign-off" .planning/phases/02-observability-for-transacting-users/02-PRIVACY-REVIEW.md | awk '$1 >= 5 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    - File exists with all 6 sections
    - Each of §1-§5 has at least one sign-off line
    - `grep -c "TradeEventProps\|trade_id\|maskAllText\|maskAllInputs" .planning/phases/02-observability-for-transacting-users/02-PRIVACY-REVIEW.md` >= 4
    - `grep -c "scrub.ts\|CONCERNS.md" .planning/phases/02-observability-for-transacting-users/02-PRIVACY-REVIEW.md` >= 2
  </done>
  <acceptance_criteria>
    - All 6 sections present (grep section headers `^## §`)
    - Every `TradeEventProps` field from `src/lib/services/observability/tradeEvents.ts` enumerated in §2
    - §3 references the three regex constants from `scrub.ts` verbatim
    - §4 documents the cookie-consent stance with operator sign-off line
    - §5 has at least 4 audit checkboxes
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator runs SaaS-side configuration steps</name>
  <what-built>
    Plans 01-03 deployed code changes that depend on three SaaS-side configurations being in place. This checkpoint pauses for the operator to execute them. None of these can be automated (Sentry/PostHog APIs for these specific settings are not exposed for as-code per RESEARCH §State of the Art).
  </what-built>
  <how-to-verify>
    Follow `02-RUNBOOK.md` §1, §2, §3:

    1. **Sentry Replay enable** (RUNBOOK §1):
       - Visit https://sentry.io → project settings → Replay → toggle ON
       - Confirm via DevTools console on production trade page: no CSP violations on Sentry's worker URL

    2. **PostHog session-recording sample rate** (RUNBOOK §2, Pitfall 1):
       - Visit https://eu.posthog.com → Settings → Replay → Session sample rate → set to `0.05`
       - Verify `maskAllInputs` still active (read SDK config in same panel)

    3. **OBS-08 funnel build** (RUNBOOK §3):
       - In PostHog dashboard, create a new dashboard `Trade Funnels — Phase 2 Observability`
       - Build Funnel A (intent → submit) and Funnel B (submit → confirmed) per RUNBOOK §3 step definitions
       - Add breakdown by `order_type`
       - Export funnel JSON definitions to `.planning/phases/02-observability-for-transacting-users/artifacts/funnel-market.json` and `funnel-limit.json`
       - Screenshot the populated dashboard → embed into RUNBOOK §3 SCREENSHOT-3 placeholder

    4. **Cookie-consent stance** (RUNBOOK §4):
       - Operator/legal review: confirm or revise the "essential-tool" stance for Sentry Replay
       - Sign off in `02-PRIVACY-REVIEW.md` §4

    Smoke check that wiring works: in PostHog "Live events" view during a staging or production deploy, perform a test trade and confirm events with `trade_id` appear in real time.
  </how-to-verify>
  <resume-signal>Type "operator-config-complete" once all 4 substeps are done, screenshots captured, and funnel JSON exported. Then provide the funnel JSON file paths to confirm artifacts are checked in.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator runs OBS-10 production smoke + completes OBS-11 sign-off</name>
  <what-built>
    Plans 01-03 + Task 3 of this plan are deployed. Phase 2 is now end-to-end live in production. This checkpoint captures the OBS-10 verification artifact and finalizes the OBS-11 privacy review.
  </what-built>
  <how-to-verify>
    Follow `02-RUNBOOK.md` §5 OBS-10 recipe:

    1. From a real wallet on production, perform:
       - ONE Buy market trade (~$5 USDC of any tToken)
       - ONE limit-order deploy + cancel (or wait for natural fill)
       - (Optional) ONE DCA deploy
       - (Optional) ONE intentional failure (submit outside NYSE hours) to verify Sentry Replay attaches

    2. **Pitfall 6 verification:** at least ONE of the above trades MUST use a Dynamic embedded wallet (not just wagmi-direct) to verify embedded-wallet flow does not lose `trade_id` synchronous context (Assumption A6).

    3. For each trade, capture and embed into `02-RUNBOOK.md` §5:
       - SCREENSHOT-4a: Sentry event page (for the failure trade) showing attached Replay + `trade_id` tag visible
       - SCREENSHOT-4b: PostHog event timeline filtered by `trade_id` showing the OBS-07 step sequence in order
       - SCREENSHOT-4c: Vercel Logs filter showing pino lines with the matching `trade_id` field

    4. Verify cross-tool correlation: same `trade_id` visible across all three sinks. If yes → PASS.

    5. **OBS-11 sign-off:** complete each of the five operator sign-off lines in `02-PRIVACY-REVIEW.md`:
       - §1 Replay Masking
       - §2 Event Property Contract Audit
       - §3 Sentry Boundary Scrubber Coverage
       - §4 Cookie Consent Stance (operator/legal call)
       - §5 CONCERNS.md Cross-Reference Audit
       - §6 Acceptance Summary countersignature

    6. Verify `02-PRIVACY-REVIEW.md` has all sign-offs filled (no empty `[ ]` boxes left in operator lines).

    7. Verify `02-RUNBOOK.md` SCREENSHOT placeholders 1-4c are filled or the section notes "screenshot embedded" with file path.
  </how-to-verify>
  <resume-signal>Type "obs-10-complete" once OBS-10 screenshots are embedded in RUNBOOK and OBS-11 sign-off is filled. If a regression is found during smoke (e.g. `trade_id` missing from one sink), describe the issue — Plan 03 may need revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Sentry SaaS dashboard | Project Replay enable; quota/billing |
| Operator → PostHog SaaS dashboard | Session-recording sample rate; funnel definition |
| Real production trade → all three sinks | OBS-10 verification — operator manually exercises the system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-K | Information disclosure | OBS-10 smoke trade screenshots embedded in RUNBOOK contain real wallet addresses / trade_ids | mitigate | RUNBOOK §5 instructs operator to redact wallet addresses in screenshots before commit (use a freshly generated test trade_id; wallet address can be the operator's own published-public address — same risk class as block explorer URLs operators routinely share). Trade_id is opaque UUID — non-PII. |
| T-2-L | Tampering | Funnel JSON in repo drifts from PostHog dashboard | accept | Funnel is owned operator-side. Repo JSON is documentation, not source of truth. RUNBOOK §3 instructs re-export when funnel is materially edited. |
| T-2-D | Information disclosure | Cookie-consent bypass for Sentry Replay (carries forward from Plan 02) | mitigate (documented) | PRIVACY-REVIEW §4 documents the "essential-tool" stance with operator/legal sign-off. If sign-off is rejected, Plan 02 hooks.client.ts must be revised to gate `replayIntegration` behind `initAnalytics`-style consent — out of current plan scope (revision-mode rerun). |
| T-2-M | Information disclosure | OBS-11 review missed a PII-bearing event property | mitigate | PRIVACY-REVIEW §2 enumerates EVERY field of `TradeEventProps`; future additions to the type require revisiting this checklist (acceptance: §6 sign-off line is the gate). |

</threat_model>

<verification>
- `02-RUNBOOK.md` and `02-PRIVACY-REVIEW.md` both exist and pass the grep `<done>` checks
- After Task 3: `artifacts/funnel-market.json` (or `funnel-trade.json`) exists and parses as valid JSON (`jq . artifacts/funnel-trade.json`)
- After Task 4: All operator sign-off lines in PRIVACY-REVIEW have a name + date filled (no empty `[ ]`)
- After Task 4: `02-RUNBOOK.md` §5 has all 3 screenshot bundles embedded for at least one smoke trade
- Cross-tool correlation verified by operator: same `trade_id` visible in Sentry, PostHog, Vercel Logs (acceptance: Task 4 §4)
</verification>

<success_criteria>
- OBS-08 funnel dashboard exists in PostHog with two funnels broken out by `order_type`
- OBS-10 verified end-to-end on production with screenshot evidence
- OBS-11 privacy review signed off by operator across all 5 sections
- Phase 2 deliverables complete: 6/6 REQ-IDs satisfied (OBS-06, OBS-07, OBS-08, OBS-09, OBS-10, OBS-11)
- All four locked decisions D-01..D-04 implemented:
  - D-01 (dual-stack): Sentry Replay (Plan 02) + PostHog session_recording (Plan 04 Task 3) coexist
  - D-02 (Sentry sampling): Plan 02 Task 1 verbatim values
  - D-03 (Sentry masking defaults): Plan 02 Task 1 verbatim values
  - D-04 (PostHog low session sample rate): Plan 04 Task 3 operator config
</success_criteria>

<output>
After completion, create `.planning/phases/02-observability-for-transacting-users/02-04-SUMMARY.md` documenting:
- RUNBOOK + PRIVACY-REVIEW sections committed
- Funnel JSON artifacts checked in
- OBS-10 smoke results (timestamp + screenshots committed)
- OBS-11 sign-off table (who signed each section, date)
- Decisions implemented in this plan: D-01 (PostHog half via session_recording sample rate), D-04 (verbatim)
- Phase 2 verification: all 6 REQ-IDs covered; ready for `/gsd-verify-work`
</output>
