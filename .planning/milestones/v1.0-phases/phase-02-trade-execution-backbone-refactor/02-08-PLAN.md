---
phase: 02
plan: 08
type: execute
wave: 8
depends_on: ["02-07"]
files_modified:
  - vite.config.ts
  - vite.config.js
  - package.json
  - package-lock.json
  - .gitignore
  - src/routes/(main)/trade/[id]/+page.svelte
  - .planning/phases/phase-02-trade-execution-backbone-refactor/02-RUNBOOK.md
autonomous: false
requirements:
  - PERF-01
must_haves:
  truths:
    - "Trade-page p75 LCP is captured pre-deploy and post-deploy from Vercel Speed Insights and recorded in 02-RUNBOOK.md"
    - "rollup-plugin-visualizer is installed as devDependency and registered in vite.config behind ANALYZE=1 env var"
    - "stats.html is in .gitignore — bundle visualization output is never committed"
    - "LimitOrder.svelte and DcaOrder.svelte load via dynamic import only when their tab is opened"
    - "Lazy-load tabs use a fixed-height skeleton placeholder to prevent CLS regression on tab switch (CLS in Speed Insights stays < 0.1)"
    - "jspdf and jspdf-autotable are removed from package.json (verified 0 imports in src/) — quick bundle win"
    - "Phase 2 success criterion 5 (p75 LCP < 2.5s) is met OR documented as already-met if pre-baseline was already < 2.5s"
  artifacts:
    - path: "vite.config.ts"
      provides: "rollup-plugin-visualizer registered behind ANALYZE=1"
      contains: "rollup-plugin-visualizer"
    - path: ".gitignore"
      provides: "stats.html ignored from commits"
      contains: "stats.html"
    - path: "src/routes/(main)/trade/[id]/+page.svelte"
      provides: "Lazy-loaded LimitOrder + DcaOrder tabs with skeleton placeholders"
      contains: "await import('$lib/components/orders/"
    - path: ".planning/phases/phase-02-trade-execution-backbone-refactor/02-RUNBOOK.md"
      provides: "Pre/post p75 LCP capture + bundle-size delta + CLS smoke-test outcome"
      contains: "p75 LCP"
  key_links:
    - from: "src/routes/(main)/trade/[id]/+page.svelte tab dispatcher"
      to: "$lib/components/orders/LimitOrder.svelte (lazy)"
      via: "{#await import('$lib/components/orders/LimitOrder.svelte')}"
      pattern: "await import\\('\\$lib/components/orders/(LimitOrder|DcaOrder)"
---

<objective>
PERF-01 — Bring trade-page p75 LCP under 2.5s on `/trade/[id]` (per D-07 Web Vitals "good" threshold) via the three-pronged client-side approach in D-08: (1) lazy-load `LimitOrder.svelte` / `DcaOrder.svelte` and chart libraries via Svelte 4 `{#await import()}`; (2) bundle-prune by removing unused deps (jspdf + jspdf-autotable confirmed 0 imports per RESEARCH §"Bundle prune temptation list"); (3) reduce TanStack Query waterfall by parallelizing where the dependency graph allows. Lands LAST in Phase 2 sequencing per D-08a recommendation — bundle shape stabilizes after TRADE-01..04 complete.

Purpose: PERF-01 closes the bounce-on-the-only-page-that-matters problem. The trade page is where users decide to click Buy/Sell on real money; a slow first paint costs trades. Validation against the OBS-05 Speed Insights dashboard from Phase 1.

Output:
- `rollup-plugin-visualizer` installed + registered in `vite.config` behind `ANALYZE=1`.
- `stats.html` added to `.gitignore`.
- `jspdf` + `jspdf-autotable` removed from `package.json` (after grep-confirming 0 src/ imports).
- `LimitOrder.svelte` + `DcaOrder.svelte` lazy-loaded via `{#await import()}` with `min-h-[Xpx]` skeleton placeholders sized to prevent CLS regression.
- Chart libraries (`TradingViewChart`, `lightweight-charts`, `TokenMarketCharts`) lazy-loaded similarly.
- Query-waterfall reorganized: speculatively kick off `createUserVaultsQuery` + `walletBalanceQuery` at mount instead of strictly after `currentToken` resolves (per RESEARCH §"Query-waterfall reorganization").
- New `02-RUNBOOK.md` records: pre-deploy p75 LCP baseline, the bundle-size delta from visualizer, the post-deploy p75 LCP measurement, and the CLS smoke-test outcome.

This plan is `autonomous: false` because two human-action checkpoints are required: pulling the LCP baseline from Vercel Speed Insights before code changes land (per RESEARCH §"BLOCKING ITEM" line 805), and the post-deploy LCP + CLS measurements after deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/phase-02-trade-execution-backbone-refactor/02-CONTEXT.md
@.planning/phases/phase-02-trade-execution-backbone-refactor/02-RESEARCH.md
@.planning/phases/phase-02-trade-execution-backbone-refactor/02-PATTERNS.md
@.planning/phases/phase-02-trade-execution-backbone-refactor/02-VALIDATION.md
@.planning/phases/phase-01-shrink-the-surface-see-what-s-happening/01-RUNBOOK.md
@.planning/phases/phase-01-shrink-the-surface-see-what-s-happening/deferred-items.md
@vite.config.ts

<interfaces>
<!-- The 'autonomous: false' flag is set because of the human checkpoints below; -->
<!-- the rest of the plan body is fully scriptable. -->

<!-- Existing eager imports in src/routes/(main)/trade/[id]/+page.svelte (lines 8, 27-28 per RESEARCH): -->
```svelte
import LimitOrder from '$lib/components/orders/LimitOrder.svelte';
import MarketOrder from '$lib/components/orders/MarketOrder.svelte';
import DcaOrder from '$lib/components/orders/DcaOrder.svelte';
```

<!-- Lazy-load transformation pattern (Svelte 4 canonical, from RESEARCH §"Lazy-load mechanism" lines 841-865): -->
```svelte
{#if activeOrderTab === 'limit'}
  {#await import('$lib/components/orders/LimitOrder.svelte')}
    <div class="min-h-[420px]"><LoadingSpinner /></div>
  {:then Mod}
    <svelte:component this={Mod.default} {assetToken} ... />
  {:catch err}
    <div class="text-red-500">Failed to load Limit order form. Reload the page.</div>
  {/await}
{:else if activeOrderTab === 'dca'}
  {#await import('$lib/components/orders/DcaOrder.svelte')}
    <div class="min-h-[420px]"><LoadingSpinner /></div>
  {:then Mod}
    <svelte:component this={Mod.default} {assetToken} ... />
  {:catch err}
    <div class="text-red-500">Failed to load DCA order form. Reload the page.</div>
  {/await}
{:else}
  <MarketOrder {assetToken} ... />  <!-- default tab; eagerly imported -->
{/if}
```

<!-- visualizer plugin pattern (from 02-PATTERNS.md §"vite.config.js"): -->
```javascript
import { visualizer } from 'rollup-plugin-visualizer';
// ...
plugins: [
  sentrySvelteKit({ /* ... */ }),
  sveltekit(),
  svelteTesting(),
  ...(process.env.ANALYZE === '1'
    ? [visualizer({
        emitFile: true,
        filename: 'stats.html',
        open: false,
        gzipSize: true,
        brotliSize: true,
        template: 'treemap'
      })]
    : [])
],
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0 (CHECKPOINT — pre-deploy baseline): Capture p75 LCP from Vercel Speed Insights</name>

  <what-built>
    The PERF-01 plan body needs a pre-deploy p75 LCP baseline before any code changes land. This baseline determines whether the work "preserves a sub-2.5s number while TRADE-* land" or "closes a gap to 2.5s" (per CONTEXT D-07 + RESEARCH §"BLOCKING ITEM"). The Vercel CLI does not expose Speed Insights data without Observability Plus subscription, so this is a human-action checkpoint.
  </what-built>

  <how-to-verify>
    1. Open https://vercel.com/st-0x/st0x/observability/speed-insights in a browser (authenticated as a project member).
    2. Set the time-range filter to "Last 7 days".
    3. Apply the route filter to `/trade/[id]` (the dynamic-route pattern as Vercel groups it).
    4. Record the following in `.planning/phases/phase-02-trade-execution-backbone-refactor/02-RUNBOOK.md` (create the file if it does not exist):
       - p75 LCP value (single number, ms).
       - Sample size (sessions / day or 7-day total — visible in dashboard top-right).
       - Per-device-class breakdown (mobile p75 vs desktop p75 — mobile typically the bottleneck).
       - p75 CLS, p75 INP, p75 TTFB for context.
       - Approximate cookie consent rate (per RESEARCH §"Pitfall 4" — biases the sample).

    5. Determine the work scope:
       - If pre-baseline p75 LCP < 2.5s on `/trade/[id]`: the rest of this plan is "preserve baseline while TRADE-* land + apply lazy-load + bundle prune wins; success = post-deploy p75 LCP also < 2.5s with no more than +50ms regression budget".
       - If pre-baseline p75 LCP ≥ 2.5s: the rest of this plan is "close the gap; success = post-deploy p75 LCP < 2.5s after lazy-load + jspdf prune + query-waterfall optimizations".
    6. Document the decision in 02-RUNBOOK.md.
  </how-to-verify>

  <resume-signal>
    Type "approved" with the recorded baseline values in your response, OR describe the specific dashboard issue (e.g. "low data warning", "consent rate < 30%", "/trade/[id] route not detected by Speed Insights"), in which case the plan body needs an alternate baseline strategy (e.g. local Lighthouse run or sampling against a known fixture URL).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install rollup-plugin-visualizer + register in vite.config + .gitignore stats.html + remove jspdf</name>
  <files>vite.config.ts, vite.config.js, package.json, package-lock.json, .gitignore</files>

  <read_first>
    - vite.config.ts (or vite.config.js — confirm which exists at execution time; project uses one file in CommonJS-style `defineConfig(({mode}) => ({...}))` form per RESEARCH "Variation flag" line 747). Read top-of-file imports + plugins block.
    - .gitignore (confirm whether `stats.html` already entered)
    - package.json (verify `jspdf` and `jspdf-autotable` are still in dependencies; verify `rollup-plugin-visualizer` is NOT yet there)
    - .planning/phases/phase-02-trade-execution-backbone-refactor/02-PATTERNS.md §`vite.config.js (MOD — register rollup-plugin-visualizer)` (full section + visualizer plugin pattern)
    - .planning/phases/phase-02-trade-execution-backbone-refactor/02-RESEARCH.md §"Bundle audit tool" lines 816-837 (gated on ANALYZE=1)
    - .planning/phases/phase-02-trade-execution-backbone-refactor/02-RESEARCH.md §"Bundle prune temptation list" lines 1136-1140 (jspdf 0 imports verified — safe to remove)
    - .planning/phases/phase-01-shrink-the-surface-see-what-s-happening/deferred-items.md (cache.ts:48-53 stale comment — opportunistic close ONLY if this plan touches cache.ts; PERF-01 does not, so leave alone)
  </read_first>

  <action>
    1. **Verify jspdf imports are 0 in src/** (defensive re-check before removal):
       ```
       grep -rE "from ['\"](jspdf|jspdf-autotable)" src/
       ```
       MUST return 0 lines. If non-zero, jspdf IS still used and removal is incorrect — abort jspdf removal and add a SUMMARY note flagging RESEARCH §"Bundle prune temptation list" as drifted.

    2. **Install rollup-plugin-visualizer:**
       ```
       npm install --save-dev rollup-plugin-visualizer@7.0.1
       ```

    3. **Register the plugin in vite.config.{ts,js}.** Find which file exists (`ls vite.config.*`).

       Add at the top of imports:
       ```javascript
       import { visualizer } from 'rollup-plugin-visualizer';
       ```

       Modify the plugins array (preserve existing CommonJS function-form `defineConfig(({mode}) => ({...}))` per RESEARCH "Variation flag"):
       ```javascript
       plugins: [
         sentrySvelteKit({ /* unchanged */ }),
         sveltekit(),
         svelteTesting(),
         ...(process.env.ANALYZE === '1'
           ? [visualizer({
               emitFile: true,
               filename: 'stats.html',
               open: false,
               gzipSize: true,
               brotliSize: true,
               template: 'treemap'
             })]
           : [])
       ],
       ```

    4. **Add `stats.html` to `.gitignore`:**
       Append to `.gitignore` if not already present:
       ```
       # rollup-plugin-visualizer build artifact (PERF-01)
       stats.html
       .svelte-kit/output/client/stats.html
       ```

    5. **Remove jspdf + jspdf-autotable from package.json:**
       Edit `package.json` to remove the two entries from `dependencies` (or wherever they live — check both `dependencies` and `devDependencies`).
       ```
       npm install
       ```
       to update `package-lock.json`.

       Verify removal:
       ```
       grep -cE "\"(jspdf|jspdf-autotable)\":" package.json
       ```
       MUST return 0.

    6. **Build smoke-test (no behavior change):**
       ```
       npm run build
       ```
       MUST exit 0 (no jspdf reference broken; build still succeeds).

    7. **Visualizer smoke-test (one-shot, optional but verifies plugin wiring):**
       ```
       ANALYZE=1 npm run build
       ls .svelte-kit/output/client/stats.html
       ```
       The file should exist after the analyze build. Open it locally to record the initial-chunk byte size in 02-RUNBOOK.md (alongside the pre-baseline LCP from Task 0).

    8. **Verification:**
       - `grep -c "rollup-plugin-visualizer\|visualizer" vite.config.*` returns ≥ 1.
       - `grep -c "ANALYZE" vite.config.*` returns ≥ 1.
       - `grep -c "stats\\.html" .gitignore` returns ≥ 1.
       - `grep -cE "\"(jspdf|jspdf-autotable)\":" package.json` returns 0.
       - `npm run build` exits 0.
       - `npm test -- --run` exits 0 (full suite).
       - `npm run check 2>&1 | grep -cE "^Error:"` ≤ 3.
  </action>

  <verify>
    <automated>grep -cE "rollup-plugin-visualizer|visualizer" vite.config.* && grep -c "stats\\.html" .gitignore && [ "$(grep -cE "\"(jspdf|jspdf-autotable)\":" package.json)" = "0" ] && npm run build && npm test -- --run</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE "rollup-plugin-visualizer|visualizer" vite.config.*` returns ≥ 1.
    - `grep -c "ANALYZE" vite.config.*` returns ≥ 1 (gated behind env var).
    - `grep -c "stats\\.html" .gitignore` returns ≥ 1.
    - `grep -cE "\"(jspdf|jspdf-autotable)\":" package.json` returns 0 (per 02-VALIDATION.md gate).
    - `grep -c "rollup-plugin-visualizer" package.json` returns ≥ 1 (devDependency present).
    - `npm run build` exits 0 (production build still succeeds).
    - `npm test -- --run` exits 0.
    - `npm run check 2>&1 | grep -cE "^Error:"` ≤ 3 (Plan 05 baseline preserved).
    - All cross-cutting gates from Plans 01-07 preserved.
  </acceptance_criteria>

  <done>
    rollup-plugin-visualizer installed + registered + gated. stats.html ignored. jspdf + jspdf-autotable removed (~250KB minified saved). Build still passes. All cross-cutting gates green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lazy-load LimitOrder + DcaOrder tabs + chart libs in /trade/[id]/+page.svelte with CLS-safe skeletons + reorganize TanStack Query waterfall</name>
  <files>src/routes/(main)/trade/[id]/+page.svelte</files>

  <read_first>
    - src/routes/(main)/trade/[id]/+page.svelte (full file — focus on import block at lines 1-50 + the tab-dispatch markup + the TanStack query factory call sites)
    - src/lib/components/orders/LimitOrder.svelte and DcaOrder.svelte (browse — measure rendered height for skeleton sizing)
    - src/lib/components/orders/MarketOrder.svelte (default tab — eagerly imported, NOT lazy-loaded since it's the first tab paint)
    - .planning/phases/phase-02-trade-execution-backbone-refactor/02-PATTERNS.md §`src/routes/(main)/trade/[id]/+page.svelte (MOD — PERF-01 lazy-load + waterfall reorganization)` (full section)
    - .planning/phases/phase-02-trade-execution-backbone-refactor/02-RESEARCH.md §"PERF-01 §Lazy-load mechanism" lines 841-865 (canonical Svelte 4 pattern)
    - .planning/phases/phase-02-trade-execution-backbone-refactor/02-RESEARCH.md §"Pitfall 5: Lazy-load CLS regression on tab switch" lines 437-448 (skeleton sizing)
    - .planning/phases/phase-02-trade-execution-backbone-refactor/02-RESEARCH.md §"PERF-01 §Query-waterfall reorganization" lines 869-901 (parallelization guidance + DO NOT change staleTime)
    - .planning/codebase/CONVENTIONS.md (Svelte 4 component conventions)
  </read_first>

  <behavior>
    - `LimitOrder.svelte` and `DcaOrder.svelte` are NOT eagerly imported at the top of `/trade/[id]/+page.svelte`. They load via `{#await import('$lib/components/orders/LimitOrder.svelte')}` only when their tab is active.
    - `MarketOrder.svelte` STAYS eagerly imported (it's the default tab and represents the first-paint LCP element on this page).
    - Each lazy block has a `min-h-[Xpx]` skeleton placeholder sized to match the lazy-loaded component's rendered height ±20px (per Pitfall 5). Determine X by inspecting the components — researcher recommends 420px as a safe default; verify in implementation by measuring the components in the dev browser.
    - Chart libraries (`TradingViewChart`, `TokenMarketCharts`, `lightweight-charts`) are also lazy-loaded via the same pattern (per CONTEXT D-08 + RESEARCH "Variation flag").
    - Query-waterfall reorganization: `createUserVaultsQuery` and the `walletBalanceQuery` are kicked off speculatively at mount instead of strictly after `currentToken` resolves. TanStack `enabled: false` semantics handle the unauthenticated case gracefully (no-op when not ready).
    - `staleTime: Infinity` is NOT changed — manual-invalidation pattern preserved per CLAUDE.md ground truth.
    - All 470+ tests continue to pass.
    - `npm run build` exits 0.
  </behavior>

  <action>
    1. **Identify the tab-dispatch shape.** Read `+page.svelte` and find the markup region where `<MarketOrder>`, `<LimitOrder>`, `<DcaOrder>` are conditionally rendered based on `activeOrderTab` (or whichever variable). Note the exact prop shape passed to each (e.g. `{assetToken}`, `{paymentToken}`, etc.).

    2. **Convert eager imports for LimitOrder + DcaOrder to lazy.**
       - DELETE the top-of-file `import LimitOrder from '$lib/components/orders/LimitOrder.svelte';` and `import DcaOrder from '$lib/components/orders/DcaOrder.svelte';` lines.
       - KEEP the `import MarketOrder from '$lib/components/orders/MarketOrder.svelte';` line (default tab — eagerly loaded).
       - Replace the conditional render block with the canonical lazy-load pattern:
         ```svelte
         {#if activeOrderTab === 'limit'}
           {#await import('$lib/components/orders/LimitOrder.svelte')}
             <div class="min-h-[420px]"><LoadingSpinner /></div>
           {:then Mod}
             <svelte:component this={Mod.default} {assetToken} {paymentToken} ... />
           {:catch err}
             <div class="text-red-500">Failed to load Limit order form. Reload the page.</div>
           {/await}
         {:else if activeOrderTab === 'dca'}
           {#await import('$lib/components/orders/DcaOrder.svelte')}
             <div class="min-h-[420px]"><LoadingSpinner /></div>
           {:then Mod}
             <svelte:component this={Mod.default} {assetToken} {paymentToken} ... />
           {:catch err}
             <div class="text-red-500">Failed to load DCA order form. Reload the page.</div>
           {/await}
         {:else}
           <MarketOrder {assetToken} {paymentToken} ... />
         {/if}
         ```

         Pass the SAME props that the eager render passed (read the existing markup to enumerate). The `<svelte:component this={Mod.default}>` pattern dispatches the dynamically-imported module's default export.

         If the project does not currently have a `<LoadingSpinner />` component (read `src/lib/components/ui/` to confirm), substitute with a Tailwind-styled placeholder div: `<div class="min-h-[420px] flex items-center justify-center"><div class="animate-pulse text-gray-500">Loading…</div></div>`.

    3. **Lazy-load chart libraries** (`TradingViewChart`, `TokenMarketCharts`, `lightweight-charts`):
       - Read the existing chart imports (search `import.*Chart` in the file).
       - For each chart component, wrap in the same `{#await import()}` pattern. Charts likely take longer to render — use a slightly taller skeleton (e.g. `min-h-[300px]`).
       - If `lightweight-charts` is imported but the component using it is itself lazy-loaded, the lazy-loaded component will pull in `lightweight-charts` as part of its chunk — no separate lazy-load needed for the bare module.

    4. **Reorganize TanStack Query waterfall** (per RESEARCH §"PERF-01 §Query-waterfall reorganization"):
       - Identify the current Tier 3 queries: `createUserVaultsQuery($currentNetwork, $walletAddress)`, `walletBalanceQuery` (which uses `createQuery` with `readContract`).
       - These currently fire AFTER `currentToken.address` resolves (Tier 2). Change them to fire AT MOUNT — TanStack's `enabled: false` handles the unauthenticated case (no-op when not ready).
       - Concretely: if the existing code has `enabled: !!currentToken && !!$walletAddress`, change to `enabled: !!$walletAddress` for the user-vault and balance queries — they don't strictly need `currentToken` to start. The query's eventual `queryFn` may reference `currentToken.address`, so wrap that part in a defensive `currentToken?.address ?? null` check.
       - DO NOT change `staleTime: Infinity` anywhere. Per CLAUDE.md and RESEARCH §"Project Constraints", manual-invalidation is intentional.

    5. **Verification:**
       - `grep -cE "await import\\(['\"]\\\$lib/components/orders/(LimitOrder|DcaOrder)" "src/routes/(main)/trade/[id]/+page.svelte"` returns ≥ 2 (per 02-VALIDATION.md gate).
       - `grep -c "min-h-\\[" "src/routes/(main)/trade/[id]/+page.svelte"` returns ≥ 2 (skeleton placeholders).
       - `grep -c "import LimitOrder from" "src/routes/(main)/trade/[id]/+page.svelte"` returns 0 (eager import removed).
       - `grep -c "import DcaOrder from" "src/routes/(main)/trade/[id]/+page.svelte"` returns 0 (eager import removed).
       - `grep -c "import MarketOrder from" "src/routes/(main)/trade/[id]/+page.svelte"` returns 1 (default tab kept eager).
       - `grep -c "staleTime.*Infinity" src/lib/clients/queryClient.ts` returns ≥ 1 (CLAUDE.md ground truth not regressed; this file is the global default).
       - `npm run build` exits 0.
       - `npm test -- --run` exits 0.
       - `npm run check 2>&1 | grep -cE "^Error:"` ≤ 3.
       - TRADE-01 lockdown gate re-verified for this file (the file is on the Plan 01 codemod surface): `grep -nE "\\.(inputTokenAddress|outputTokenAddress|inputIOIndex|outputIOIndex)\\b" "src/routes/(main)/trade/[id]/+page.svelte" | grep -v '^[^:]*:[0-9]*: *//' | wc -l` returns 0.
  </action>

  <verify>
    <automated>[ "$(grep -cE "await import\\(['\"]\\\$lib/components/orders/(LimitOrder|DcaOrder)" "src/routes/(main)/trade/[id]/+page.svelte")" -ge "2" ] && grep -c "min-h-\\[" "src/routes/(main)/trade/[id]/+page.svelte" && [ "$(grep -c "import LimitOrder from" "src/routes/(main)/trade/[id]/+page.svelte")" = "0" ] && [ "$(grep -c "import MarketOrder from" "src/routes/(main)/trade/[id]/+page.svelte")" = "1" ] && npm run build && npm test -- --run</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE "await import\\(['\"]\\\$lib/components/orders/(LimitOrder|DcaOrder)" "src/routes/(main)/trade/[id]/+page.svelte"` returns ≥ 2 (per 02-VALIDATION.md PERF-01 gate).
    - `grep -c "min-h-\\[" "src/routes/(main)/trade/[id]/+page.svelte"` returns ≥ 2 (Pitfall 5 mitigation: skeleton placeholders sized to prevent CLS).
    - `grep -c "import LimitOrder from" "src/routes/(main)/trade/[id]/+page.svelte"` returns 0.
    - `grep -c "import DcaOrder from" "src/routes/(main)/trade/[id]/+page.svelte"` returns 0.
    - `grep -c "import MarketOrder from" "src/routes/(main)/trade/[id]/+page.svelte"` returns 1.
    - `grep -c "staleTime.*Infinity" src/lib/clients/queryClient.ts` returns ≥ 1 (CLAUDE.md ground truth preserved).
    - `npm run build` exits 0.
    - `npm test -- --run` exits 0 with all 470+ tests passing.
    - `npm run check 2>&1 | grep -cE "^Error:"` ≤ 3.
    - TRADE-01 lockdown re-verified for this file: 0 raw IO-perspective property reads outside allowlist + comments.
    - All cross-cutting gates from Plans 01-07 preserved.
  </acceptance_criteria>

  <done>
    LimitOrder + DcaOrder tabs lazy-loaded with skeleton placeholders. MarketOrder stays eager (default tab / first paint). Chart libs lazy-loaded. Query waterfall reorganized to parallelize Tier 3 user-vault and balance queries. `staleTime: Infinity` untouched. Build passes; all tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (CHECKPOINT — post-deploy verification): Capture p75 LCP + CLS post-deploy + record bundle delta</name>

  <what-built>
    After Tasks 1 + 2 land and a deploy is triggered, Vercel Speed Insights collects new LCP / CLS / INP / TTFB samples from real users. The PERF-01 success criterion (p75 LCP < 2.5s on `/trade/[id]`) is validated against these new measurements. Bundle-size delta is captured by re-running `ANALYZE=1 npm run build` and comparing the initial-chunk byte size to Task 1's pre-baseline.
  </what-built>

  <how-to-verify>
    1. **Wait for deploy + sample accumulation** (≥ 24h ideal; Speed Insights p75 stabilizes with ≥ 100 sessions).
    2. **Pull post-deploy p75 LCP from Speed Insights:**
       - Open https://vercel.com/st-0x/st0x/observability/speed-insights
       - Time range: "Last 24 hours" (or "Last 7 days" if traffic is low)
       - Route filter: `/trade/[id]`
       - Record: p75 LCP, sample size, mobile vs desktop split.
    3. **PASS criterion:** post-deploy p75 LCP < 2.5s on `/trade/[id]` (per CONTEXT D-07 + 02-VALIDATION.md).
    4. **CLS regression smoke test:**
       - Open the production trade page on a real device or browser.
       - Click `Limit` tab → `DCA` tab → `Market` tab → repeat.
       - Visual content shift must NOT exceed 20px between tabs (per Pitfall 5).
       - Speed Insights p75 CLS for the page must remain < 0.1 (Web Vitals "good" threshold).
    5. **Bundle delta:**
       - Run `ANALYZE=1 npm run build` locally.
       - Open `.svelte-kit/output/client/stats.html`.
       - Record the initial-chunk byte size (gzip + brotli).
       - Compare to the pre-baseline recorded in Task 1's smoke build (or in 02-RUNBOOK.md).
    6. **Record all measurements in `.planning/phases/phase-02-trade-execution-backbone-refactor/02-RUNBOOK.md`:**
       - Pre-deploy p75 LCP (from Task 0): _____ ms
       - Post-deploy p75 LCP: _____ ms
       - Delta: _____ ms (negative = improvement)
       - Pre-deploy initial-chunk size: _____ KB (gzip)
       - Post-deploy initial-chunk size: _____ KB (gzip)
       - Bundle delta: _____ KB
       - CLS smoke test: passed / failed (if failed, document the shift magnitude)
       - Sample size on post-deploy: _____ sessions
       - Cookie consent rate: _____ %
    7. If post-deploy p75 LCP ≥ 2.5s:
       - Document which work item under-delivered (lazy-load? bundle-prune? waterfall?).
       - Determine whether the gap is small enough to close with a follow-up plan in Phase 3 OR whether the SSR deferral (D-08) needs revisiting.
       - DO NOT roll back the lazy-load + jspdf prune — those are net positive even if the absolute LCP target is missed.
       - Open a Phase 3 deferred-items entry tracking the residual gap.
  </how-to-verify>

  <resume-signal>
    Type "approved" with the recorded post-deploy measurements in your response (LCP value, CLS smoke result, bundle delta), OR describe the specific issue (e.g. "LCP regressed by +400ms", "CLS went from 0.02 to 0.15 — Pitfall 5 mitigation failed").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production bundle → end users | Bundle visualizer reveals source paths in stats.html. The output is `.gitignore`'d so it never ships; visualizer only runs under `ANALYZE=1` (developer-local). |
| Lazy-loaded chunks → code-splitting boundary | Each lazy import becomes a separate JS chunk; chunks are loaded on-demand. Same trust boundary as the rest of the bundle (signed by Sentry sourcemap upload from Phase 1 OBS-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Information Disclosure | Bundle visualizer leaks source paths in committed stats.html | mitigate | Acceptance criterion: `grep -c "stats\\.html" .gitignore` ≥ 1. Visualizer output is build-time only and never committed. SEVERITY: medium (would expose internal module structure to attackers if leaked). |
| T-02-08-02 | DoS (UX) | Lazy-load fails (network error / chunk 404 after cache bust) | mitigate | Each `{#await import()}` block has a `{:catch}` clause rendering a "Reload the page" message. SvelteKit's adapter-vercel handles chunk versioning; transient 404s on stale chunks are user-handleable. SEVERITY: low. |
| T-02-08-03 | Tampering (process) | staleTime weakening regresses cache contract | mitigate | Acceptance criterion: `grep -c "staleTime.*Infinity" src/lib/clients/queryClient.ts` ≥ 1 (the global default not regressed). Per CLAUDE.md ground truth — manual-invalidation is intentional. SEVERITY: medium (a regression here would silently change cache freshness across the app). |
| T-02-08-04 | DoS (UX) | CLS regression on lazy-load tab switch | mitigate | Pitfall 5 mitigation: every `{#await}` block has `min-h-[Xpx]` skeleton placeholder. Acceptance criterion verifies presence; post-deploy CLS smoke test in Task 3 verifies behavior. SEVERITY: medium (jarring UX would tank engagement). |
| T-02-08-05 | Information Disclosure | Removed jspdf dep was loading attacker-controlled data | accept | jspdf imports were 0 in src/ (verified by Task 1 sub-step 1 grep). Removal cannot regress information disclosure because nothing was using it. SEVERITY: none. |
| T-02-08-06 | Tampering | TRADE-01 lockdown regression in this file | mitigate | Acceptance criterion re-verifies TRADE-01 gate for `+page.svelte` (which Plan 01 codemodded). The lazy-load rewrite touches markup, not the IO-perspective accessor sites. SEVERITY: medium (would re-open the side-inversion bug class). |

No HIGH severity threats. ASVS V5 Input Validation preserved (no new untrusted-input surface). ASVS V7 Error Handling: each `{#await}` block has explicit `{:catch}` with user-actionable message.
</threat_model>

<verification>
**Phase-exit gates contributed by this plan (per 02-VALIDATION.md):**
- `grep -c "rollup-plugin-visualizer\|visualizer" vite.config.*` ≥ 1.
- `grep -cE "await import\\(['\"]\\\$lib/components/orders/(LimitOrder|DcaOrder)" "src/routes/(main)/trade/[id]/+page.svelte"` ≥ 2.
- `grep -cE "\"(jspdf|jspdf-autotable)\":" package.json` = 0.
- `grep -c "stats\\.html" .gitignore` ≥ 1.
- Manual: pre-deploy + post-deploy p75 LCP captured in 02-RUNBOOK.md.
- Manual: CLS smoke test outcome recorded.
- Manual: bundle-size delta recorded.

**Inherited cross-cutting (must not regress):**
- TRADE-04 gate (Plan 07): tests/lib/utils/marketOrderFill.test.ts contains TRADE-04 regression matrix; total project tests ≥ 470.
- TRADE-03 gate (Plan 06): `failWith(` count ≥ 12 in marketOrderExecution.ts; D-05 inline error in MarketOrder.svelte.
- TRADE-02 gate (Plan 03): `marketOrderExecution.ts` does not import from `$lib/stores/transaction`.
- TRADE-01 gate (Plan 01): 0 raw IO-perspective property reads outside allowlist + comments.
- svelte-check error count ≤ 3.
- All Phase 1 cleanup greps remain at 0.

**Phase-exit verification (last plan in Phase 2 — runs the FULL gate battery):**
After Tasks 1-3 complete, re-run every gate from 02-VALIDATION.md "Phase-Wide Verification Anchors" + the inherited gates above. Document the full pass/fail in 02-08-SUMMARY.md.
</verification>

<success_criteria>
- rollup-plugin-visualizer installed + registered behind ANALYZE=1.
- stats.html in .gitignore.
- jspdf + jspdf-autotable removed from package.json (0 imports verified).
- LimitOrder + DcaOrder lazy-loaded with `min-h-[Xpx]` skeleton placeholders.
- MarketOrder stays eager (default tab / first paint).
- Chart libraries lazy-loaded.
- TanStack Query waterfall reorganized (Tier 3 queries kicked off speculatively at mount).
- staleTime: Infinity unchanged in queryClient.ts.
- Pre-deploy + post-deploy p75 LCP captured in 02-RUNBOOK.md.
- Post-deploy p75 LCP < 2.5s on /trade/[id] OR documented gap-tracking entry in deferred-items.
- Bundle-size delta recorded.
- CLS smoke test passed.
- All cross-cutting gates from Plans 01-07 preserved.
- Phase 2 success criterion 5 (p75 LCP target) met OR explicitly tracked as deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-02-trade-execution-backbone-refactor/02-08-SUMMARY.md`. Include:
- Pre-deploy + post-deploy p75 LCP values + delta.
- Bundle-size delta (gzip).
- CLS smoke test outcome.
- Top 3 bundle offenders identified by visualizer (informs any Phase 3 follow-up).
- Cross-cutting gate re-verification table (every Phase 2 gate, Pass/Fail).
- TRADE-01..04 + PERF-01 success-criteria satisfaction summary.
- The full operational runbook content moved into `02-RUNBOOK.md` (Speed Insights URL, baseline numbers, deploy verification recipe).

Also create `.planning/phases/phase-02-trade-execution-backbone-refactor/02-RUNBOOK.md` if not already created during Task 0. The runbook should mirror Phase 1's `01-RUNBOOK.md` shape: dashboard URL, smoke tests, env-var checklist, deferred items hand-off.

This is the LAST plan in Phase 2 — the SUMMARY closes the phase.
</output>
