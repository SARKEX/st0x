---
phase: 04
plan: 10
type: execute
wave: 6
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05, 04-06, 04-07, 04-08, 04-09]
files_modified:
  - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, DRIFT-01, DRIFT-02, DRIFT-03]
requirements_addressed: [TEST-01, TEST-02, TEST-03, TEST-04, DRIFT-01, DRIFT-02, DRIFT-03]
requirements_verified: [TEST-01, TEST-02, TEST-03, TEST-04, DRIFT-01, DRIFT-02, DRIFT-03]
tags: [phase-4, runbook, phase-exit, verification, cross-cutting-gates, milestone-close]
must_haves:
  truths:
    - "All 7 Phase 4 REQ-IDs (TEST-01..04, DRIFT-01..03) verified by named grep gates from RESEARCH §Validation Architecture (lines 600–706) and PATTERNS Pattern E"
    - "TEST-01 evidence: tests/hooks/{cors,csp,public-paths,admin-gate,wallet-session,bot-rejection}.test.ts all exist with describe blocks"
    - "TEST-02 evidence: every state-mutating admin handler imports $lib/server/auditLog; per-endpoint audit test files exist for codes / excluded-wallets / pool-wallets / team-wallets / snapshots-trigger / snapshots-regenerate / referral-programme-migrate / referral-programme-refresh"
    - "TEST-03 evidence: tests/helpers/anvil.ts + tests/helpers/loadTranscript.ts + tests/integration/marketOrder/anvil-fork.test.ts + ≥ 7 redacted JSON fixtures under tests/fixtures/marketOrder/ exist; no un-redacted hex addresses in fixtures (modulo canonical contract allowlist)"
    - "TEST-04 evidence: src/lib/server/snapshots/scraper.test.ts exists and contains pagination + wrappedTokenTransfers fallback + transient categories"
    - "DRIFT-01 evidence: zero (TOKENS|ALL_TOKENS).find( occurrences in src/ outside src/lib/config/tokens.ts and tests/fixtures/eslint/token-lookup-violation.ts; ESLint no-restricted-syntax rule active in eslint.config.js"
    - "DRIFT-02 evidence: zero hardcoded USDC mainnet address (0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913) occurrences in src/routes/admin/ + src/routes/api/admin/"
    - "DRIFT-03 evidence: zero Rhinestone/EIP-7702/account-abstraction/Account Abstraction occurrences in CLAUDE.md; Order Semantics INPUT/OUTPUT preserved; Ground Truth header present"
    - "Phase 2 carry-forward gates remain green: TRADE-01 IO-perspective lockdown (0 violations outside allowlist), TRADE-02 cycle severance (no $lib/stores/transaction import in marketOrderExecution.ts), failWith() count ≥ 12 in marketOrderExecution.ts, EMERGENCY_RATIO_MULTIPLIER = 0, svelte-check baseline ≤ 3 errors, staleTime: Infinity preserved in src/lib/queries/"
    - "Phase 3 carry-forward gates remain green: no Alchemy hardcoding (alchemy.com / alchemyapi) in src/, no Math.random in src/lib/server/accessCodes/ + src/lib/server/referrals/, no fallback secret strings ((SECRET|TOKEN|KEY).*?? '...') in src/lib/server/, session-cookie shape preserved in src/hooks.server.ts, all Phase 3 SEC-* + REL-* grep gates from 03-VALIDATION.md re-pass"
    - "Cross-cutting build/test gates green: npm test -- --run exits 0; npm run test:integration exits 0; npm run check ≤ 3 errors (Phase 2 baseline); npm run lint exits 0; npm run build exits 0"
    - "04-RUNBOOK.md authored mirroring 03-RUNBOOK.md shape (≥ 200 lines) with 7 named sections from <interfaces>"
    - "REQUIREMENTS.md TEST-01..04 + DRIFT-01..03 marked [x] Complete with closing-note synthesis matching Phase 2 + Phase 3 entry style; Traceability table updated for all 7 IDs"
    - "ROADMAP.md Phase 4 marked [x] Complete with completion date and 10/10 progress entry"
    - "Milestone-close handoff documents HUMAN-UAT carry-forwards by name: PERF-01 numeric p75 LCP < 2.5s (Phase 2 deferred), SEC-03+04 D-04b runtime UX assertion (Phase 3 deferred); 04-RUNBOOK.md instructs operator to run /gsd-verify-work against each"
  artifacts:
    - path: .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md
      provides: "Phase 4 operational runbook — Foundry/anvil CI setup, OBS-03 transcript-capture, fixture refresh, DRIFT-01 codemod replay, phase-exit grep recipe, cross-cutting gate verification, milestone-close handoff"
      min_lines: 200
    - path: .planning/REQUIREMENTS.md
      provides: "All 7 Phase 4 REQ-IDs marked Complete; Traceability table closed"
      contains: "TEST-01"
    - path: .planning/ROADMAP.md
      provides: "Phase 4 entry marked [x] Complete; per-phase progress table updated to 10/10"
      contains: "Phase 4"
  key_links:
    - from: 04-RUNBOOK.md
      to: .planning/phases/phase-03-production-grade-hardening/03-RUNBOOK.md
      via: "Same shape (env-var checklist, smoke recipes, hand-off section); 04-RUNBOOK adds Foundry/anvil + transcript-capture + milestone-close handoff"
      pattern: "RUNBOOK"
    - from: REQUIREMENTS.md
      to: ROADMAP.md
      via: "Both files reflect Phase 4 close + milestone close consistently (10/10 plans, 7/7 REQ-IDs)"
      pattern: "Complete"
---

<objective>
Phase 4 close (and milestone close): phase-exit grep gates re-verified, Phase 2 + Phase 3 cross-cutting carry-forward gates re-verified, 04-RUNBOOK.md authored, REQUIREMENTS.md + ROADMAP.md updated to mark Phase 4 + the stabilization milestone closed.

Per CONTEXT D-06 Wave 6 + RESEARCH §"Validation Architecture" (lines 600–706) + PATTERNS Pattern E (analog citations to 01-08 / 02-08 / 03-11): this plan re-runs every grep gate listed in those sources as ITS acceptance criteria. **No new behavioral code work — entirely verification + documentation.**

Per CONTEXT discretion + Pattern E precedent: 04-RUNBOOK.md mirrors the shape of 03-RUNBOOK.md (~250–300 lines) and adds Phase 4-specific surfaces:
1. Pre-deploy env-var checklist (carry-forward only — no NEW env vars in Phase 4; BASE_RPC_URL already provisioned by Phase 3 SEC-01 and reused for CI integration tests)
2. Foundry / anvil CI setup (NEW Phase 4 surface — install step pin, BASE_RPC_URL secret read access, integration job wiring, npm run test:integration boundary)
3. OBS-03 transcript-capture procedure for refreshing TEST-03 fixtures (Vercel Logs query + jq pipeline + redaction recipe + target dir + schema versioning note)
4. DRIFT-01 codemod replay procedure (idempotent ts-morph script for future TOKENS.find migrations + ESLint rule recurrence guard)
5. Phase-exit grep recipe (single bash block re-runnable on demand — union of per-REQ-ID gates + Phase 2 + Phase 3 carry-forward)
6. Cross-cutting gate verification recipe (npm test / test:integration / check / lint / build)
7. Milestone-close hand-off — HUMAN-UAT items still pending (PERF-01 p75 LCP < 2.5s post-deploy, SEC-03+04 D-04b runtime UX assertion); /gsd-verify-work invocation instruction; milestone exit checklist

Output: 04-RUNBOOK.md + REQUIREMENTS.md updates + ROADMAP.md updates + this plan's SUMMARY closing the phase + the milestone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-CONTEXT.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RESEARCH.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-PATTERNS.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-VALIDATION.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-01-SUMMARY.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-02-SUMMARY.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-03-SUMMARY.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-04-SUMMARY.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-05-SUMMARY.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-06-SUMMARY.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-07-SUMMARY.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-08-SUMMARY.md
@.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-09-SUMMARY.md
@.planning/phases/phase-01-shrink-the-surface-see-what-s-happening/01-RUNBOOK.md
@.planning/phases/phase-02-trade-execution-backbone-refactor/02-RUNBOOK.md
@.planning/phases/phase-03-production-grade-hardening/03-RUNBOOK.md

<interfaces>
<!-- Mirror Phase 3 RUNBOOK shape (250–300 lines). Sections in order: -->

01-RUNBOOK.md / 02-RUNBOOK.md / 03-RUNBOOK.md base shape:
- Phase Summary (table of waves → plans → REQs → surfaces)
- Pre-Deploy Env-Var Checklist
- Cross-cutting Cleanup Grep Recipe (runnable bash block)
- Hand-off to Next Phase / Milestone Exit
- Notes / Anomalies

03-RUNBOOK.md added (Phase 3-specific):
- Alchemy rotation procedure (D-02a 6-step)
- Session-cookie smoke recipe (D-04 manual gate)
- Vercel preview hCaptcha verification (SEC-07)
- Rain registry refresh procedure (REL-03)
- Phase-Exit Verification Log (Task 1 output)

04-RUNBOOK.md adds (Phase 4-specific):
- Foundry / anvil CI setup procedure (install pin + BASE_RPC_URL secret read access + ~/.foundry cache + integration job wiring + npm run test:integration boundary)
- OBS-03 transcript-capture procedure for TEST-03 fixture refresh (Vercel Logs query + jq + redaction recipe + target dir + schema versioning)
- DRIFT-01 codemod replay procedure (idempotent script invocation; ESLint rule as recurrence guard)
- Per-REQ-ID phase-exit grep recipe (union of TEST-01..04 + DRIFT-01..03 gates from RESEARCH lines 600–662)
- Cross-cutting Phase 2 + Phase 3 carry-forward grep recipe (RESEARCH lines 664–706 + 03-VALIDATION §"Phase-Exit Verification")
- Build/test gate recipe (npm test / test:integration / check / lint / build)
- Milestone-close handoff with HUMAN-UAT items by name (PERF-01 p75 LCP, SEC-03+04 D-04b runtime UX) + /gsd-verify-work invocation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run per-REQ-ID phase-exit grep gates (TEST-01..04 file + describe evidence; DRIFT-01..03 evidence)</name>
  <read_first>
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RESEARCH.md lines 600–662 (per-REQ-ID grep recipes)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-VALIDATION.md §"Per-Task Verification Map" + §"Manual-Only Verifications"
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-PATTERNS.md §"Pattern E: Phase-exit grep gates"
  </read_first>
  <files></files>
  <action>
Run the following grep set verbatim. Capture output (gate name → command → expected vs actual → pass/fail) in a verification log to be inserted into 04-RUNBOOK.md §"Phase-Exit Verification Log" (Task 5). If ANY gate fails, do NOT proceed — surface the regression and produce a remediation plan.

**DRIFT-03 — CLAUDE.md ground-truth alignment:**

```bash
# Expect 0 matches (no AA / multi-chain claims)
grep -E "Rhinestone|EIP-7702|account-abstraction|Account Abstraction" CLAUDE.md
test "$(grep -cE "Rhinestone|EIP-7702|account-abstraction|Account Abstraction" CLAUDE.md)" -eq 0

# Expect ≥ 1 (Order Semantics preserved per D-05)
test "$(grep -c "INPUT/OUTPUT Perspective" CLAUDE.md)" -ge 1

# Expect ≥ 1 (Ground Truth header added per D-05)
test "$(grep -c "Ground Truth" CLAUDE.md)" -ge 1
```

**DRIFT-01 — TOKENS.find / ALL_TOKENS.find allowlist:**

```bash
# Expect 0 violations outside allowlist (canonical lookup module + ESLint fixture)
test "$(grep -RE '(TOKENS|ALL_TOKENS)\.find\(' src/ \
  | grep -v 'src/lib/config/tokens.ts' \
  | grep -v 'tests/fixtures/eslint/token-lookup-violation.ts' \
  | wc -l)" -eq 0

# Expect ≥ 1 (ESLint no-restricted-syntax rule registered for DRIFT-01 — mirrors TRADE-01 at eslint.config.js:46-65)
test "$(grep -cE 'TOKENS\.find|ALL_TOKENS\.find|drift-01|no-restricted-syntax' eslint.config.js)" -ge 1
```

**DRIFT-02 — hardcoded USDC address removal in admin paths:**

```bash
# Expect 0 hardcoded USDC mainnet address occurrences in admin surfaces
test "$(grep -RE '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913' \
  src/routes/admin/ src/routes/api/admin/ | wc -l)" -eq 0

# Expect ≥ 1 (helper used in place of hardcoded constant)
test "$(grep -RE 'getPaymentTokensForNetwork|isPaymentToken' \
  src/routes/admin/ src/routes/api/admin/ | wc -l)" -ge 1
```

**TEST-01 — hooks.server.ts test files + describe blocks:**

```bash
# Expect 6 files all present
ls tests/hooks/cors.test.ts \
   tests/hooks/csp.test.ts \
   tests/hooks/public-paths.test.ts \
   tests/hooks/admin-gate.test.ts \
   tests/hooks/wallet-session.test.ts \
   tests/hooks/bot-rejection.test.ts

# Expect ≥ 1 describe block per file
for f in tests/hooks/cors.test.ts tests/hooks/csp.test.ts tests/hooks/public-paths.test.ts \
         tests/hooks/admin-gate.test.ts tests/hooks/wallet-session.test.ts tests/hooks/bot-rejection.test.ts; do
  test "$(grep -c 'describe(' "$f")" -ge 1 || echo "FAIL describe: $f"
done

# Expect tests/hooks/_helpers.ts factories present (D-02a)
test -f tests/hooks/_helpers.ts
test "$(grep -cE 'createMockRequestEvent|createMockKv|createMockSession' tests/hooks/_helpers.ts)" -ge 3
```

**TEST-02 — admin audit-log fan-out:**

```bash
# Expect every state-mutating admin handler imports auditLog
MISSING=0
for f in $(grep -lE "export (const|async function) (POST|PUT|PATCH|DELETE)" \
           $(find src/routes/api/admin -name "+server.ts")); do
  grep -q "from '\$lib/server/auditLog'" "$f" || { echo "MISSING: $f"; MISSING=$((MISSING+1)); }
done
test "$MISSING" -eq 0

# Expect per-endpoint audit test files (8 endpoints per RESEARCH §TEST-02 inventory)
ls tests/lib/admin/codes.audit.test.ts \
   tests/lib/admin/excluded-wallets.audit.test.ts \
   tests/lib/admin/pool-wallets.audit.test.ts \
   tests/lib/admin/team-wallets.audit.test.ts \
   tests/lib/admin/snapshots-trigger.audit.test.ts \
   tests/lib/admin/snapshots-regenerate.audit.test.ts \
   tests/lib/admin/referral-programme-migrate.audit.test.ts \
   tests/lib/admin/referral-programme-refresh.audit.test.ts

# Expect ≥ 1 createAuditLogger call in the 5 endpoints that previously lacked it (RESEARCH Wave 0 ADD list)
for f in src/routes/api/admin/excluded-wallets/+server.ts \
         src/routes/api/admin/pool-wallets/+server.ts \
         src/routes/api/admin/team-wallets/+server.ts \
         src/routes/api/admin/snapshots/trigger/+server.ts \
         src/routes/api/admin/snapshots/regenerate/+server.ts; do
  test "$(grep -c 'createAuditLogger' "$f")" -ge 1 || echo "FAIL audit emission: $f"
done
```

**TEST-03 — marketOrderExecution.ts anvil-fork + replay JSON suite:**

```bash
# Expect anvil + transcript loader helpers
test -f tests/helpers/anvil.ts
test -f tests/helpers/loadTranscript.ts

# Expect anvil-fork integration test
test -f tests/integration/marketOrder/anvil-fork.test.ts

# Expect ≥ 7 fixture files (RESEARCH A2 / D-01)
test "$(ls tests/fixtures/marketOrder/*.json 2>/dev/null | wc -l)" -ge 7

# Expect 0 un-redacted hex addresses in fixtures (modulo canonical USDC contract address allowlist + redacted marker)
test "$(grep -RhE '0x[a-fA-F0-9]{40}' tests/fixtures/marketOrder/ \
  | grep -v '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913' \
  | grep -v '0x...redacted' \
  | wc -l)" -eq 0

# Expect integration config + script entry
test -f vitest.integration.config.ts -o -f vite.config.integration.js
test "$(grep -c '"test:integration"' package.json)" -ge 1
```

**TEST-04 — scraper edge-case suite:**

```bash
# Expect co-located test file
test -f src/lib/server/snapshots/scraper.test.ts

# Expect 3 categories from RESEARCH §TEST-04 Resolution
test "$(grep -cE '(pagination|wrappedTokenTransfers fallback|transient)' src/lib/server/snapshots/scraper.test.ts)" -ge 3
```

Record each gate's actual output for verbatim inclusion in 04-RUNBOOK.md §"Phase-Exit Verification Log".
  </action>
  <verify>
    <automated>test "$(grep -cE 'Rhinestone|EIP-7702|account-abstraction|Account Abstraction' CLAUDE.md)" -eq 0 &amp;&amp; test "$(grep -c 'INPUT/OUTPUT Perspective' CLAUDE.md)" -ge 1 &amp;&amp; test "$(grep -c 'Ground Truth' CLAUDE.md)" -ge 1 &amp;&amp; test "$(grep -RE '(TOKENS|ALL_TOKENS)\.find\(' src/ | grep -v 'src/lib/config/tokens.ts' | grep -v 'tests/fixtures/eslint/token-lookup-violation.ts' | wc -l)" -eq 0 &amp;&amp; test "$(grep -RE '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913' src/routes/admin/ src/routes/api/admin/ | wc -l)" -eq 0 &amp;&amp; ls tests/hooks/cors.test.ts tests/hooks/csp.test.ts tests/hooks/public-paths.test.ts tests/hooks/admin-gate.test.ts tests/hooks/wallet-session.test.ts tests/hooks/bot-rejection.test.ts &amp;&amp; test -f tests/lib/admin/codes.audit.test.ts &amp;&amp; test -f tests/helpers/anvil.ts &amp;&amp; test -f tests/integration/marketOrder/anvil-fork.test.ts &amp;&amp; test "$(ls tests/fixtures/marketOrder/*.json 2>/dev/null | wc -l)" -ge 7 &amp;&amp; test -f src/lib/server/snapshots/scraper.test.ts &amp;&amp; test "$(grep -cE '(pagination|wrappedTokenTransfers fallback|transient)' src/lib/server/snapshots/scraper.test.ts)" -ge 3</automated>
  </verify>
  <acceptance_criteria>
    - All DRIFT-01..03 grep gates pass with exact expected counts
    - All TEST-01..04 file-existence + describe + category gates pass
    - Per-endpoint audit-log emission grep passes for the 5 ADD endpoints
    - Verification output captured for inclusion in 04-RUNBOOK.md (Task 5)
  </acceptance_criteria>
  <done>
    - All per-REQ-ID gates pass
    - Findings logged for inclusion in RUNBOOK
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run Phase 2 carry-forward grep gates (TRADE-01 lockdown, TRADE-02 cycle severance, failWith ≥ 12, EMERGENCY_RATIO_MULTIPLIER = 0, staleTime: Infinity)</name>
  <read_first>
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RESEARCH.md lines 664–706 (cross-cutting re-verification grep set)
    - .planning/phases/phase-02-trade-execution-backbone-refactor/02-CONTEXT.md §"Cross-cutting constraints" (gates that must hold)
    - .planning/phases/phase-03-production-grade-hardening/03-VALIDATION.md §"Phase-Exit Verification" lines 105–119 (Phase 2 carry-forward block established by Phase 3 — re-use verbatim)
  </read_first>
  <files></files>
  <action>
Run the following Phase 2 carry-forward gates verbatim. Capture each output for the verification log.

```bash
# TRADE-01 IO-perspective lockdown — 0 raw IO-perspective property reads outside canonical allowlist
test "$(grep -RE '\.(inputTokenAddress|outputTokenAddress|inputIOIndex|outputIOIndex)\b' src/ tests/ \
  | grep -vE 'orderPerspective\.ts|utils/orderbook\.ts|api/orders\.ts|generated-graphql\.ts|io-perspective-violation\.ts' \
  | wc -l)" -eq 0

# TRADE-02 cycle severance — marketOrderExecution must NOT import from $lib/stores/transaction
test "$(grep -cE "from ['\"]\\\$lib/stores/transaction['\"]" src/lib/services/marketOrderExecution.ts)" -eq 0

# OBS-03 / Phase 1 transcript discipline — failWith() count ≥ 12 in marketOrderExecution.ts
test "$(grep -c 'failWith(' src/lib/services/marketOrderExecution.ts)" -ge 12

# EMERGENCY_RATIO_MULTIPLIER = 0 occurrences anywhere under src/
test "$(grep -RcE 'EMERGENCY_RATIO_MULTIPLIER' src/ | grep -v ':0$' | wc -l)" -eq 0

# staleTime: Infinity preserved (TanStack Query default) — at least 1 reference in src/lib/queries
test "$(grep -RcE 'staleTime:\s*Infinity' src/lib/queries/ | grep -v ':0$' | wc -l)" -ge 1
```

If ANY gate fails, halt — Phase 4 must NOT close while Phase 2 invariants are regressed.
  </action>
  <verify>
    <automated>test "$(grep -RE '\.(inputTokenAddress|outputTokenAddress|inputIOIndex|outputIOIndex)\b' src/ tests/ | grep -vE 'orderPerspective\.ts|utils/orderbook\.ts|api/orders\.ts|generated-graphql\.ts|io-perspective-violation\.ts' | wc -l)" -eq 0 &amp;&amp; test "$(grep -cE "from ['\"]\\\$lib/stores/transaction['\"]" src/lib/services/marketOrderExecution.ts)" -eq 0 &amp;&amp; test "$(grep -c 'failWith(' src/lib/services/marketOrderExecution.ts)" -ge 12 &amp;&amp; test "$(grep -RcE 'EMERGENCY_RATIO_MULTIPLIER' src/ | grep -v ':0$' | wc -l)" -eq 0 &amp;&amp; test "$(grep -RcE 'staleTime:\s*Infinity' src/lib/queries/ | grep -v ':0$' | wc -l)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - All 5 Phase 2 carry-forward gates pass
    - Output captured for the verification log
  </acceptance_criteria>
  <done>
    - Phase 2 invariants confirmed green; Phase 4 has not regressed any of them
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run Phase 3 carry-forward grep gates (no Alchemy hardcoding, no Math.random in security paths, no fallback secrets, session-cookie shape preserved, all SEC-* + REL-* gates re-pass)</name>
  <read_first>
    - .planning/phases/phase-03-production-grade-hardening/03-VALIDATION.md §"Phase-Exit Verification" (full grep set, lines 88–104)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RESEARCH.md lines 687–701 (Phase 3 cross-cutting subset)
  </read_first>
  <files></files>
  <action>
Run the Phase 3 SEC + REL grep set verbatim — these MUST remain green at Phase 4 close. Capture each output for the verification log.

```bash
# SEC-01 — committed Alchemy key absent from src/
! grep -r "y3BXawVv5uuP" src/

# SEC-01 — no Alchemy hardcoding (alchemy.com / alchemyapi) in source
test "$(grep -RE 'alchemy\.com|alchemyapi' src/ --include='*.ts' --include='*.svelte' \
  | grep -vE 'config/networks\.ts|server/raindex\.ts' \
  | wc -l)" -eq 0

# SEC-02 — fallback secret strings removed
! grep -E "'st0x-session-secret-2024'|'default-csrf-secret-change-in-production'" \
  src/lib/server/auth.ts src/lib/server/csrf.ts

# SEC-02 generalized — no env-var fallback secrets pattern in src/lib/server/
test "$(grep -RhE '(SECRET|TOKEN|KEY).*\?\?\s*['\''\"]' src/lib/server/ \
  | grep -vE '//|\\*' \
  | wc -l)" -eq 0

# SEC-03 — wallet-address auth read removed; session cookie consumer present
! grep -rn "cookies.get('wallet-address')" src/lib src/hooks.server.ts src/routes/api 2>/dev/null
test "$(grep -c 'readSession' src/hooks.server.ts)" -ge 1

# SEC-03+04 — session-cookie shape preserved in hooks.server.ts
test "$(grep -cE 'wallet-session|session-id|readSession' src/hooks.server.ts)" -ge 1

# SEC-04 — session-bound CSRF helpers present
test "$(grep -cE 'generateCsrfTokenForSession|validateCsrfTokenForSession' src/lib/server/csrf.ts)" -ge 2

# SEC-05 — no Math.random in security-sensitive paths
! grep -RE 'Math\.random' src/lib/server/accessCodes/ src/lib/server/referrals/ 2>/dev/null
! grep -E 'Math\.random\(\)' src/lib/server/accessCodes.ts src/lib/server/referrals.ts 2>/dev/null

# SEC-06 — snapshotsPreview tier + requireAdmin present
test "$(grep -c 'snapshotsPreview' src/lib/server/rateLimit.ts)" -ge 1
test "$(grep -c 'requireAdmin' src/routes/api/snapshots/generate/+server.ts)" -ge 2

# SEC-07 — VERCEL_ENV gate replaces NODE_ENV gate
! grep -E "process\.env\.NODE_ENV === 'production'" src/lib/server/accessCodes.ts
test "$(grep -c 'VERCEL_ENV' src/lib/server/accessCodes.ts)" -ge 1

# REL-01 — withRetry + chain-exhaustion throw
test "$(grep -c 'withRetry' src/lib/server/snapshots/generator.ts)" -ge 1
test "$(grep -c 'all .* RPCs exhausted' src/lib/server/snapshots/generator.ts)" -eq 1
test "$(grep -c 'no block lookup succeeded' src/lib/server/snapshots/generator.ts)" -eq 1

# REL-02 — viem fallback transport in accessCodes.ts; old label gone
test "$(grep -c 'fallback(' src/lib/server/accessCodes.ts)" -ge 1
! grep -E "'alchemy-base-mainnet'" src/lib/server/accessCodes.ts
test "$(grep -c \"'fallback-chain-base'\" src/lib/server/accessCodes.ts)" -ge 3

# REL-03 — runtime GitHub raw fetch removed; vendored registry present
! grep -RE 'RAIN_STRATEGIES_COMMIT|raw\.githubusercontent\.com.*rain\.strategies' src/
test -d static/registry
```

If ANY gate fails, halt — Phase 4 close cannot proceed while Phase 3 SEC/REL invariants are regressed.
  </action>
  <verify>
    <automated>! grep -r "y3BXawVv5uuP" src/ &amp;&amp; ! grep -E "'st0x-session-secret-2024'|'default-csrf-secret-change-in-production'" src/lib/server/auth.ts src/lib/server/csrf.ts &amp;&amp; test "$(grep -c 'readSession' src/hooks.server.ts)" -ge 1 &amp;&amp; test "$(grep -cE 'generateCsrfTokenForSession|validateCsrfTokenForSession' src/lib/server/csrf.ts)" -ge 2 &amp;&amp; test "$(grep -c 'snapshotsPreview' src/lib/server/rateLimit.ts)" -ge 1 &amp;&amp; test "$(grep -c 'VERCEL_ENV' src/lib/server/accessCodes.ts)" -ge 1 &amp;&amp; test "$(grep -c 'withRetry' src/lib/server/snapshots/generator.ts)" -ge 1 &amp;&amp; test "$(grep -c 'fallback(' src/lib/server/accessCodes.ts)" -ge 1 &amp;&amp; ! grep -RE 'RAIN_STRATEGIES_COMMIT|raw\.githubusercontent\.com.*rain\.strategies' src/ &amp;&amp; test -d static/registry</automated>
  </verify>
  <acceptance_criteria>
    - All Phase 3 SEC-01..07 + REL-01..03 grep gates re-pass
    - No Alchemy hardcoding outside config layer
    - No fallback secret strings in src/lib/server/
    - Session-cookie shape preserved in hooks.server.ts
    - Output captured for the verification log
  </acceptance_criteria>
  <done>
    - All Phase 3 invariants confirmed green
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Run cross-cutting build/test gates (npm test / test:integration / check / lint / build)</name>
  <read_first>
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-VALIDATION.md §"Sampling Rate" (full suite command + thresholds)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RESEARCH.md §"Validation Architecture" (sampling rate)
    - .planning/phases/phase-02-trade-execution-backbone-refactor/02-CONTEXT.md (svelte-check baseline = 3 errors discipline)
  </read_first>
  <files></files>
  <action>
Run the full cross-cutting gate set. ALL must exit 0 (svelte-check is allowed up to the established baseline of ≤ 3 errors). Capture each command's tail output for the verification log.

```bash
# Quick suite (jsdom, fast)
npm test -- --run 2>&1 | tail -20
# Expect: exit 0, all tests pass

# Integration suite (anvil fork + replay JSON)
npm run test:integration 2>&1 | tail -20
# Expect: exit 0; anvil spawns, fork connects to BASE_RPC_URL, all integration tests pass

# svelte-check (baseline ≤ 3 errors per Phase 2 close)
npm run check 2>&1 | tail -10
# Expect: ≤ 3 errors

# ESLint (DRIFT-01 rule active; lint fixture exits non-zero in isolation, but full lint must pass for production code)
npm run lint 2>&1 | tail -10
# Expect: exit 0

# Production build (no boot-time crashes from SEC-02 fail-closed; bundle builds)
npm run build 2>&1 | tail -20
# Expect: exit 0
```

If `npm run check` exceeds 3 errors, halt and triage. If any other command exits non-zero, halt and triage.

Record each command's tail output for verbatim inclusion in 04-RUNBOOK.md §"Phase-Exit Verification Log".
  </action>
  <verify>
    <automated>npm test -- --run 2>&amp;1 | tail -5 &amp;&amp; npm run test:integration 2>&amp;1 | tail -5 &amp;&amp; npm run check 2>&amp;1 | tail -5 &amp;&amp; npm run lint 2>&amp;1 | tail -5 &amp;&amp; npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm test -- --run exits 0
    - npm run test:integration exits 0
    - npm run check ≤ 3 errors
    - npm run lint exits 0
    - npm run build exits 0
    - All command outputs captured for the verification log
  </acceptance_criteria>
  <done>
    - All build/test gates green
    - Phase 4 ready for documentation + close-out tasks
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Author 04-RUNBOOK.md (Foundry/anvil CI + OBS-03 transcript-capture + DRIFT-01 codemod replay + phase-exit grep recipe + milestone-close handoff)</name>
  <read_first>
    - .planning/phases/phase-01-shrink-the-surface-see-what-s-happening/01-RUNBOOK.md (full file — RUNBOOK template shape)
    - .planning/phases/phase-02-trade-execution-backbone-refactor/02-RUNBOOK.md (full file — RUNBOOK additions for Phase 2)
    - .planning/phases/phase-03-production-grade-hardening/03-RUNBOOK.md (full file — most recent precedent; mirror its shape directly)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RESEARCH.md lines 708–716 (04-RUNBOOK.md sections enumerated by RESEARCH)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-VALIDATION.md §"Manual-Only Verifications" (HUMAN-UAT items by name)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-CONTEXT.md (Claude's discretion bullet on cross-phase HUMAN-UAT carry-forward)
  </read_first>
  <files>.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md</files>
  <action>
Author `.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md` (~250–300 lines, mirroring 03-RUNBOOK.md shape).

**Required sections (in order, all must be present):**

## Header (mirror 03-RUNBOOK.md lines 1–17)
- Phase 4 — Operational Runbook
- **Phase:** 04-boundary-tests-and-drift-cleanup
- **Created:** <execution date>
- **Last verified:** <execution date> (phase-exit grep + svelte-check + vitest run by Plan 04-10)
- **Status:** Phase 4 plans complete; milestone close handoff items below
- Pointer paragraphs to 01/02/03-RUNBOOK.md for prior-phase canonical content

## Phase 4 Summary
Wave → plan → REQ → surface table mirroring 03-RUNBOOK.md lines 22–34:

| Wave | Plan | REQ | Surface |
|------|------|-----|---------|
| 1 | 04-01 | DRIFT-03 | CLAUDE.md surgical edit + Ground Truth header pointing at .planning/codebase/ |
| 2 | 04-02 | DRIFT-02 | admin/+page.svelte + api/admin/nansen/+server.ts USDC hardcoding → getPaymentTokensForNetwork / isPaymentToken |
| 3 | 04-03 | DRIFT-01 | ts-morph codemod + ESLint no-restricted-syntax rule banning TOKENS.find / ALL_TOKENS.find outside allowlist; lint fixture |
| 4 | 04-04 | TEST-01 | tests/hooks/{cors,csp,public-paths,admin-gate,wallet-session,bot-rejection}.test.ts + _helpers.ts |
| 4 | 04-05 | TEST-02 | tests/lib/admin/*.audit.test.ts (8 endpoints) + createAuditLogger ADD on 5 missing endpoints |
| 5 | 04-06 | TEST-03 (anvil scaffold) | tests/helpers/anvil.ts + tests/helpers/loadTranscript.ts + vitest.integration.config.ts + Foundry CI install |
| 5 | 04-07 | TEST-03 (anvil suite) | tests/integration/marketOrder/anvil-fork.test.ts |
| 5 | 04-08 | TEST-03 (replay suite) | tests/integration/marketOrder/replay-*.test.ts + tests/fixtures/marketOrder/*.json (≥ 7 redacted transcripts) |
| 5 | 04-09 | TEST-04 | src/lib/server/snapshots/scraper.test.ts |
| 6 | 04-10 | — | This runbook + REQUIREMENTS / ROADMAP close-out + milestone exit |

## Pre-Deploy Env-Var Checklist
NO new env vars introduced by Phase 4. CI integration tests reuse `BASE_RPC_URL` (Phase 3 SEC-01 provisioned). Enumerate the carry-forward (verbatim from 03-RUNBOOK.md table) noting "no Phase 4 additions".

## Foundry / Anvil CI Setup (NEW Phase 4 surface)
- Install step (pin to version installed by 04-06): `curl -L https://foundry.paradigm.xyz | bash && foundryup`
- GHA cache key for `~/.foundry` (per RESEARCH §"Pitfall 3" reference; cite the cache pattern used in `.github/workflows/*.yml`)
- `BASE_RPC_URL` secret read access — same Vercel/GHA secret used by Phase 3 SEC-01; CI job inherits read scope (no new secret to provision)
- Integration job wiring — separate GHA job invoking `npm run test:integration`; runs in parallel with the unit-test job; required for green PR
- npm scripts boundary: `npm test` excludes `tests/integration/`; `npm run test:integration` runs them via `vitest.integration.config.ts`
- Local dev: anvil already installed via Homebrew (`/opt/homebrew/bin/anvil` v1.2.3); contributors without anvil run only `npm test`
- A1 risk: if `BASE_RPC_URL` provider is not archive-capable, anvil fork fails with `MissingTrieNode` — switch to archive-capable RPC and document in this section

## OBS-03 Transcript-Capture Procedure (TEST-03 fixture refresh)
Step-by-step — operator runs this when (a) OBS-03 transcript schema changes, (b) marketOrderExecution.ts behavior changes, (c) periodic operator review chooses new representative scenarios:

```bash
# 1. Pull recent OBS-03 failures from Vercel Logs (CLI; or use dashboard if --filter unavailable per A7)
vercel logs --filter '"failWith"' --since 7d --output json > /tmp/raw-transcripts.jsonl

# 2. Extract just the transcript JSON payload via jq
jq -r 'select(.message | contains("failWith")) | .meta.transcript' /tmp/raw-transcripts.jsonl > /tmp/transcripts.json

# 3. Redact wallet addresses (replace 40-hex addresses with the placeholder; preserve canonical contract addresses on allowlist)
sed -E 's/0x[a-fA-F0-9]{40}/0x...redacted/g; s/0x\.\.\.redacted (USDC|0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913)/\1/g' \
  /tmp/transcripts.json > /tmp/transcripts.redacted.json

# 4. Split into per-scenario JSON files in tests/fixtures/marketOrder/
#    Naming: <failure-mode>-<short-suffix>.json (e.g. stale-quote-bid-01.json)

# 5. Verify redaction completeness (phase-exit grep gate from Task 1):
grep -RhE '0x[a-fA-F0-9]{40}' tests/fixtures/marketOrder/ \
  | grep -v '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913' \
  | grep -v '0x...redacted'
# expect: 0 matches

# 6. Run replay suite to confirm fixtures load + parse against current TakeOrderFailureTranscript type:
npm run test:integration -- tests/integration/marketOrder/replay-

# 7. Commit + push.
```

**Schema versioning:** existing fixtures stay valid as long as new transcript fields are optional in consuming code. Schema-incompatible changes require a fixture refresh in the same PR that introduces them (per CONTEXT D-01c).

## DRIFT-01 Codemod Replay Procedure
Idempotent ts-morph script — re-runnable on demand for future TOKENS.find migrations:

```bash
# Re-run if a future contributor accidentally introduces TOKENS.find / ALL_TOKENS.find
npx tsx scripts/codemods/migrate-token-find.ts

# Verify post-codemod cleanliness:
grep -RE '(TOKENS|ALL_TOKENS)\.find\(' src/ \
  | grep -v 'src/lib/config/tokens.ts' \
  | grep -v 'tests/fixtures/eslint/token-lookup-violation.ts'
# expect: 0 matches

# ESLint rule is the recurrence guard — runs on every PR via `npm run lint`:
npm run lint
```

The ESLint `no-restricted-syntax` rule in `eslint.config.js` (mirroring TRADE-01 at lines 46–65) is the primary recurrence prevention; the codemod is the one-shot migration tool.

## Phase-Exit Verification Log
Insert verbatim the verification output captured by Tasks 1, 2, 3, and 4:
- Per-REQ-ID grep gates (Task 1) — gate name, command, expected, actual, pass/fail, timestamp
- Phase 2 carry-forward grep gates (Task 2)
- Phase 3 carry-forward grep gates (Task 3)
- Build/test gate output tails (Task 4)

## Cross-cutting Cleanup Grep Recipe
Single runnable bash block (the union of Tasks 1+2+3 grep sets) that future plans can invoke as a snapshot of "all 7 phase REQ-IDs + Phase 2 + Phase 3 invariants green":

```bash
#!/bin/bash
set -e
# Phase 4 + Phase 3 + Phase 2 carry-forward verification — all assertions must pass
# DRIFT-03
test "$(grep -cE 'Rhinestone|EIP-7702|account-abstraction|Account Abstraction' CLAUDE.md)" -eq 0
test "$(grep -c 'Ground Truth' CLAUDE.md)" -ge 1
# DRIFT-01
test "$(grep -RE '(TOKENS|ALL_TOKENS)\.find\(' src/ | grep -v 'src/lib/config/tokens.ts' | grep -v 'tests/fixtures/eslint/token-lookup-violation.ts' | wc -l)" -eq 0
# DRIFT-02
test "$(grep -RE '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913' src/routes/admin/ src/routes/api/admin/ | wc -l)" -eq 0
# TEST-01..04 file existence (terse — see RUNBOOK §Phase-Exit for full list)
test -f tests/hooks/wallet-session.test.ts
test -f tests/integration/marketOrder/anvil-fork.test.ts
test -f src/lib/server/snapshots/scraper.test.ts
# Phase 2 carry-forward
test "$(grep -c 'failWith(' src/lib/services/marketOrderExecution.ts)" -ge 12
test "$(grep -RcE 'EMERGENCY_RATIO_MULTIPLIER' src/ | grep -v ':0$' | wc -l)" -eq 0
# Phase 3 carry-forward
! grep -r "y3BXawVv5uuP" src/
test "$(grep -c 'readSession' src/hooks.server.ts)" -ge 1
test -d static/registry
echo "All Phase 4 + carry-forward gates passed"
```

## Hand-off — Milestone Close
Phase 4 is the final phase of the stabilization milestone. All 7 Phase 4 REQ-IDs (TEST-01..04 + DRIFT-01..03) shipped. All Phase 1 (8 REQs), Phase 2 (5 REQs), Phase 3 (10 REQs) REQ-IDs remain Complete. **Total milestone REQ-IDs delivered: 30.**

### HUMAN-UAT Carry-Forward Items (deferred to milestone exit)
These items deliberately landed as code/test changes without numeric post-deploy validation. The operator MUST run `/gsd-verify-work` against each as part of milestone exit (per CONTEXT discretion + 04-VALIDATION.md §"Manual-Only Verifications"):

| Item | Originating Phase | What to verify | How |
|------|-------------------|----------------|-----|
| **PERF-01 numeric p75 LCP < 2.5s** | Phase 2 (PERF-01 closed structurally; numeric validation deferred) | After 24h+ Speed Insights window post-deploy, p75 LCP on /trade/[id] < 2.5s | Visit Vercel Speed Insights dashboard; record numeric values into 02-RUNBOOK.md table |
| **SEC-03+04 D-04b runtime UX (no mid-session re-signing)** | Phase 3 (SEC-03+04 atomic-flip shipped; runtime UX deferred) | Multi-tab + multi-day session — no re-sign prompts during 30-day sliding window | Per `03-RUNBOOK.md` §"Session-Cookie Smoke Recipe" — run for 30+ days |
| **CLAUDE.md surgical edit reads naturally** | Phase 4 (DRIFT-03) | Reviewer reads post-edit CLAUDE.md end-to-end; prose flows | Manual review |

### /gsd-verify-work Invocation
```bash
/gsd-verify-work --milestone stabilization --human-uat
```

### Milestone Exit Checklist
- [ ] All 30 milestone REQ-IDs marked Complete in REQUIREMENTS.md
- [ ] All 4 phases marked Complete in ROADMAP.md (Phase 1: 8/8, Phase 2: 8/8, Phase 3: 11/11, Phase 4: 10/10)
- [ ] STATE.md status updated to "Stabilization milestone closed"
- [ ] Production deploy of Phase 4 plans clean (DRIFT codemods + new test files; no behavioral code shipped, so risk is minimal)
- [ ] HUMAN-UAT items above run via /gsd-verify-work; outcomes recorded
- [ ] Retrospective written (per `.planning/RETROSPECTIVE.md` template) — solo dev + Claude

## Open Items / Deferred to Future Milestones
Carried verbatim from CONTEXT §"Deferred Ideas":
- Numeric line-coverage threshold in CI (rejected per D-07; revisit only if a future post-milestone phase needs a coarse regression signal)
- Full CLAUDE.md rewrite (rejected per D-05)
- External integration test environment (Tenderly / fork-as-a-service) — anvil is the chosen tool
- Future drift cleanups (DRIFT-04+) — opportunistic landings or a future milestone
- Multi-chain expansion / account abstraction / new features (out of scope per Phase 1 D-13 + PROJECT.md)
- Admin-page architectural refactor (admin/+page.svelte 2898 lines stays as-is)
- Per-RPC instrumentation granularity for REL-02 (Phase 4 deferred per Phase 3 hand-off)
- Wallet-address cookie permanent removal (Phase 3 ships downgrade to non-authoritative hint; permanent removal post-TEST-01 confirmation that no consumer reads it)

## Notes / Anomalies
Any deviations or findings discovered during Phase 4 execution that don't fit elsewhere.

---
*Phase 4 closed: <execution date>; 7/7 REQ-IDs (TEST-01..04, DRIFT-01..03)*
*Stabilization milestone closed: <execution date>; 30/30 REQ-IDs across 4 phases*
*Last verified: <execution date> by Plan 04-10*

**Word count target:** ~250–300 lines. Style mirrors 01-RUNBOOK.md, 02-RUNBOOK.md, 03-RUNBOOK.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md &amp;&amp; test "$(wc -l < .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md)" -ge 200 &amp;&amp; test "$(grep -c 'Foundry\|anvil' .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md)" -ge 1 &amp;&amp; test "$(grep -c 'Transcript-Capture\|transcript-capture\|OBS-03 Transcript' .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md)" -ge 1 &amp;&amp; test "$(grep -c 'Codemod Replay\|codemod replay\|migrate-token-find' .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md)" -ge 1 &amp;&amp; test "$(grep -c 'Phase-Exit Verification Log\|Phase-Exit' .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md)" -ge 1 &amp;&amp; test "$(grep -c 'Milestone\|milestone' .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md)" -ge 1 &amp;&amp; test "$(grep -c 'PERF-01' .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md)" -ge 1 &amp;&amp; test "$(grep -c 'SEC-03\|D-04b' .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - 04-RUNBOOK.md exists with ≥ 200 lines
    - All 7 named sections present: Pre-Deploy Env-Var Checklist, Foundry/Anvil CI Setup, OBS-03 Transcript-Capture, DRIFT-01 Codemod Replay, Phase-Exit Verification Log, Cross-cutting Cleanup Grep Recipe, Hand-off Milestone Close
    - Verification output from Tasks 1+2+3+4 included verbatim in §"Phase-Exit Verification Log"
    - HUMAN-UAT items named explicitly: PERF-01 p75 LCP, SEC-03+04 D-04b runtime UX
    - /gsd-verify-work invocation present
    - Milestone exit checklist present
    - Style mirrors 03-RUNBOOK.md
  </acceptance_criteria>
  <done>
    - 04-RUNBOOK.md authored
    - All Phase 4 manual-only verifications + HUMAN-UAT carry-forwards documented
    - Cross-cutting cleanup grep recipe runnable end-to-end
    - Milestone-close handoff explicit
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Update REQUIREMENTS.md — mark TEST-01..04 + DRIFT-01..03 Complete with closing-note synthesis + Traceability table close</name>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — find TEST-01..04 entries at lines 59–62, DRIFT-01..03 entries at lines 68–70, Traceability table at lines 126–132)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-01-SUMMARY.md through 04-09-SUMMARY.md (per-plan close synthesis to draw from)
    - .planning/REQUIREMENTS.md PERF-01 + SEC-* entries (style precedent for verbose closing notes; e.g. PERF-01 5-sentence summary at line 33)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
**For each of TEST-01, TEST-02, TEST-03, TEST-04, DRIFT-01, DRIFT-02, DRIFT-03 (7 IDs):**

Change the entry from `[ ] **<ID>**:` to `[x] **<ID>**:` and append a closing note in the same shape as PERF-01 and the SEC-*/REL-* entries:

```
- [x] **TEST-01**: ... existing description ... **[Completed 04-04, <date>: ... 3-5 sentence synthesis from 04-04-SUMMARY.md citing test files added (tests/hooks/{cors,csp,public-paths,admin-gate,wallet-session,bot-rejection}.test.ts), key invariants pinned (D-04 atomic-flip session cookie shape, CSP host allowlist Pitfall 1, bot-rejection ordering at hooks.server.ts:152-469), and any HUMAN-UAT carry-forward (none for TEST-01).]**
```

Repeat the structure for each REQ-ID:
- **TEST-01** → cite plan 04-04 + tests/hooks/ files + helpers
- **TEST-02** → cite plan 04-05 + tests/lib/admin/*.audit.test.ts + 5 audit-emission ADD endpoints (excluded-wallets, pool-wallets, team-wallets, snapshots/trigger, snapshots/regenerate)
- **TEST-03** → cite plans 04-06 + 04-07 + 04-08 + anvil-fork + ≥ 7 redacted replay JSON fixtures + Foundry CI install
- **TEST-04** → cite plan 04-09 + src/lib/server/snapshots/scraper.test.ts + 3 categories
- **DRIFT-01** → cite plan 04-03 + ts-morph codemod (scripts/codemods/migrate-token-find.ts) + ESLint no-restricted-syntax rule (eslint.config.js, mirroring TRADE-01 at lines 46-65) + lint fixture
- **DRIFT-02** → cite plan 04-02 + getPaymentTokensForNetwork / isPaymentToken helpers + admin/+page.svelte + api/admin/nansen/+server.ts edits
- **DRIFT-03** → cite plan 04-01 + CLAUDE.md surgical edit (struck multi-chain table + Account Abstraction section + Project Structure account-abstraction/ + Tech Stack Rhinestone) + Ground Truth header + grep gate (D-05b)

**Update the Traceability table (lines 126–132)** — change "Pending" to "Complete (04-NN, <date>)" for each of the 7 IDs:

```
| TEST-01 | Phase 4 | Complete (04-04, <date>) |
| TEST-02 | Phase 4 | Complete (04-05, <date>) |
| TEST-03 | Phase 4 | Complete (04-06+04-07+04-08, <date>) |
| TEST-04 | Phase 4 (conditional on DEPR-02 — confirmed in scope) | Complete (04-09, <date>) |
| DRIFT-01 | Phase 4 | Complete (04-03, <date>) |
| DRIFT-02 | Phase 4 | Complete (04-02, <date>) |
| DRIFT-03 | Phase 4 | Complete (04-01, <date>) |
```

**Update the trailing summary line (line 145):**
```
*Last updated: <date> — Phase 4 closed (Plan 04-10): all 7 TEST-01..04 + DRIFT-01..03 marked Complete; runbook + phase-exit grep gates green; Phase 2 + Phase 3 carry-forward invariants preserved; **stabilization milestone closed (30/30 REQ-IDs across 4 phases)**.*
```

**Search/replace targets (concrete edits):**
- Line 59 `- [ ] **TEST-01**` → `- [x] **TEST-01**` + append closing note
- Line 60 `- [ ] **TEST-02**` → `- [x] **TEST-02**` + append closing note
- Line 61 `- [ ] **TEST-03**` → `- [x] **TEST-03**` + append closing note
- Line 62 `- [ ] **TEST-04**` → `- [x] **TEST-04**` + append closing note
- Line 68 `- [ ] **DRIFT-01**` → `- [x] **DRIFT-01**` + append closing note
- Line 69 `- [ ] **DRIFT-02**` → `- [x] **DRIFT-02**` + append closing note
- Line 70 `- [ ] **DRIFT-03**` → `- [x] **DRIFT-03**` + append closing note
- Lines 126–132 Traceability rows: replace "Pending" with "Complete (04-NN, <date>)"
- Line 145 trailing summary: replace with milestone-close text above

After edits: zero `Pending` rows remain in the Traceability table.
  </action>
  <verify>
    <automated>test "$(grep -c '^\- \[x\] \*\*TEST-01\*\*\|^\- \[x\] \*\*TEST-02\*\*\|^\- \[x\] \*\*TEST-03\*\*\|^\- \[x\] \*\*TEST-04\*\*' .planning/REQUIREMENTS.md)" -ge 4 &amp;&amp; test "$(grep -c '^\- \[x\] \*\*DRIFT-01\*\*\|^\- \[x\] \*\*DRIFT-02\*\*\|^\- \[x\] \*\*DRIFT-03\*\*' .planning/REQUIREMENTS.md)" -ge 3 &amp;&amp; test "$(grep -c '| Pending |' .planning/REQUIREMENTS.md)" -eq 0 &amp;&amp; test "$(grep -c 'milestone closed\|Stabilization milestone' .planning/REQUIREMENTS.md)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - All 4 TEST-* IDs marked `[x]` with closing notes citing plan + date + key artifacts
    - All 3 DRIFT-* IDs marked `[x]` with closing notes
    - Traceability table updated for all 7 IDs (zero "Pending" rows remain for the active milestone)
    - Trailing summary line reflects Phase 4 close + milestone close
  </acceptance_criteria>
  <done>
    - REQUIREMENTS.md reflects Phase 4 close
    - All 7 phase REQ-IDs marked Complete
    - Milestone-close summary present
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Update ROADMAP.md — flip Phase 4 to Complete + update progress table to 10/10</name>
  <read_first>
    - .planning/ROADMAP.md (full file — Phase 4 entry at line 18 + Phase 4 detailed section at lines 177–192 + per-phase progress table at lines 199–204)
    - Plan list within Phase 4 detailed section (line 187 `**Plans**: TBD` — must be replaced with the 10 plan IDs)
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
**Edit 1 — top-level checklist (line 18):**
```
- [ ] **Phase 4: Boundary Tests & Drift Cleanup** ...
```
→
```
- [x] **Phase 4: Boundary Tests & Drift Cleanup** ...
```

**Edit 2 — Phase 4 detailed section (line 187 `**Plans**: TBD`):**

Replace `**Plans**: TBD` with the explicit plan list:
```
**Plans**: 10 plans
- [x] 04-01-PLAN.md — DRIFT-03: CLAUDE.md surgical edit + Ground Truth header
- [x] 04-02-PLAN.md — DRIFT-02: USDC hardcoding → getPaymentTokensForNetwork / isPaymentToken in admin paths
- [x] 04-03-PLAN.md — DRIFT-01: ts-morph codemod + ESLint no-restricted-syntax rule banning TOKENS.find / ALL_TOKENS.find outside allowlist
- [x] 04-04-PLAN.md — TEST-01: tests/hooks/{cors,csp,public-paths,admin-gate,wallet-session,bot-rejection}.test.ts + _helpers.ts factories
- [x] 04-05-PLAN.md — TEST-02: tests/lib/admin/*.audit.test.ts (8 endpoints) + createAuditLogger ADD on 5 missing endpoints
- [x] 04-06-PLAN.md — TEST-03 anvil scaffold: tests/helpers/anvil.ts + loadTranscript.ts + vitest.integration.config.ts + Foundry CI install step
- [x] 04-07-PLAN.md — TEST-03 anvil suite: tests/integration/marketOrder/anvil-fork.test.ts
- [x] 04-08-PLAN.md — TEST-03 replay suite: tests/integration/marketOrder/replay-*.test.ts + ≥ 7 redacted JSON fixtures under tests/fixtures/marketOrder/
- [x] 04-09-PLAN.md — TEST-04: src/lib/server/snapshots/scraper.test.ts (pagination + wrappedTokenTransfers fallback + transient subgraph failure)
- [x] 04-10-PLAN.md — Phase-exit grep gates + 04-RUNBOOK.md (Foundry/anvil CI + OBS-03 transcript-capture + DRIFT-01 codemod replay + milestone close handoff) — depends_on: all 9 prior plans (<execution date>)
```

**Edit 3 — per-phase progress table (line 204):**
```
| 4. Boundary Tests & Drift Cleanup | 0/TBD | Not started | - |
```
→
```
| 4. Boundary Tests & Drift Cleanup | 10/10 | Complete | <execution date> |
```

**Edit 4 — append milestone-close note immediately after the progress table (after line 204):**
```
**Stabilization milestone closed: <execution date>** — 30/30 REQ-IDs across 4 phases (Phase 1: 8/8 DEPR-* + OBS-*; Phase 2: 5/5 TRADE-* + PERF-*; Phase 3: 10/10 SEC-* + REL-*; Phase 4: 7/7 TEST-* + DRIFT-*). HUMAN-UAT carry-forwards (PERF-01 p75 LCP, SEC-03+04 D-04b runtime UX) deferred to /gsd-verify-work post-deploy per 04-RUNBOOK.md §"Hand-off — Milestone Close".
```

After edits: ROADMAP.md shows all 4 phases as Complete; the milestone-close note is present.
  </action>
  <verify>
    <automated>test "$(grep -c '^- \[x\] \*\*Phase 4:' .planning/ROADMAP.md)" -ge 1 &amp;&amp; test "$(grep -c '04-10-PLAN.md' .planning/ROADMAP.md)" -ge 1 &amp;&amp; test "$(grep -cE '\| 4\..*\| 10/10 \| Complete' .planning/ROADMAP.md)" -ge 1 &amp;&amp; test "$(grep -c 'Stabilization milestone closed\|stabilization milestone closed\|milestone closed' .planning/ROADMAP.md)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - Phase 4 top-level checklist marked `[x]`
    - `**Plans**: TBD` replaced with explicit 10-plan list (all `[x]`)
    - Per-phase progress table row updated to "10/10 | Complete | <date>"
    - Milestone-close note appended after progress table
  </acceptance_criteria>
  <done>
    - ROADMAP.md reflects Phase 4 close + milestone close
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Author 04-10-SUMMARY.md per gsd summary template + commit sequence</name>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (summary template)
    - .planning/phases/phase-03-production-grade-hardening/03-11-SUMMARY.md (most recent precedent)
    - .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md (just authored — for verification log reference)
  </read_first>
  <files>.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-10-SUMMARY.md</files>
  <action>
Author `.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-10-SUMMARY.md` per the gsd summary template + 03-11-SUMMARY.md precedent. Required content:

- **Frontmatter:** `phase: 04`, `plan: 10`, `date: <execution date>`, `requirements_verified: [TEST-01, TEST-02, TEST-03, TEST-04, DRIFT-01, DRIFT-02, DRIFT-03]`
- **Summary:** one-paragraph synthesis — Phase 4 phase-exit re-verified all 7 REQ-IDs + Phase 2 + Phase 3 carry-forward gates; 04-RUNBOOK.md authored; REQUIREMENTS.md + ROADMAP.md updated to close Phase 4 and the stabilization milestone
- **Phase-exit grep verification log:** verbatim output captured by Tasks 1–4
- **04-RUNBOOK.md sections delivered:** enumerated checklist
- **REQUIREMENTS.md / ROADMAP.md update diffs:** before/after line snippets for the 7 REQ-IDs + Traceability + progress table
- **Phase 4 close metrics:** total plans (10), total commits (TBD at execution time; reference Wave 1–6 commit sequence), total context spent
- **Milestone-close handoff line:** explicit list of HUMAN-UAT carry-forwards by name (PERF-01 numeric p75 LCP < 2.5s, SEC-03+04 D-04b runtime UX); /gsd-verify-work invocation
- **Final cross-cutting gate snapshot:** TRADE-01 lockdown, TRADE-02 cycle severance, failWith ≥ 12, EMERGENCY_RATIO_MULTIPLIER = 0, svelte-check baseline ≤ 3, staleTime: Infinity preserved, no Alchemy hardcoding, no Math.random in security paths, no fallback secrets, session-cookie shape preserved, all SEC-01..07 + REL-01..03 grep gates green

**Commit sequence (atomic, mirroring 03-11 precedent):**

```bash
# Commit 1 — phase-exit verification + RUNBOOK
git add .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-RUNBOOK.md
git commit -m "docs(04-10): phase-exit verification + 04-RUNBOOK.md"

# Commit 2 — close Phase 4 + milestone
git add .planning/REQUIREMENTS.md .planning/ROADMAP.md \
        .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-10-SUMMARY.md
git commit -m "docs(04-10): close Phase 4 — 7/7 TEST + DRIFT REQ-IDs and milestone exit"
```

Both commits must leave svelte-check ≤ 3 errors + tests green (Phase 2 atomic-commits-with-svelte-check-green discipline; verified by Task 4).
  </action>
  <verify>
    <automated>test -f .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-10-SUMMARY.md &amp;&amp; test "$(grep -c 'requirements_verified\|TEST-01\|DRIFT-03' .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-10-SUMMARY.md)" -ge 3 &amp;&amp; test "$(grep -c 'PERF-01\|SEC-03\|D-04b\|HUMAN-UAT' .planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-10-SUMMARY.md)" -ge 2 &amp;&amp; git log --oneline -5 | grep -c '04-10' | xargs test 1 -le</automated>
  </verify>
  <acceptance_criteria>
    - 04-10-SUMMARY.md exists with all required sections
    - Phase-exit grep verification log present verbatim
    - Milestone-close handoff line present with HUMAN-UAT items by name
    - Two atomic commits land cleanly with descriptive messages
    - svelte-check + tests still green after each commit
  </acceptance_criteria>
  <done>
    - 04-10-SUMMARY.md authored
    - Both commits land cleanly
    - Phase 4 + milestone closed in git history
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Phase 4 + milestone close hand-off — confirm before /gsd-verify-work</name>
  <what-built>
    Phase 4 closed: 7 REQ-IDs (TEST-01..04, DRIFT-01..03) shipped across plans 04-01..04-09; phase-exit grep gates re-verified (Tasks 1–3); cross-cutting build/test gates green (Task 4); 04-RUNBOOK.md authored (Task 5); REQUIREMENTS.md + ROADMAP.md + 04-10-SUMMARY.md updated to mark Phase 4 + the stabilization milestone closed (Tasks 6–8).
  </what-built>
  <how-to-verify>
    Confirm:
    1. **All 4 phases marked Complete in ROADMAP.md** (Phase 1: 8/8, Phase 2: 8/8, Phase 3: 11/11, Phase 4: 10/10).
    2. **All 30 milestone REQ-IDs marked Complete in REQUIREMENTS.md Traceability table** — zero "Pending" rows remain.
    3. **Phase-exit verification log in 04-RUNBOOK.md** is complete and shows all gates passing with timestamps.
    4. **HUMAN-UAT carry-forwards understood:** operator commits to running `/gsd-verify-work --milestone stabilization --human-uat` against PERF-01 p75 LCP and SEC-03+04 D-04b runtime UX after the 24h+ post-deploy window.
    5. **Production deploy of Phase 4** completed cleanly (or operator decision to defer is documented in 04-RUNBOOK.md "Notes / Anomalies"). Phase 4 ships codemods + tests + docs only — no behavioral code change — so deploy risk is minimal.
    6. **Retrospective:** operator either authors the milestone retrospective at `.planning/RETROSPECTIVE.md` OR explicitly defers it to a follow-up session.
  </how-to-verify>
  <resume-signal>
    Type "approved" to close Phase 4 and the stabilization milestone. If any post-deploy issue surfaces (HUMAN-UAT regression, integration test flakiness, codemod side-effect), describe specifics and a milestone-follow-up wave will be drafted.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production deploy | Phase 4 ships codemods + tests + docs only — zero behavioral code change. Risk is regression in tooling (ESLint rule, codemod idempotency) or test flakiness (anvil fork). |
| Operator manual milestone close | The bisect line for "milestone closed prematurely while a Phase 2/3 invariant regressed" — Tasks 1–3 grep gates are the mechanical guard against this. |

## STRIDE Threat Register

No new attack surface — phase-exit verification + documentation only. Re-verifies all Phase 3 SEC-* threat model mitigations remain in place via grep gates.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-PHASE-EXIT-01 | Tampering | Phase 4 plans landed but a regression slipped past per-plan acceptance criteria | mitigate | Tasks 1–3 grep gates + Task 4 build/test gates; if any fails, do not close phase |
| T-04-PHASE-EXIT-02 | Repudiation | 04-RUNBOOK.md incomplete → future operators can't reproduce Foundry CI / transcript-capture / codemod replay | mitigate (BLOCKER) | Task 5 RUNBOOK acceptance: ≥ 200 lines + all 7 named sections present |
| T-04-PHASE-EXIT-03 | Tampering | Phase 2 or Phase 3 invariant regressed during Phase 4 wave execution and went undetected | mitigate | Tasks 2–3 re-run the Phase 2 + Phase 3 gates verbatim; halt on any failure |
| T-04-PHASE-EXIT-04 | Information Disclosure | Un-redacted wallet addresses in TEST-03 fixtures committed to git | mitigate | Task 1 grep gate `grep -RhE '0x[a-fA-F0-9]{40}' tests/fixtures/marketOrder/` returns 0 modulo allowlist; codified in 04-RUNBOOK.md transcript-capture procedure |
| T-04-PHASE-EXIT-05 | Repudiation | HUMAN-UAT carry-forward items (PERF-01 p75 LCP, SEC-03+04 D-04b) silently dropped at milestone close | mitigate | Task 5 + Task 8 + Task 9 explicitly enumerate them; /gsd-verify-work invocation included in 04-RUNBOOK.md milestone-close handoff |
| T-04-PHASE-EXIT-06 | Denial of Service | Anvil fork in CI fails (RPC archive-node access lost; A1 risk) → integration test job blocks all PRs | accept (mitigated by RUNBOOK) | 04-RUNBOOK.md §"Foundry / Anvil CI Setup" documents A1 risk + remediation (switch to archive-capable provider); not Phase 4 work to pre-emptively switch |
</threat_model>

<verification>
**Phase 4 phase-exit (Tasks 1–3 grep gates):** All gates pass — TEST-01..04 + DRIFT-01..03 evidence + Phase 2 carry-forward + Phase 3 SEC-01..07 + REL-01..03 carry-forward (full grep set from RESEARCH lines 600–706 + 03-VALIDATION.md §"Phase-Exit Verification").

**Cross-cutting build/test gates (Task 4):**
- `npm test -- --run` exits 0
- `npm run test:integration` exits 0 (anvil fork green)
- `npm run check` ≤ 3 errors (Phase 2 baseline)
- `npm run lint` exits 0
- `npm run build` exits 0

**Documentation (Tasks 5–8):**
- 04-RUNBOOK.md ≥ 200 lines with all 7 named sections (header, Phase 4 summary, env-var checklist, Foundry/anvil CI setup, OBS-03 transcript-capture, DRIFT-01 codemod replay, phase-exit verification log, cross-cutting cleanup grep recipe, milestone-close handoff with HUMAN-UAT carry-forwards by name)
- REQUIREMENTS.md: 7 REQ-IDs marked `[x]` Complete with closing-note synthesis; Traceability table updated; trailing summary reflects milestone close
- ROADMAP.md: Phase 4 marked `[x]` Complete; per-phase progress table updated to 10/10 Complete; milestone-close note appended
- 04-10-SUMMARY.md authored per template

**Manual hand-off (Task 9):**
- All 4 phases Complete in ROADMAP
- All 30 REQ-IDs Complete in REQUIREMENTS Traceability
- HUMAN-UAT carry-forwards understood + /gsd-verify-work commitment captured
- Production deploy clean (or deferred + documented)
</verification>

<success_criteria>
- All Phase 4 phase-exit grep gates pass
- All Phase 2 + Phase 3 carry-forward gates remain green
- All build/test gates green (npm test, test:integration, check, lint, build)
- 04-RUNBOOK.md authored with 7 named sections
- REQUIREMENTS.md, ROADMAP.md, 04-10-SUMMARY.md updated to mark Phase 4 + milestone closed
- Two atomic commits land cleanly
- Manual hand-off checkpoint approved
- Stabilization milestone exit ready (HUMAN-UAT via /gsd-verify-work)
</success_criteria>

<output>
After completion (including manual hand-off approval), `.planning/phases/phase-04-boundary-tests-and-drift-cleanup/04-10-SUMMARY.md` is in place documenting:
- Phase-exit grep verification log (verbatim from Tasks 1–4)
- 04-RUNBOOK.md sections delivered
- REQUIREMENTS.md / ROADMAP.md update diffs
- Manual hand-off outcome
- Phase 4 close metrics (10 plans, commit count, context spent)
- Milestone-close handoff line listing HUMAN-UAT carry-forwards by name (PERF-01 p75 LCP, SEC-03+04 D-04b runtime UX)
- Final cross-cutting gate snapshot (Phase 2 + Phase 3 invariants green)
</output>
