---
phase: 01-shrink-the-surface-see-what-s-happening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/admin/rewards/+page.svelte
  - src/routes/admin/+layout.svelte
  - src/routes/api/admin/rewards-pool/+server.ts
  - src/routes/api/admin/snapshots/recalculate/+server.ts
  - src/routes/api/admin/snapshots/regenerate/+server.ts
  - src/routes/api/admin/snapshots/trigger/+server.ts
  - src/routes/api/snapshots/preview/+server.ts
  - src/routes/api/snapshots/preview-stream/+server.ts
  - src/routes/api/snapshots/points/+server.ts
  - src/routes/api/cron/snapshots/+server.ts
  - src/lib/server/snapshots/points.ts
  - src/lib/server/snapshots/processor.ts
  - src/lib/server/snapshots/index.ts
  - src/lib/server/rewards/rewardsCommon.ts
  - src/lib/server/cache.ts
  - src/lib/server/kv.ts
  - src/lib/config/networks.ts
  - .env.example
autonomous: true
requirements:
  - DEPR-02

must_haves:
  truths:
    - "Internal admin TVL + total volume + per-token TVL + per-token volume views still load after deletion"
    - "Vercel cron at /api/cron/snapshots still runs daily and writes snapshot blobs (without per-wallet points data)"
    - "Per-wallet monthly points calculation is removed from the snapshot pipeline (no `updateMonthlyPoints` call survives)"
    - "LP_SUBGRAPH_URL is removed from .env.example and from any code consumers"
    - "`getRewardsExcludedWalletsSet()` and the orderbook excluded-wallet logic remain intact (consumed by surviving processor.ts and generator.ts for non-rewards reasons)"
    - "Nansen integration is RETAINED per D-02: `src/lib/server/nansenTiers.ts`, `/api/nansen/tiers`, `/api/admin/nansen` all survive DEPR-02 (admin uses Nansen tiers in the activity tab, not just for rewards weighting). CSP entry stays. No deletion task in this plan touches Nansen surfaces — guard against accidental deletion via grep `! grep -L 'nansen' src/lib/server/nansenTiers.ts` (file still exists)."
  artifacts:
    - path: "src/routes/admin/rewards/+page.svelte"
      provides: "DELETED — 4933-line admin rewards UI gone"
    - path: "src/lib/server/snapshots/points.ts"
      provides: "DELETED — per-wallet monthly points calc gone"
    - path: "src/lib/server/rewards/rewardsCommon.ts"
      provides: "DELETED — rewards data-fetch helpers gone"
    - path: "src/routes/api/snapshots/points/+server.ts"
      provides: "DELETED — public points API gone"
    - path: "src/routes/api/admin/rewards-pool/+server.ts"
      provides: "DELETED — rewards pool config endpoint gone"
    - path: "src/routes/api/cron/snapshots/+server.ts"
      provides: "MODIFIED — no longer calls updateMonthlyPoints; cron continues to write TVL/volume aggregates"
    - path: "src/lib/server/snapshots/processor.ts"
      provides: "MODIFIED — no per-wallet points step; comment documents legacy blob field tolerance per D-04"
    - path: "src/lib/server/snapshots/index.ts"
      provides: "MODIFIED — no longer re-exports from './points'"
    - path: ".env.example"
      provides: "MODIFIED — LP_SUBGRAPH_URL removed (D-05)"
  key_links:
    - from: "src/routes/api/cron/snapshots/+server.ts"
      to: "snapshot generator + blob writer"
      via: "no updateMonthlyPoints call between them"
      pattern: "(?s)await generateAllTokenSnapshots[^}]*await writeSnapshotsToBlobStorage"
    - from: "src/lib/server/snapshots/processor.ts"
      to: "src/lib/server/kv.ts"
      via: "getRewardsExcludedWalletsSet (KEPT — orderbook contract addresses)"
      pattern: "getRewardsExcludedWalletsSet\\(\\)"
---

<objective>
Surgically prune the rewards layer per CONTEXT D-01 (delete rewards layer, keep snapshot pipeline) while retaining the TVL + total trade volume + per-token TVL + per-token volume aggregates that admin views consume. Per D-03, delete the per-wallet monthly points calculation entirely from `processor.ts` + sibling `points.ts`. Per D-05, delete `LP_SUBGRAPH_URL` wiring (rewards-only subgraph slug `st0x-rewards-base/1.0.23`). Per D-04, leave existing Vercel Blob snapshots with legacy points fields as-is — new blobs use pruned schema.

Purpose: Eliminate 4933+ lines of admin rewards UI bug surface and the per-wallet points pipeline that no surviving feature reads. Keep TVL/volume views — they're internally used. Prerequisite for OBS-04 (Plan 01-06) because OBS-04 instruments `cron/snapshots/+server.ts:callRpc` and we need the cron stripped of the dead `updateMonthlyPoints` call before instrumenting it (per RESEARCH §"Sequencing").

Output: ~5 deleted files (admin/rewards page, points.ts, rewardsCommon.ts, points API route, rewards-pool admin endpoint), 2 modified preview routes (drop points step), modified cron (drop updateMonthlyPoints), modified processor (drop points step), modified snapshots/index.ts barrel, modified .env.example (LP_SUBGRAPH_URL gone), modified networks.ts (drop subgraph_lp_url field if present), modified cache.ts (drop invalidateRewardsCaches if no surviving caller).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-01-shrink-the-surface-see-what-s-happening/01-CONTEXT.md
@.planning/phase-01-shrink-the-surface-see-what-s-happening/01-RESEARCH.md
@.planning/phase-01-shrink-the-surface-see-what-s-happening/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@src/lib/server/snapshots/processor.ts
@src/lib/server/snapshots/points.ts
@src/lib/server/snapshots/index.ts
@src/routes/api/cron/snapshots/+server.ts
@src/routes/api/snapshots/preview/+server.ts
@src/routes/api/snapshots/preview-stream/+server.ts
@src/lib/server/cache.ts
@src/lib/server/kv.ts
@src/lib/server/rewards/rewardsCommon.ts
@src/lib/server/referrals.ts

<interfaces>
<!-- Surviving consumers of `MonthlyPointsData` / `RewardsPoolConfig` types (from kv.ts) -->
<!-- IMPORTANT: src/lib/server/referrals.ts (KEPT per D-14) reads MonthlyPointsData and RewardsPoolConfig
     from KV in `calculateReferralPerformance` (referrals.ts:298-395). The function gracefully
     handles the no-data case (`if (!monthlyData) return zeros`). After D-03 removes the writer
     (updateMonthlyPoints), this reader will see stale/no data — referral-performance becomes a no-op.
     This is EXPECTED (rewards layer deletion). DO NOT delete the type definitions in kv.ts
     (`MonthlyPointsData` interface, `RewardsPoolConfig` interface, `KV_KEYS.monthlyPoints`,
     `KV_KEYS.monthlyPointsList`, `KV_KEYS.rewardsPool`, `KV_KEYS.rewardsPoolList`) — referrals.ts
     still imports them. ADD a comment in kv.ts explaining the orphan tolerance per D-04. -->

From src/lib/server/kv.ts (KEEP):
```typescript
export interface MonthlyPointsData { /* used by referrals.ts; legacy data tolerance per D-04 */ }
export interface RewardsPoolConfig { /* used by referrals.ts */ }
export const KV_KEYS = {
  monthlyPoints: (month: string) => `snapshots:points:${month}`, // KEEP — referrals.ts reads
  rewardsPool: (month: string) => `rewards:pool:${month}`,        // KEEP — referrals.ts reads
  rewardsExcludedWallets: () => '...',                              // KEEP — orderbook excluded-wallets, non-rewards
  // ...
};
export async function getRewardsExcludedWalletsSet(): Promise<Set<string>>; // KEEP — orderbook reasons
```

From src/lib/server/snapshots/points.ts (DELETE entire file):
```typescript
// All exports go: updateMonthlyPoints, calculateWalletPointsFromSnapshots,
// calculateWalletPointsFromSnapshotsWithProgress, getMonthlyPoints, createEmptyMonthlyData
```

From src/lib/server/cache.ts (INSPECT then DELETE if no surviving caller):
```typescript
export async function invalidateRewardsCaches(): Promise<void>;     // DELETE if only callers were the cron + admin/rewards
export async function invalidatePublicApiCaches(): Promise<void>;   // INSPECT — invalidates rewardsApy + rocketboost + allWalletData (all rewards-only per RESEARCH); DELETE
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete admin rewards UI + admin rewards-pool endpoint + admin nav link</name>
  <files>src/routes/admin/rewards/+page.svelte, src/routes/admin/+layout.svelte, src/routes/api/admin/rewards-pool/+server.ts</files>
  <read_first>
    - src/routes/admin/rewards/+page.svelte (current 4933-line file — confirm only one consumer; no exports needed)
    - src/routes/admin/+layout.svelte (current — locate the rewards nav entry at line 10: `{ href: '/admin/rewards', label: 'Rewards' }`)
    - src/routes/api/admin/rewards-pool/+server.ts (current — verify it's standalone)
    - .planning/phase-01-shrink-the-surface-see-what-s-happening/01-PATTERNS.md §"DELETE: rewards UI files + rewards APIs + admin rewards + Onramper" (planner-checklist row "Admin rewards (DEPR-02)")
    - .planning/phase-01-shrink-the-surface-see-what-s-happening/01-RESEARCH.md §"Deletion Graph — DEPR-02"
    - .planning/phase-01-shrink-the-surface-see-what-s-happening/01-UI-SPEC.md §"Non-blocking Recommendations for Planner" item 1 (default tab safety)
  </read_first>
  <action>
    Implementing D-01 + DEPR-02 (admin rewards prune):

    1. **Delete** `src/routes/admin/rewards/+page.svelte` entirely (`rm` the whole file — directory becomes empty; remove the `src/routes/admin/rewards/` directory if empty after delete).
    2. **Delete** `src/routes/api/admin/rewards-pool/+server.ts` entirely. Remove the `src/routes/api/admin/rewards-pool/` directory.
    3. **Edit** `src/routes/admin/+layout.svelte`: remove the line `{ href: '/admin/rewards', label: 'Rewards' }` (line ~10) from the nav-items array.
    4. Per UI-SPEC non-blocking rec #1: confirm by inspection that the admin landing's default tab in `src/routes/admin/+page.svelte` is `activity` and remains so. **Do not modify `admin/+page.svelte`** (out of scope per D-13). Just visually verify the default tab string is `'activity'` unchanged. If it was `'rewards'` for some reason, switch it to `'activity'` — but RESEARCH §"Deletion Graph DEPR-02" already confirmed the rewards section does NOT live in `admin/+page.svelte`, so this is just a sanity check.
    5. Per UI-SPEC + CONVENTIONS.md "Naming Patterns": no naming changes needed in surviving files.

    All edits are pure deletions of dead code. No new imports. No type changes.
  </action>
  <verify>
    <automated>! test -f src/routes/admin/rewards/+page.svelte && ! test -f src/routes/api/admin/rewards-pool/+server.ts && ! grep -q "/admin/rewards" src/routes/admin/+layout.svelte && npm run check 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f src/routes/admin/rewards/+page.svelte` (file deleted)
    - `! test -f src/routes/api/admin/rewards-pool/+server.ts` (file deleted)
    - `! grep -q "/admin/rewards\|label: 'Rewards'" src/routes/admin/+layout.svelte` (nav link removed)
    - `npm run check` exits 0 (no broken imports)
    - `grep -c "default.*activity\|currentTab.*activity" src/routes/admin/+page.svelte` returns ≥1 (default tab still activity — sanity check, do not modify the file)
  </acceptance_criteria>
  <done>Admin rewards page + rewards-pool endpoint + admin nav link to /admin/rewards are gone. Admin landing's default tab remains `activity`. svelte-check exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Delete points pipeline (points.ts + points API + rewardsCommon.ts) and unwire from preview routes + cron + processor</name>
  <files>src/lib/server/snapshots/points.ts, src/lib/server/rewards/rewardsCommon.ts, src/routes/api/snapshots/points/+server.ts, src/lib/server/snapshots/index.ts, src/routes/api/cron/snapshots/+server.ts, src/routes/api/snapshots/preview/+server.ts, src/routes/api/snapshots/preview-stream/+server.ts, src/lib/server/snapshots/processor.ts, src/routes/api/admin/snapshots/recalculate/+server.ts, src/routes/api/admin/snapshots/regenerate/+server.ts, src/routes/api/admin/snapshots/trigger/+server.ts, src/lib/server/cache.ts, src/lib/server/kv.ts</files>
  <read_first>
    - src/lib/server/snapshots/points.ts (the file being deleted — confirm exports: `updateMonthlyPoints`, `calculateWalletPointsFromSnapshots`, `calculateWalletPointsFromSnapshotsWithProgress`, `getMonthlyPoints`, `createEmptyMonthlyData`)
    - src/lib/server/rewards/rewardsCommon.ts (the file being deleted — confirm exports: `RewardsData`, `fetchRewardsData`, `calculateTotalPoints`, `calculateRocketBoostAmount`)
    - src/lib/server/snapshots/index.ts (barrel — line 6 `export * from './points'` to remove)
    - src/routes/api/cron/snapshots/+server.ts (lines 13, 22, 106, 143 — points + cache imports/calls)
    - src/routes/api/snapshots/preview/+server.ts (line 11 — `calculateWalletPointsFromSnapshots` import; line 65 — call site; lines 62-95 — points step in response)
    - src/routes/api/snapshots/preview-stream/+server.ts (line 9 — `calculateWalletPointsFromSnapshotsWithProgress` import; line 85 — call site; lines 78-135 — points step)
    - src/lib/server/snapshots/processor.ts (find the per-wallet points calc step inside `processBalances` or sibling — grep for any call into `points.ts` exports OR points-shaped writes; **keep** `getRewardsExcludedWalletsSet()` consumption)
    - src/lib/server/cache.ts (`invalidateRewardsCaches` at line 63; `invalidatePublicApiCaches` at line 51)
    - src/lib/server/kv.ts (line 79-80, 88-89, 133+ — KV_KEYS.monthlyPoints, monthlyPointsList, rewardsPool, rewardsPoolList; type `MonthlyPointsData`; `getRewardsExcludedWalletsSet`)
    - src/routes/api/admin/snapshots/recalculate/+server.ts (line 10 — `MonthlyPointsData` import; line 63 — usage)
    - src/lib/server/referrals.ts (KEPT — line 9 imports `MonthlyPointsData`, line 321 reads from KV — DO NOT BREAK THIS)
    - .planning/phase-01-shrink-the-surface-see-what-s-happening/01-PATTERNS.md §"MODIFY: src/lib/server/snapshots/processor.ts" + §"MODIFY: src/routes/api/cron/snapshots/+server.ts"
    - .planning/phase-01-shrink-the-surface-see-what-s-happening/01-RESEARCH.md §"Deletion Graph — DEPR-02" rows for points.ts, rewardsCommon.ts, cron, preview, processor; §"Common Pitfalls — Pitfall 5" (run grep audit before deleting)
  </read_first>
  <action>
    Implementing D-03 (delete per-wallet monthly points calc) + D-04 (legacy blob field tolerance) + the rewards-points sub-prune of DEPR-02. Step-by-step:

    1. **Pre-deletion grep audit (Pitfall 5):** run these greps and capture output. They must return only the files being deleted/modified in this task — any other hit means a missed dependency:
       ```bash
       grep -rn "from '\$lib/server/snapshots/points'" src/
       grep -rn "from '\$lib/server/rewards/rewardsCommon'" src/
       grep -rn "MonthlyPointsData" src/  # surviving hits MUST be referrals.ts only (line 9, 321) + kv.ts (line 133 — type def)
       grep -rn "RewardsPoolConfig" src/  # surviving hits MUST be referrals.ts only + kv.ts (type def)
       grep -rn "calculateWalletPointsFromSnapshots\|updateMonthlyPoints\|invalidateRewardsCaches\|invalidatePublicApiCaches" src/
       ```
       If any unexpected hit (admin endpoint, route handler, etc.), STOP and update the plan.

    2. **Delete files entirely:**
       - `rm src/lib/server/snapshots/points.ts`
       - `rm src/lib/server/rewards/rewardsCommon.ts`
       - `rm src/routes/api/snapshots/points/+server.ts` (and remove the directory)
       - `rmdir src/lib/server/rewards/` if empty (the directory becomes orphaned after rewardsCommon.ts goes)

    3. **Edit `src/lib/server/snapshots/index.ts`:** remove the line `export * from './points';` (line 6). Surviving exports are types, scraper, processor, pyth, vaults.

    4. **Edit `src/routes/api/cron/snapshots/+server.ts`:**
       - Remove import line 13: `import { updateMonthlyPoints } from '$lib/server/snapshots/points';`
       - Remove import line 22: `import { invalidateRewardsCaches } from '$lib/server/cache';`
       - Remove the call at line 106: `await updateMonthlyPoints(snapshots, blockNumber, timestamp);`
       - Remove the call at line 143: `await invalidateRewardsCaches();`
       - **KEEP everything else** — including the surviving cron logic (CRON_SECRET fail-closed at lines 38-52, snapshot generation, blob writes, KV updates for `snapshotBlocks`).
       - Add a single comment above the deleted lines block: `// per-wallet points calculation removed in Phase 1 (DEPR-02 D-03); cron now writes only TVL + volume aggregates`

    5. **Edit `src/routes/api/snapshots/preview/+server.ts`:**
       - Remove import at line 11: `import { calculateWalletPointsFromSnapshots } from '$lib/server/snapshots/points';`
       - Remove the points-calc step (lines 62-65 — Step 5/6 console log + the `calculateWalletPointsFromSnapshots` call assigning to `walletPointsMap`).
       - Remove the `points: tokenData.points` field at line 95 from the response payload.
       - Remove the comment at line 2 `// Uses the same core calculation functions as points.ts for consistency`.
       - Renumber any "Step N/M" log strings that were sequential (e.g., previously "Step 5/6" → now last step becomes "Step 4/5").

    6. **Edit `src/routes/api/snapshots/preview-stream/+server.ts`:**
       - Remove import at line 9: `import { calculateWalletPointsFromSnapshotsWithProgress } from '$lib/server/snapshots/points';`
       - Remove the SSE points step (lines 78-85 — "Calculating points..." status emit + `calculateWalletPointsFromSnapshotsWithProgress` call).
       - Remove the `points: tokenData.points` field at line 135 from the SSE payload shape.

    7. **Edit `src/lib/server/snapshots/processor.ts`:**
       - Find and delete the per-wallet monthly points calculation step (it's the call site that produces points-per-wallet output; grep within the file for any reference to `points` or computeshape). Confirm with `grep -n "points" src/lib/server/snapshots/processor.ts`.
       - **KEEP** `getRewardsExcludedWalletsSet()` consumption at line ~118 — it's used for orderbook contract addresses (non-rewards).
       - Add a code comment near the top of `processBalances` (or wherever the points step was): `// Per-wallet points calc deleted in Phase 1 (D-03). Existing Vercel Blob snapshots may contain legacy points/rewards fields — readers ignore them per D-04 (no backfill, no wipe).`

    8. **Edit `src/routes/api/admin/snapshots/recalculate/+server.ts`, `regenerate/+server.ts`, `trigger/+server.ts`:**
       - Inspect each file. If it imports from `$lib/server/snapshots/points` or from `$lib/server/rewards/rewardsCommon`, remove those imports and the call sites. If the entire file's purpose was points recalculation (verify by reading), DELETE the file. If it has TVL/volume regeneration logic surviving, retain those parts and remove only points-related code.
       - Specifically: `recalculate/+server.ts:10` imports `MonthlyPointsData`. Remove that import (it's still re-exported from `kv.ts` for `referrals.ts`, but if recalculate/+server.ts has no other use, the import goes too); inspect line 63+ for usage.

    9. **Edit `src/lib/server/cache.ts`:**
       - Delete `invalidateRewardsCaches` (line 63+) entirely IF no surviving caller (grep confirms only the cron used it, and cron just deleted that call).
       - Delete `invalidatePublicApiCaches` (line 51+) entirely IF it only invalidates `rewardsApy` + `rocketboost` + `allWalletData` (all rewards APIs which DEPR-01 deletes — see Plan 01-02). Keep if any non-rewards consumer remains.

    10. **Edit `src/lib/server/kv.ts`:**
       - **KEEP** `MonthlyPointsData` interface, `RewardsPoolConfig` interface, `KV_KEYS.monthlyPoints`, `KV_KEYS.monthlyPointsList`, `KV_KEYS.rewardsPool`, `KV_KEYS.rewardsPoolList`, `KV_KEYS.rewardsExcludedWallets`, `getRewardsExcludedWalletsSet()` — `referrals.ts` (KEPT per D-14) consumes these, and orderbook excluded-wallets is non-rewards.
       - Add a JSDoc comment block above `MonthlyPointsData` and the four KV keys: `// Phase 1 (D-04): The per-wallet points pipeline that wrote these keys was removed in DEPR-02. The keys + types are retained because src/lib/server/referrals.ts (referral-performance calc) still reads them. After DEPR-02, those reads will return null/empty data, and referral-performance becomes a no-op. This is expected. Existing KV entries are LEFT AS-IS — no backfill, no wipe.`

    11. **Verify post-deletion:**
       ```bash
       grep -rn "from '\$lib/server/snapshots/points'" src/   # MUST return 0 hits
       grep -rn "from '\$lib/server/rewards/rewardsCommon'" src/  # MUST return 0 hits
       grep -rn "calculateWalletPointsFromSnapshots\|updateMonthlyPoints" src/  # MUST return 0 hits
       grep -rn "MonthlyPointsData" src/  # MUST be ONLY src/lib/server/kv.ts (type def) + src/lib/server/referrals.ts (consumer)
       npm run check  # MUST exit 0
       ```

    Critical guardrails:
    - DO NOT touch `getRewardsExcludedWalletsSet` or `KV_KEYS.rewardsExcludedWallets` — orderbook reasons (per D-01 + RESEARCH §"Runtime State Inventory").
    - DO NOT touch `referrals.ts` — kept per D-14.
    - DO NOT add retry-with-backoff to `callRpc` (Pitfall 3 — that's REL-01 Phase 3).
  </action>
  <verify>
    <automated>! test -f src/lib/server/snapshots/points.ts && ! test -f src/lib/server/rewards/rewardsCommon.ts && ! test -f src/routes/api/snapshots/points/+server.ts && ! grep -q "from './points'" src/lib/server/snapshots/index.ts && ! grep -q "updateMonthlyPoints\|calculateWalletPointsFromSnapshots\|invalidateRewardsCaches" src/routes/api/cron/snapshots/+server.ts src/routes/api/snapshots/preview/+server.ts src/routes/api/snapshots/preview-stream/+server.ts && grep -q "getRewardsExcludedWalletsSet" src/lib/server/snapshots/processor.ts && grep -q "MonthlyPointsData" src/lib/server/kv.ts && npm run check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f src/lib/server/snapshots/points.ts` (deleted)
    - `! test -f src/lib/server/rewards/rewardsCommon.ts` (deleted)
    - `! test -f src/routes/api/snapshots/points/+server.ts` (deleted)
    - `! grep -q "export \\* from './points'" src/lib/server/snapshots/index.ts` (barrel re-export gone)
    - `! grep -q "updateMonthlyPoints\\|invalidateRewardsCaches" src/routes/api/cron/snapshots/+server.ts` (cron unwired)
    - `! grep -q "calculateWalletPointsFromSnapshots" src/routes/api/snapshots/preview/+server.ts src/routes/api/snapshots/preview-stream/+server.ts` (preview unwired)
    - `grep -q "getRewardsExcludedWalletsSet" src/lib/server/snapshots/processor.ts` (orderbook excluded-wallets retained)
    - `grep -q "MonthlyPointsData" src/lib/server/kv.ts` (type retained for referrals.ts)
    - `grep -c "MonthlyPointsData" src/lib/server/referrals.ts` returns ≥1 (referrals.ts still imports the type)
    - `grep -v '^#' src/lib/server/kv.ts | grep -c "D-04\\|legacy" ` returns ≥1 (D-04 comment added)
    - `npm run check 2>&1 | grep -E "error|Error"` returns no compile errors
    - `npm test 2>&1 | tail -5` shows test suite passes (or note if pre-existing failures unrelated to this change)
  </acceptance_criteria>
  <done>Per-wallet points pipeline files deleted; cron + preview routes + processor stripped of points calls; surviving snapshot pipeline writes only TVL + volume aggregates; `MonthlyPointsData` type retained in kv.ts with D-04 comment for referrals.ts consumer; svelte-check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Remove LP_SUBGRAPH_URL wiring (D-05)</name>
  <files>.env.example, src/lib/config/networks.ts</files>
  <read_first>
    - .env.example (current — confirm `LP_SUBGRAPH_URL=...` line exists at line ~30)
    - src/lib/config/networks.ts (search for `LP_SUBGRAPH_URL` or `subgraph_lp_url` consumers)
    - .planning/phase-01-shrink-the-surface-see-what-s-happening/01-RESEARCH.md §"Deletion Graph — DEPR-02" row for `.env.example:30` and `src/lib/config/networks.ts`; §"Assumptions Log" A9 (verified — only `.env.example:30` reference in source)
  </read_first>
  <action>
    Implementing D-05 (delete LP attribution subgraph wiring):

    1. **Edit `.env.example`:** delete the line `LP_SUBGRAPH_URL=https://api.goldsky.com/...` (around line 30). If there's a comment block above it about "LP attribution subgraph (rewards)" or similar, remove that too.

    2. **Inspect `src/lib/config/networks.ts`:** grep for `LP_SUBGRAPH_URL` and any field named `subgraph_lp_url`, `lpSubgraphUrl`, etc. If a Network type field exists and is consumed only by deleted code, remove the field declaration AND any per-network value assignments. If it's not consumed (`networks.ts` doesn't reference `env.LP_SUBGRAPH_URL`), no change to networks.ts.

    3. **Verify no surviving consumers:**
       ```bash
       grep -rn "LP_SUBGRAPH_URL\|subgraph_lp_url\|lpSubgraphUrl" src/ .env.example
       ```
       MUST return 0 hits.

    4. **Operational reminder (record in summary, NOT a code task):** Vercel project env mutation — remove `LP_SUBGRAPH_URL` from Vercel project settings at deploy time. This is captured in the phase summary, not a code change.
  </action>
  <verify>
    <automated>! grep -q "LP_SUBGRAPH_URL\|subgraph_lp_url\|lpSubgraphUrl" src/ .env.example -r 2>/dev/null && npm run check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rq "LP_SUBGRAPH_URL\\|subgraph_lp_url\\|lpSubgraphUrl" src/ .env.example` (zero hits across source + env example)
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>`LP_SUBGRAPH_URL` is removed from `.env.example` and any code consumer; svelte-check passes; phase summary notes the Vercel project env removal as a deploy-time runbook step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Cron → Snapshot pipeline | Cron-triggered server code performs RPC calls to public RPCs and writes to Vercel Blob / KV. Untrusted input is the chain data + subgraph data; both go through existing scrubbing/processing. |
| Admin user → /api/admin/snapshots/* | Admin-gated endpoints (require admin session). Phase 1 does NOT change auth gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Deleted points.ts handlers — risk that audit-log calls were embedded in deleted code, removing protection from a surviving admin endpoint | accept | RESEARCH §"Deletion Graph DEPR-02" performed audit-log non-regression check: only `admin/rewards/+page.svelte` (rewards-pool save) emits audit logs in the DEPR-02 deletion set, and that endpoint also goes — no surviving admin endpoint loses coverage. Verified by grep before delete in Task 2 step 1. |
| T-01-02 | Tampering | Surviving `getRewardsExcludedWalletsSet()` consumed by processor.ts/generator.ts for orderbook excluded-wallet logic — risk that deletion accidentally removes this | mitigate | Task 2 acceptance criterion: `grep -q "getRewardsExcludedWalletsSet" src/lib/server/snapshots/processor.ts` returns ≥1 after edit. Explicit "DO NOT touch" guardrail in action text. |
| T-01-03 | Information Disclosure | Legacy KV/Blob entries with rewards/points fields persist after deletion — risk that future contributors think the data is live | accept | Per D-04 (no backfill, no wipe). Documented in code comment added to `kv.ts` and `processor.ts` per Task 2 step 7+10. |
| T-01-04 | Denial of Service | `/api/snapshots/preview*` endpoints survive without rate limiting (CONCERNS.md flagged this); deletion of points step makes them faster but doesn't fix DoS | accept | DoS gap is SEC-06 in Phase 3 (now applies because we kept the snapshot pipeline per D-01). Phase 1 explicitly defers per CONTEXT D-13. No regression — preview was already unrestricted. |
| T-01-05 | Tampering | `MonthlyPointsData` / `RewardsPoolConfig` types retained in kv.ts but no longer written by the cron — `referrals.ts:calculateReferralPerformance` will read stale/null data | accept | Function gracefully degrades (`if (!monthlyData) return zeros`). This is expected behavior — rewards layer is being deleted; referral-performance becomes a no-op. Documented in kv.ts comment per Task 2 step 10. SEC-05 in Phase 3 hardens referrals separately. |

## Residual Risk

LOW. All deletions are surgical and verified by grep audit before AND after. Audit-log non-regression is enforced by RESEARCH's pre-flight check. The retained KV types (`MonthlyPointsData`/`RewardsPoolConfig`) are documented as intentionally-orphaned-for-readers per D-04. No new attack surface introduced.
</threat_model>

<verification>
- All deletions complete; `npm run check` passes.
- Admin TVL/volume views still load (manual smoke test in execute phase summary).
- Cron at `/api/cron/snapshots` still runs daily (verified by next cron tick after deploy; `vercel.json` cron schedule unchanged per RESEARCH §"Runtime State Inventory" A5).
- Cross-cutting grep audit: `grep -rn "monthlyPoints\|MonthlyPointsData" src/` returns ONLY kv.ts (type def) + referrals.ts (consumer).
</verification>

<success_criteria>
- 4 files deleted: `admin/rewards/+page.svelte`, `admin/rewards-pool/+server.ts`, `points.ts`, `rewardsCommon.ts`, `snapshots/points/+server.ts`.
- 6+ files modified: cron, preview, preview-stream, processor, snapshots/index.ts, admin layout, .env.example, possibly cache.ts + kv.ts + admin snapshot routes.
- Svelte-check exits 0; vitest suite still green.
- Per-wallet points pipeline gone; TVL + volume pipeline intact; `getRewardsExcludedWalletsSet()` retained.
- D-04 comment present in kv.ts; D-04 comment present in processor.ts.
- LP_SUBGRAPH_URL gone from `.env.example` + any source consumer.
- Phase summary records: "Vercel project env removal of `LP_SUBGRAPH_URL` is a manual deploy-time step."
</success_criteria>

<output>
After completion, create `.planning/phase-01-shrink-the-surface-see-what-s-happening/01-01-SUMMARY.md` per the standard SUMMARY template.
</output>
